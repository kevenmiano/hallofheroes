// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/SystemArmyMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface SystemArmyMsg {
  heroTemplated: number[];
  soldierTemplated: number[];
}

function createBaseSystemArmyMsg(): SystemArmyMsg {
  return { heroTemplated: [], soldierTemplated: [] };
}

export const SystemArmyMsg: MessageFns<SystemArmyMsg> = {
  encode(message: SystemArmyMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.heroTemplated) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.soldierTemplated) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SystemArmyMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSystemArmyMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.heroTemplated.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.heroTemplated.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.soldierTemplated.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.soldierTemplated.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SystemArmyMsg {
    return {
      heroTemplated: globalThis.Array.isArray(object?.heroTemplated)
        ? object.heroTemplated.map((e: any) => globalThis.Number(e))
        : [],
      soldierTemplated: globalThis.Array.isArray(object?.soldierTemplated)
        ? object.soldierTemplated.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: SystemArmyMsg): unknown {
    const obj: any = {};
    if (message.heroTemplated?.length) {
      obj.heroTemplated = message.heroTemplated.map((e) => Math.round(e));
    }
    if (message.soldierTemplated?.length) {
      obj.soldierTemplated = message.soldierTemplated.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SystemArmyMsg>, I>>(base?: I): SystemArmyMsg {
    return SystemArmyMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SystemArmyMsg>, I>>(object: I): SystemArmyMsg {
    const message = createBaseSystemArmyMsg();
    message.heroTemplated = object.heroTemplated?.map((e) => e) || [];
    message.soldierTemplated = object.soldierTemplated?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
