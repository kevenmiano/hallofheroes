// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/CampaignFogMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface CampaignFogMsg {
  row: number;
  col: number;
  forArray: number[];
  mapTempId: number;
}

function createBaseCampaignFogMsg(): CampaignFogMsg {
  return { row: 0, col: 0, forArray: [], mapTempId: 0 };
}

export const CampaignFogMsg: MessageFns<CampaignFogMsg> = {
  encode(message: CampaignFogMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.row !== 0) {
      writer.uint32(8).int32(message.row);
    }
    if (message.col !== 0) {
      writer.uint32(16).int32(message.col);
    }
    writer.uint32(26).fork();
    for (const v of message.forArray) {
      writer.int32(v);
    }
    writer.join();
    if (message.mapTempId !== 0) {
      writer.uint32(32).int32(message.mapTempId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CampaignFogMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCampaignFogMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.row = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.col = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.forArray.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.forArray.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.mapTempId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CampaignFogMsg {
    return {
      row: isSet(object.row) ? globalThis.Number(object.row) : 0,
      col: isSet(object.col) ? globalThis.Number(object.col) : 0,
      forArray: globalThis.Array.isArray(object?.forArray) ? object.forArray.map((e: any) => globalThis.Number(e)) : [],
      mapTempId: isSet(object.mapTempId) ? globalThis.Number(object.mapTempId) : 0,
    };
  },

  toJSON(message: CampaignFogMsg): unknown {
    const obj: any = {};
    if (message.row !== 0) {
      obj.row = Math.round(message.row);
    }
    if (message.col !== 0) {
      obj.col = Math.round(message.col);
    }
    if (message.forArray?.length) {
      obj.forArray = message.forArray.map((e) => Math.round(e));
    }
    if (message.mapTempId !== 0) {
      obj.mapTempId = Math.round(message.mapTempId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CampaignFogMsg>, I>>(base?: I): CampaignFogMsg {
    return CampaignFogMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CampaignFogMsg>, I>>(object: I): CampaignFogMsg {
    const message = createBaseCampaignFogMsg();
    message.row = object.row ?? 0;
    message.col = object.col ?? 0;
    message.forArray = object.forArray?.map((e) => e) || [];
    message.mapTempId = object.mapTempId ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
