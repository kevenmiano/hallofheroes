// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: leed/SingleDayMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.leed";

export interface SingleDayMsg {
  day: number;
  getReward: number;
  simpleTask: number[];
}

function createBaseSingleDayMsg(): SingleDayMsg {
  return { day: 0, getReward: 0, simpleTask: [] };
}

export const SingleDayMsg: MessageFns<SingleDayMsg> = {
  encode(message: SingleDayMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.day !== 0) {
      writer.uint32(8).int32(message.day);
    }
    if (message.getReward !== 0) {
      writer.uint32(16).int32(message.getReward);
    }
    writer.uint32(26).fork();
    for (const v of message.simpleTask) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SingleDayMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSingleDayMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.day = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.getReward = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.simpleTask.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.simpleTask.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SingleDayMsg {
    return {
      day: isSet(object.day) ? globalThis.Number(object.day) : 0,
      getReward: isSet(object.getReward) ? globalThis.Number(object.getReward) : 0,
      simpleTask: globalThis.Array.isArray(object?.simpleTask)
        ? object.simpleTask.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: SingleDayMsg): unknown {
    const obj: any = {};
    if (message.day !== 0) {
      obj.day = Math.round(message.day);
    }
    if (message.getReward !== 0) {
      obj.getReward = Math.round(message.getReward);
    }
    if (message.simpleTask?.length) {
      obj.simpleTask = message.simpleTask.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SingleDayMsg>, I>>(base?: I): SingleDayMsg {
    return SingleDayMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SingleDayMsg>, I>>(object: I): SingleDayMsg {
    const message = createBaseSingleDayMsg();
    message.day = object.day ?? 0;
    message.getReward = object.getReward ?? 0;
    message.simpleTask = object.simpleTask?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
