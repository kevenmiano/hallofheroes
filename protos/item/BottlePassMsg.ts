// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: item/BottlePassMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.item";

export interface BottlePassMsg {
  passType: number;
  infos: number[];
  count: number;
  dropId: number;
}

function createBaseBottlePassMsg(): BottlePassMsg {
  return { passType: 0, infos: [], count: 0, dropId: 0 };
}

export const BottlePassMsg: MessageFns<BottlePassMsg> = {
  encode(message: BottlePassMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.passType !== 0) {
      writer.uint32(8).int32(message.passType);
    }
    writer.uint32(18).fork();
    for (const v of message.infos) {
      writer.int32(v);
    }
    writer.join();
    if (message.count !== 0) {
      writer.uint32(24).int32(message.count);
    }
    if (message.dropId !== 0) {
      writer.uint32(32).int32(message.dropId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BottlePassMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBottlePassMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.passType = reader.int32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.infos.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.infos.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.dropId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BottlePassMsg {
    return {
      passType: isSet(object.passType) ? globalThis.Number(object.passType) : 0,
      infos: globalThis.Array.isArray(object?.infos) ? object.infos.map((e: any) => globalThis.Number(e)) : [],
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      dropId: isSet(object.dropId) ? globalThis.Number(object.dropId) : 0,
    };
  },

  toJSON(message: BottlePassMsg): unknown {
    const obj: any = {};
    if (message.passType !== 0) {
      obj.passType = Math.round(message.passType);
    }
    if (message.infos?.length) {
      obj.infos = message.infos.map((e) => Math.round(e));
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.dropId !== 0) {
      obj.dropId = Math.round(message.dropId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BottlePassMsg>, I>>(base?: I): BottlePassMsg {
    return BottlePassMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BottlePassMsg>, I>>(object: I): BottlePassMsg {
    const message = createBaseBottlePassMsg();
    message.passType = object.passType ?? 0;
    message.infos = object.infos?.map((e) => e) || [];
    message.count = object.count ?? 0;
    message.dropId = object.dropId ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
