// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: charge/ChargeOrderArrive.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.charge";

export interface ChargeOrderArrive {
  productId: string;
  orderId: string;
  point: number;
  moneyFen: number;
}

function createBaseChargeOrderArrive(): ChargeOrderArrive {
  return { productId: "", orderId: "", point: 0, moneyFen: 0 };
}

export const ChargeOrderArrive: MessageFns<ChargeOrderArrive> = {
  encode(message: ChargeOrderArrive, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.productId !== "") {
      writer.uint32(10).string(message.productId);
    }
    if (message.orderId !== "") {
      writer.uint32(18).string(message.orderId);
    }
    if (message.point !== 0) {
      writer.uint32(24).int32(message.point);
    }
    if (message.moneyFen !== 0) {
      writer.uint32(32).int32(message.moneyFen);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChargeOrderArrive {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChargeOrderArrive();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.productId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.point = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.moneyFen = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChargeOrderArrive {
    return {
      productId: isSet(object.productId) ? globalThis.String(object.productId) : "",
      orderId: isSet(object.orderId) ? globalThis.String(object.orderId) : "",
      point: isSet(object.point) ? globalThis.Number(object.point) : 0,
      moneyFen: isSet(object.moneyFen) ? globalThis.Number(object.moneyFen) : 0,
    };
  },

  toJSON(message: ChargeOrderArrive): unknown {
    const obj: any = {};
    if (message.productId !== "") {
      obj.productId = message.productId;
    }
    if (message.orderId !== "") {
      obj.orderId = message.orderId;
    }
    if (message.point !== 0) {
      obj.point = Math.round(message.point);
    }
    if (message.moneyFen !== 0) {
      obj.moneyFen = Math.round(message.moneyFen);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChargeOrderArrive>, I>>(base?: I): ChargeOrderArrive {
    return ChargeOrderArrive.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChargeOrderArrive>, I>>(object: I): ChargeOrderArrive {
    const message = createBaseChargeOrderArrive();
    message.productId = object.productId ?? "";
    message.orderId = object.orderId ?? "";
    message.point = object.point ?? 0;
    message.moneyFen = object.moneyFen ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
