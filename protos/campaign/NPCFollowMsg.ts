// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/NPCFollowMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface NPCFollowMsg {
  escortId: number;
  followId: number;
  curPosX: number;
  curPosY: number;
}

function createBaseNPCFollowMsg(): NPCFollowMsg {
  return { escortId: 0, followId: 0, curPosX: 0, curPosY: 0 };
}

export const NPCFollowMsg: MessageFns<NPCFollowMsg> = {
  encode(message: NPCFollowMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.escortId !== 0) {
      writer.uint32(8).int32(message.escortId);
    }
    if (message.followId !== 0) {
      writer.uint32(16).int32(message.followId);
    }
    if (message.curPosX !== 0) {
      writer.uint32(24).int32(message.curPosX);
    }
    if (message.curPosY !== 0) {
      writer.uint32(32).int32(message.curPosY);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NPCFollowMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNPCFollowMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.escortId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.followId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.curPosX = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.curPosY = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NPCFollowMsg {
    return {
      escortId: isSet(object.escortId) ? globalThis.Number(object.escortId) : 0,
      followId: isSet(object.followId) ? globalThis.Number(object.followId) : 0,
      curPosX: isSet(object.curPosX) ? globalThis.Number(object.curPosX) : 0,
      curPosY: isSet(object.curPosY) ? globalThis.Number(object.curPosY) : 0,
    };
  },

  toJSON(message: NPCFollowMsg): unknown {
    const obj: any = {};
    if (message.escortId !== 0) {
      obj.escortId = Math.round(message.escortId);
    }
    if (message.followId !== 0) {
      obj.followId = Math.round(message.followId);
    }
    if (message.curPosX !== 0) {
      obj.curPosX = Math.round(message.curPosX);
    }
    if (message.curPosY !== 0) {
      obj.curPosY = Math.round(message.curPosY);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NPCFollowMsg>, I>>(base?: I): NPCFollowMsg {
    return NPCFollowMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NPCFollowMsg>, I>>(object: I): NPCFollowMsg {
    const message = createBaseNPCFollowMsg();
    message.escortId = object.escortId ?? 0;
    message.followId = object.followId ?? 0;
    message.curPosX = object.curPosX ?? 0;
    message.curPosY = object.curPosY ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
