// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: mall/ShopFreshRspMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.mall";

export interface ShopFreshRspMsg {
  itemId: number[];
  freshLastdate: string;
  isCanBuy: number[];
  buyCount: number;
  maxBuyCount: number;
}

function createBaseShopFreshRspMsg(): ShopFreshRspMsg {
  return { itemId: [], freshLastdate: "", isCanBuy: [], buyCount: 0, maxBuyCount: 0 };
}

export const ShopFreshRspMsg: MessageFns<ShopFreshRspMsg> = {
  encode(message: ShopFreshRspMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.itemId) {
      writer.int32(v);
    }
    writer.join();
    if (message.freshLastdate !== "") {
      writer.uint32(18).string(message.freshLastdate);
    }
    writer.uint32(26).fork();
    for (const v of message.isCanBuy) {
      writer.int32(v);
    }
    writer.join();
    if (message.buyCount !== 0) {
      writer.uint32(32).int32(message.buyCount);
    }
    if (message.maxBuyCount !== 0) {
      writer.uint32(40).int32(message.maxBuyCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShopFreshRspMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShopFreshRspMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.itemId.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.itemId.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.freshLastdate = reader.string();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.isCanBuy.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.isCanBuy.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.buyCount = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxBuyCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShopFreshRspMsg {
    return {
      itemId: globalThis.Array.isArray(object?.itemId) ? object.itemId.map((e: any) => globalThis.Number(e)) : [],
      freshLastdate: isSet(object.freshLastdate) ? globalThis.String(object.freshLastdate) : "",
      isCanBuy: globalThis.Array.isArray(object?.isCanBuy) ? object.isCanBuy.map((e: any) => globalThis.Number(e)) : [],
      buyCount: isSet(object.buyCount) ? globalThis.Number(object.buyCount) : 0,
      maxBuyCount: isSet(object.maxBuyCount) ? globalThis.Number(object.maxBuyCount) : 0,
    };
  },

  toJSON(message: ShopFreshRspMsg): unknown {
    const obj: any = {};
    if (message.itemId?.length) {
      obj.itemId = message.itemId.map((e) => Math.round(e));
    }
    if (message.freshLastdate !== "") {
      obj.freshLastdate = message.freshLastdate;
    }
    if (message.isCanBuy?.length) {
      obj.isCanBuy = message.isCanBuy.map((e) => Math.round(e));
    }
    if (message.buyCount !== 0) {
      obj.buyCount = Math.round(message.buyCount);
    }
    if (message.maxBuyCount !== 0) {
      obj.maxBuyCount = Math.round(message.maxBuyCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShopFreshRspMsg>, I>>(base?: I): ShopFreshRspMsg {
    return ShopFreshRspMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShopFreshRspMsg>, I>>(object: I): ShopFreshRspMsg {
    const message = createBaseShopFreshRspMsg();
    message.itemId = object.itemId?.map((e) => e) || [];
    message.freshLastdate = object.freshLastdate ?? "";
    message.isCanBuy = object.isCanBuy?.map((e) => e) || [];
    message.buyCount = object.buyCount ?? 0;
    message.maxBuyCount = object.maxBuyCount ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
