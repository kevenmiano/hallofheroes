// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: fashion/FashionBook.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.fashion";

export interface FashionBook {
  tempId: number;
  isSwitch: boolean;
  appraisalSkill: number;
}

function createBaseFashionBook(): FashionBook {
  return { tempId: 0, isSwitch: false, appraisalSkill: 0 };
}

export const FashionBook: MessageFns<FashionBook> = {
  encode(message: FashionBook, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tempId !== 0) {
      writer.uint32(8).int32(message.tempId);
    }
    if (message.isSwitch !== false) {
      writer.uint32(16).bool(message.isSwitch);
    }
    if (message.appraisalSkill !== 0) {
      writer.uint32(24).int32(message.appraisalSkill);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FashionBook {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFashionBook();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.tempId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isSwitch = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.appraisalSkill = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FashionBook {
    return {
      tempId: isSet(object.tempId) ? globalThis.Number(object.tempId) : 0,
      isSwitch: isSet(object.isSwitch) ? globalThis.Boolean(object.isSwitch) : false,
      appraisalSkill: isSet(object.appraisalSkill) ? globalThis.Number(object.appraisalSkill) : 0,
    };
  },

  toJSON(message: FashionBook): unknown {
    const obj: any = {};
    if (message.tempId !== 0) {
      obj.tempId = Math.round(message.tempId);
    }
    if (message.isSwitch !== false) {
      obj.isSwitch = message.isSwitch;
    }
    if (message.appraisalSkill !== 0) {
      obj.appraisalSkill = Math.round(message.appraisalSkill);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FashionBook>, I>>(base?: I): FashionBook {
    return FashionBook.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FashionBook>, I>>(object: I): FashionBook {
    const message = createBaseFashionBook();
    message.tempId = object.tempId ?? 0;
    message.isSwitch = object.isSwitch ?? false;
    message.appraisalSkill = object.appraisalSkill ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
