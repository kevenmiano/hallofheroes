// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/BattleStartMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface BattleStartMsg {
  frame: number;
  mainType: number;
  countDown: number;
}

function createBaseBattleStartMsg(): BattleStartMsg {
  return { frame: 0, mainType: 0, countDown: 0 };
}

export const BattleStartMsg: MessageFns<BattleStartMsg> = {
  encode(message: BattleStartMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.frame !== 0) {
      writer.uint32(8).int32(message.frame);
    }
    if (message.mainType !== 0) {
      writer.uint32(16).int32(message.mainType);
    }
    if (message.countDown !== 0) {
      writer.uint32(24).int32(message.countDown);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BattleStartMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBattleStartMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.frame = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.mainType = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.countDown = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BattleStartMsg {
    return {
      frame: isSet(object.frame) ? globalThis.Number(object.frame) : 0,
      mainType: isSet(object.mainType) ? globalThis.Number(object.mainType) : 0,
      countDown: isSet(object.countDown) ? globalThis.Number(object.countDown) : 0,
    };
  },

  toJSON(message: BattleStartMsg): unknown {
    const obj: any = {};
    if (message.frame !== 0) {
      obj.frame = Math.round(message.frame);
    }
    if (message.mainType !== 0) {
      obj.mainType = Math.round(message.mainType);
    }
    if (message.countDown !== 0) {
      obj.countDown = Math.round(message.countDown);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BattleStartMsg>, I>>(base?: I): BattleStartMsg {
    return BattleStartMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BattleStartMsg>, I>>(object: I): BattleStartMsg {
    const message = createBaseBattleStartMsg();
    message.frame = object.frame ?? 0;
    message.mainType = object.mainType ?? 0;
    message.countDown = object.countDown ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
