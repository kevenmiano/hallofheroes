// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/CrossScoreMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface CrossScoreMsg {
  userName: string;
  serverName: string;
  grades: number;
  score: number;
  userId: number;
}

function createBaseCrossScoreMsg(): CrossScoreMsg {
  return { userName: "", serverName: "", grades: 0, score: 0, userId: 0 };
}

export const CrossScoreMsg: MessageFns<CrossScoreMsg> = {
  encode(message: CrossScoreMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userName !== "") {
      writer.uint32(10).string(message.userName);
    }
    if (message.serverName !== "") {
      writer.uint32(18).string(message.serverName);
    }
    if (message.grades !== 0) {
      writer.uint32(24).int32(message.grades);
    }
    if (message.score !== 0) {
      writer.uint32(32).int32(message.score);
    }
    if (message.userId !== 0) {
      writer.uint32(40).int32(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CrossScoreMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCrossScoreMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.serverName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.grades = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.score = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.userId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CrossScoreMsg {
    return {
      userName: isSet(object.userName) ? globalThis.String(object.userName) : "",
      serverName: isSet(object.serverName) ? globalThis.String(object.serverName) : "",
      grades: isSet(object.grades) ? globalThis.Number(object.grades) : 0,
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: CrossScoreMsg): unknown {
    const obj: any = {};
    if (message.userName !== "") {
      obj.userName = message.userName;
    }
    if (message.serverName !== "") {
      obj.serverName = message.serverName;
    }
    if (message.grades !== 0) {
      obj.grades = Math.round(message.grades);
    }
    if (message.score !== 0) {
      obj.score = Math.round(message.score);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CrossScoreMsg>, I>>(base?: I): CrossScoreMsg {
    return CrossScoreMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CrossScoreMsg>, I>>(object: I): CrossScoreMsg {
    const message = createBaseCrossScoreMsg();
    message.userName = object.userName ?? "";
    message.serverName = object.serverName ?? "";
    message.grades = object.grades ?? 0;
    message.score = object.score ?? 0;
    message.userId = object.userId ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
