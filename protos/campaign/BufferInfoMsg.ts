// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/BufferInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface BufferInfoMsg {
  bufferTempId: number;
  leftTime: number;
}

function createBaseBufferInfoMsg(): BufferInfoMsg {
  return { bufferTempId: 0, leftTime: 0 };
}

export const BufferInfoMsg: MessageFns<BufferInfoMsg> = {
  encode(message: BufferInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bufferTempId !== 0) {
      writer.uint32(8).int32(message.bufferTempId);
    }
    if (message.leftTime !== 0) {
      writer.uint32(16).int32(message.leftTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BufferInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBufferInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.bufferTempId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.leftTime = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BufferInfoMsg {
    return {
      bufferTempId: isSet(object.bufferTempId) ? globalThis.Number(object.bufferTempId) : 0,
      leftTime: isSet(object.leftTime) ? globalThis.Number(object.leftTime) : 0,
    };
  },

  toJSON(message: BufferInfoMsg): unknown {
    const obj: any = {};
    if (message.bufferTempId !== 0) {
      obj.bufferTempId = Math.round(message.bufferTempId);
    }
    if (message.leftTime !== 0) {
      obj.leftTime = Math.round(message.leftTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BufferInfoMsg>, I>>(base?: I): BufferInfoMsg {
    return BufferInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BufferInfoMsg>, I>>(object: I): BufferInfoMsg {
    const message = createBaseBufferInfoMsg();
    message.bufferTempId = object.bufferTempId ?? 0;
    message.leftTime = object.leftTime ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
