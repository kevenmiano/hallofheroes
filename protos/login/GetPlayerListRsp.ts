// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: login/GetPlayerListRsp.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.login";

export interface GetPlayerListRsp {
  infoList: number[];
  userName: string;
  site: string;
  loginState: number;
}

function createBaseGetPlayerListRsp(): GetPlayerListRsp {
  return { infoList: [], userName: "", site: "", loginState: 0 };
}

export const GetPlayerListRsp: MessageFns<GetPlayerListRsp> = {
  encode(message: GetPlayerListRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.infoList) {
      writer.int32(v);
    }
    writer.join();
    if (message.userName !== "") {
      writer.uint32(18).string(message.userName);
    }
    if (message.site !== "") {
      writer.uint32(26).string(message.site);
    }
    if (message.loginState !== 0) {
      writer.uint32(32).int32(message.loginState);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPlayerListRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPlayerListRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.infoList.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.infoList.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.site = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.loginState = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPlayerListRsp {
    return {
      infoList: globalThis.Array.isArray(object?.infoList) ? object.infoList.map((e: any) => globalThis.Number(e)) : [],
      userName: isSet(object.userName) ? globalThis.String(object.userName) : "",
      site: isSet(object.site) ? globalThis.String(object.site) : "",
      loginState: isSet(object.loginState) ? globalThis.Number(object.loginState) : 0,
    };
  },

  toJSON(message: GetPlayerListRsp): unknown {
    const obj: any = {};
    if (message.infoList?.length) {
      obj.infoList = message.infoList.map((e) => Math.round(e));
    }
    if (message.userName !== "") {
      obj.userName = message.userName;
    }
    if (message.site !== "") {
      obj.site = message.site;
    }
    if (message.loginState !== 0) {
      obj.loginState = Math.round(message.loginState);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPlayerListRsp>, I>>(base?: I): GetPlayerListRsp {
    return GetPlayerListRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPlayerListRsp>, I>>(object: I): GetPlayerListRsp {
    const message = createBaseGetPlayerListRsp();
    message.infoList = object.infoList?.map((e) => e) || [];
    message.userName = object.userName ?? "";
    message.site = object.site ?? "";
    message.loginState = object.loginState ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
