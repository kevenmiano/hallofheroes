// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/AltarCampaignSkillMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface AltarCampaignSkillMsg {
  templateId: number;
  CDTimer: number;
  userId: number;
  nickName: string;
  consortiaId: number;
}

function createBaseAltarCampaignSkillMsg(): AltarCampaignSkillMsg {
  return { templateId: 0, CDTimer: 0, userId: 0, nickName: "", consortiaId: 0 };
}

export const AltarCampaignSkillMsg: MessageFns<AltarCampaignSkillMsg> = {
  encode(message: AltarCampaignSkillMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.templateId !== 0) {
      writer.uint32(8).int32(message.templateId);
    }
    if (message.CDTimer !== 0) {
      writer.uint32(16).int32(message.CDTimer);
    }
    if (message.userId !== 0) {
      writer.uint32(24).int32(message.userId);
    }
    if (message.nickName !== "") {
      writer.uint32(34).string(message.nickName);
    }
    if (message.consortiaId !== 0) {
      writer.uint32(40).int32(message.consortiaId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AltarCampaignSkillMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAltarCampaignSkillMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.templateId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.CDTimer = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.userId = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.nickName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.consortiaId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AltarCampaignSkillMsg {
    return {
      templateId: isSet(object.templateId) ? globalThis.Number(object.templateId) : 0,
      CDTimer: isSet(object.CDTimer) ? globalThis.Number(object.CDTimer) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      nickName: isSet(object.nickName) ? globalThis.String(object.nickName) : "",
      consortiaId: isSet(object.consortiaId) ? globalThis.Number(object.consortiaId) : 0,
    };
  },

  toJSON(message: AltarCampaignSkillMsg): unknown {
    const obj: any = {};
    if (message.templateId !== 0) {
      obj.templateId = Math.round(message.templateId);
    }
    if (message.CDTimer !== 0) {
      obj.CDTimer = Math.round(message.CDTimer);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.nickName !== "") {
      obj.nickName = message.nickName;
    }
    if (message.consortiaId !== 0) {
      obj.consortiaId = Math.round(message.consortiaId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AltarCampaignSkillMsg>, I>>(base?: I): AltarCampaignSkillMsg {
    return AltarCampaignSkillMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AltarCampaignSkillMsg>, I>>(object: I): AltarCampaignSkillMsg {
    const message = createBaseAltarCampaignSkillMsg();
    message.templateId = object.templateId ?? 0;
    message.CDTimer = object.CDTimer ?? 0;
    message.userId = object.userId ?? 0;
    message.nickName = object.nickName ?? "";
    message.consortiaId = object.consortiaId ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
