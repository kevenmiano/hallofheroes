// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: consortia/SearchConditionMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.consortia";

export interface SearchConditionMsg {
  pageIndex: number;
  consortiaName: string;
  isNewOpen: boolean;
}

function createBaseSearchConditionMsg(): SearchConditionMsg {
  return { pageIndex: 0, consortiaName: "", isNewOpen: false };
}

export const SearchConditionMsg: MessageFns<SearchConditionMsg> = {
  encode(message: SearchConditionMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pageIndex !== 0) {
      writer.uint32(8).int32(message.pageIndex);
    }
    if (message.consortiaName !== "") {
      writer.uint32(18).string(message.consortiaName);
    }
    if (message.isNewOpen !== false) {
      writer.uint32(24).bool(message.isNewOpen);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchConditionMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchConditionMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pageIndex = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.consortiaName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isNewOpen = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchConditionMsg {
    return {
      pageIndex: isSet(object.pageIndex) ? globalThis.Number(object.pageIndex) : 0,
      consortiaName: isSet(object.consortiaName) ? globalThis.String(object.consortiaName) : "",
      isNewOpen: isSet(object.isNewOpen) ? globalThis.Boolean(object.isNewOpen) : false,
    };
  },

  toJSON(message: SearchConditionMsg): unknown {
    const obj: any = {};
    if (message.pageIndex !== 0) {
      obj.pageIndex = Math.round(message.pageIndex);
    }
    if (message.consortiaName !== "") {
      obj.consortiaName = message.consortiaName;
    }
    if (message.isNewOpen !== false) {
      obj.isNewOpen = message.isNewOpen;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchConditionMsg>, I>>(base?: I): SearchConditionMsg {
    return SearchConditionMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchConditionMsg>, I>>(object: I): SearchConditionMsg {
    const message = createBaseSearchConditionMsg();
    message.pageIndex = object.pageIndex ?? 0;
    message.consortiaName = object.consortiaName ?? "";
    message.isNewOpen = object.isNewOpen ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
