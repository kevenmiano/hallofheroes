// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: fish/FishInfo.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.fish";

export interface FishInfo {
  fishId: number;
  templateId: number;
  point: boolean;
  beginTime: string;
  channelId: number;
  state: number;
  now: string;
}

function createBaseFishInfo(): FishInfo {
  return { fishId: 0, templateId: 0, point: false, beginTime: "", channelId: 0, state: 0, now: "" };
}

export const FishInfo: MessageFns<FishInfo> = {
  encode(message: FishInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fishId !== 0) {
      writer.uint32(8).int32(message.fishId);
    }
    if (message.templateId !== 0) {
      writer.uint32(16).int32(message.templateId);
    }
    if (message.point !== false) {
      writer.uint32(24).bool(message.point);
    }
    if (message.beginTime !== "") {
      writer.uint32(34).string(message.beginTime);
    }
    if (message.channelId !== 0) {
      writer.uint32(40).int32(message.channelId);
    }
    if (message.state !== 0) {
      writer.uint32(48).int32(message.state);
    }
    if (message.now !== "") {
      writer.uint32(58).string(message.now);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FishInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFishInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.fishId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.templateId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.point = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.beginTime = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.channelId = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.state = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.now = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FishInfo {
    return {
      fishId: isSet(object.fishId) ? globalThis.Number(object.fishId) : 0,
      templateId: isSet(object.templateId) ? globalThis.Number(object.templateId) : 0,
      point: isSet(object.point) ? globalThis.Boolean(object.point) : false,
      beginTime: isSet(object.beginTime) ? globalThis.String(object.beginTime) : "",
      channelId: isSet(object.channelId) ? globalThis.Number(object.channelId) : 0,
      state: isSet(object.state) ? globalThis.Number(object.state) : 0,
      now: isSet(object.now) ? globalThis.String(object.now) : "",
    };
  },

  toJSON(message: FishInfo): unknown {
    const obj: any = {};
    if (message.fishId !== 0) {
      obj.fishId = Math.round(message.fishId);
    }
    if (message.templateId !== 0) {
      obj.templateId = Math.round(message.templateId);
    }
    if (message.point !== false) {
      obj.point = message.point;
    }
    if (message.beginTime !== "") {
      obj.beginTime = message.beginTime;
    }
    if (message.channelId !== 0) {
      obj.channelId = Math.round(message.channelId);
    }
    if (message.state !== 0) {
      obj.state = Math.round(message.state);
    }
    if (message.now !== "") {
      obj.now = message.now;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FishInfo>, I>>(base?: I): FishInfo {
    return FishInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FishInfo>, I>>(object: I): FishInfo {
    const message = createBaseFishInfo();
    message.fishId = object.fishId ?? 0;
    message.templateId = object.templateId ?? 0;
    message.point = object.point ?? false;
    message.beginTime = object.beginTime ?? "";
    message.channelId = object.channelId ?? 0;
    message.state = object.state ?? 0;
    message.now = object.now ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
