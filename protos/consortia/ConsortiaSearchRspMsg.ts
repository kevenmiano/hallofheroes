// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: consortia/ConsortiaSearchRspMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.consortia";

export interface ConsortiaSearchRspMsg {
  totalRows: number;
  consortiaInfo: number[];
  isReset: boolean;
}

function createBaseConsortiaSearchRspMsg(): ConsortiaSearchRspMsg {
  return { totalRows: 0, consortiaInfo: [], isReset: false };
}

export const ConsortiaSearchRspMsg: MessageFns<ConsortiaSearchRspMsg> = {
  encode(message: ConsortiaSearchRspMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalRows !== 0) {
      writer.uint32(8).int32(message.totalRows);
    }
    writer.uint32(18).fork();
    for (const v of message.consortiaInfo) {
      writer.int32(v);
    }
    writer.join();
    if (message.isReset !== false) {
      writer.uint32(24).bool(message.isReset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConsortiaSearchRspMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConsortiaSearchRspMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.totalRows = reader.int32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.consortiaInfo.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.consortiaInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isReset = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConsortiaSearchRspMsg {
    return {
      totalRows: isSet(object.totalRows) ? globalThis.Number(object.totalRows) : 0,
      consortiaInfo: globalThis.Array.isArray(object?.consortiaInfo)
        ? object.consortiaInfo.map((e: any) => globalThis.Number(e))
        : [],
      isReset: isSet(object.isReset) ? globalThis.Boolean(object.isReset) : false,
    };
  },

  toJSON(message: ConsortiaSearchRspMsg): unknown {
    const obj: any = {};
    if (message.totalRows !== 0) {
      obj.totalRows = Math.round(message.totalRows);
    }
    if (message.consortiaInfo?.length) {
      obj.consortiaInfo = message.consortiaInfo.map((e) => Math.round(e));
    }
    if (message.isReset !== false) {
      obj.isReset = message.isReset;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConsortiaSearchRspMsg>, I>>(base?: I): ConsortiaSearchRspMsg {
    return ConsortiaSearchRspMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConsortiaSearchRspMsg>, I>>(object: I): ConsortiaSearchRspMsg {
    const message = createBaseConsortiaSearchRspMsg();
    message.totalRows = object.totalRows ?? 0;
    message.consortiaInfo = object.consortiaInfo?.map((e) => e) || [];
    message.isReset = object.isReset ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
