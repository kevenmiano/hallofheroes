// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: active/OnlineRewardInfoRsp.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.active";

export interface OnlineRewardInfoRsp {
  op: number;
  rewardTime: number;
  leftTime: number;
  allItem: number[];
  id: number;
}

function createBaseOnlineRewardInfoRsp(): OnlineRewardInfoRsp {
  return { op: 0, rewardTime: 0, leftTime: 0, allItem: [], id: 0 };
}

export const OnlineRewardInfoRsp: MessageFns<OnlineRewardInfoRsp> = {
  encode(message: OnlineRewardInfoRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.op !== 0) {
      writer.uint32(8).int32(message.op);
    }
    if (message.rewardTime !== 0) {
      writer.uint32(16).int32(message.rewardTime);
    }
    if (message.leftTime !== 0) {
      writer.uint32(24).int32(message.leftTime);
    }
    writer.uint32(34).fork();
    for (const v of message.allItem) {
      writer.int32(v);
    }
    writer.join();
    if (message.id !== 0) {
      writer.uint32(40).int32(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OnlineRewardInfoRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOnlineRewardInfoRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.op = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rewardTime = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.leftTime = reader.int32();
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.allItem.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.allItem.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OnlineRewardInfoRsp {
    return {
      op: isSet(object.op) ? globalThis.Number(object.op) : 0,
      rewardTime: isSet(object.rewardTime) ? globalThis.Number(object.rewardTime) : 0,
      leftTime: isSet(object.leftTime) ? globalThis.Number(object.leftTime) : 0,
      allItem: globalThis.Array.isArray(object?.allItem) ? object.allItem.map((e: any) => globalThis.Number(e)) : [],
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
    };
  },

  toJSON(message: OnlineRewardInfoRsp): unknown {
    const obj: any = {};
    if (message.op !== 0) {
      obj.op = Math.round(message.op);
    }
    if (message.rewardTime !== 0) {
      obj.rewardTime = Math.round(message.rewardTime);
    }
    if (message.leftTime !== 0) {
      obj.leftTime = Math.round(message.leftTime);
    }
    if (message.allItem?.length) {
      obj.allItem = message.allItem.map((e) => Math.round(e));
    }
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OnlineRewardInfoRsp>, I>>(base?: I): OnlineRewardInfoRsp {
    return OnlineRewardInfoRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OnlineRewardInfoRsp>, I>>(object: I): OnlineRewardInfoRsp {
    const message = createBaseOnlineRewardInfoRsp();
    message.op = object.op ?? 0;
    message.rewardTime = object.rewardTime ?? 0;
    message.leftTime = object.leftTime ?? 0;
    message.allItem = object.allItem?.map((e) => e) || [];
    message.id = object.id ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
