// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: room/KillPlayerRspMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.room";

export interface KillPlayerRspMsg {
  roomId: number;
  killedPlayerId: number;
}

function createBaseKillPlayerRspMsg(): KillPlayerRspMsg {
  return { roomId: 0, killedPlayerId: 0 };
}

export const KillPlayerRspMsg: MessageFns<KillPlayerRspMsg> = {
  encode(message: KillPlayerRspMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== 0) {
      writer.uint32(8).int32(message.roomId);
    }
    if (message.killedPlayerId !== 0) {
      writer.uint32(16).int32(message.killedPlayerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KillPlayerRspMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKillPlayerRspMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.roomId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.killedPlayerId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KillPlayerRspMsg {
    return {
      roomId: isSet(object.roomId) ? globalThis.Number(object.roomId) : 0,
      killedPlayerId: isSet(object.killedPlayerId) ? globalThis.Number(object.killedPlayerId) : 0,
    };
  },

  toJSON(message: KillPlayerRspMsg): unknown {
    const obj: any = {};
    if (message.roomId !== 0) {
      obj.roomId = Math.round(message.roomId);
    }
    if (message.killedPlayerId !== 0) {
      obj.killedPlayerId = Math.round(message.killedPlayerId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KillPlayerRspMsg>, I>>(base?: I): KillPlayerRspMsg {
    return KillPlayerRspMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KillPlayerRspMsg>, I>>(object: I): KillPlayerRspMsg {
    const message = createBaseKillPlayerRspMsg();
    message.roomId = object.roomId ?? 0;
    message.killedPlayerId = object.killedPlayerId ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
