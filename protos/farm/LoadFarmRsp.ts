// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: farm/LoadFarmRsp.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.farm";

export interface LoadFarmRsp {
  friendFarmState: number[];
  farm: string;
  farmLog: number[];
}

function createBaseLoadFarmRsp(): LoadFarmRsp {
  return { friendFarmState: [], farm: "", farmLog: [] };
}

export const LoadFarmRsp: MessageFns<LoadFarmRsp> = {
  encode(message: LoadFarmRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.friendFarmState) {
      writer.int32(v);
    }
    writer.join();
    if (message.farm !== "") {
      writer.uint32(18).string(message.farm);
    }
    writer.uint32(26).fork();
    for (const v of message.farmLog) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadFarmRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadFarmRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.friendFarmState.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.friendFarmState.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.farm = reader.string();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.farmLog.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.farmLog.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoadFarmRsp {
    return {
      friendFarmState: globalThis.Array.isArray(object?.friendFarmState)
        ? object.friendFarmState.map((e: any) => globalThis.Number(e))
        : [],
      farm: isSet(object.farm) ? globalThis.String(object.farm) : "",
      farmLog: globalThis.Array.isArray(object?.farmLog) ? object.farmLog.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: LoadFarmRsp): unknown {
    const obj: any = {};
    if (message.friendFarmState?.length) {
      obj.friendFarmState = message.friendFarmState.map((e) => Math.round(e));
    }
    if (message.farm !== "") {
      obj.farm = message.farm;
    }
    if (message.farmLog?.length) {
      obj.farmLog = message.farmLog.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoadFarmRsp>, I>>(base?: I): LoadFarmRsp {
    return LoadFarmRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoadFarmRsp>, I>>(object: I): LoadFarmRsp {
    const message = createBaseLoadFarmRsp();
    message.friendFarmState = object.friendFarmState?.map((e) => e) || [];
    message.farm = object.farm ?? "";
    message.farmLog = object.farmLog?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
