// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: vip/VipToConsortiaMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.vip";

export interface VipToConsortiaMsg {
  userid: number;
  isVip: boolean;
  vipType: number;
  consortiaId: number;
}

function createBaseVipToConsortiaMsg(): VipToConsortiaMsg {
  return { userid: 0, isVip: false, vipType: 0, consortiaId: 0 };
}

export const VipToConsortiaMsg: MessageFns<VipToConsortiaMsg> = {
  encode(message: VipToConsortiaMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userid !== 0) {
      writer.uint32(8).int32(message.userid);
    }
    if (message.isVip !== false) {
      writer.uint32(16).bool(message.isVip);
    }
    if (message.vipType !== 0) {
      writer.uint32(24).int32(message.vipType);
    }
    if (message.consortiaId !== 0) {
      writer.uint32(32).int32(message.consortiaId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VipToConsortiaMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVipToConsortiaMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userid = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isVip = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.vipType = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.consortiaId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VipToConsortiaMsg {
    return {
      userid: isSet(object.userid) ? globalThis.Number(object.userid) : 0,
      isVip: isSet(object.isVip) ? globalThis.Boolean(object.isVip) : false,
      vipType: isSet(object.vipType) ? globalThis.Number(object.vipType) : 0,
      consortiaId: isSet(object.consortiaId) ? globalThis.Number(object.consortiaId) : 0,
    };
  },

  toJSON(message: VipToConsortiaMsg): unknown {
    const obj: any = {};
    if (message.userid !== 0) {
      obj.userid = Math.round(message.userid);
    }
    if (message.isVip !== false) {
      obj.isVip = message.isVip;
    }
    if (message.vipType !== 0) {
      obj.vipType = Math.round(message.vipType);
    }
    if (message.consortiaId !== 0) {
      obj.consortiaId = Math.round(message.consortiaId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VipToConsortiaMsg>, I>>(base?: I): VipToConsortiaMsg {
    return VipToConsortiaMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VipToConsortiaMsg>, I>>(object: I): VipToConsortiaMsg {
    const message = createBaseVipToConsortiaMsg();
    message.userid = object.userid ?? 0;
    message.isVip = object.isVip ?? false;
    message.vipType = object.vipType ?? 0;
    message.consortiaId = object.consortiaId ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
