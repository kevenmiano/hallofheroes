// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: active/SevenSignInfoRsp.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.active";

export interface SevenSignInfoRsp {
  startTime: number;
  days: number;
  rewardSite: number;
  rewardInfo: number[];
}

function createBaseSevenSignInfoRsp(): SevenSignInfoRsp {
  return { startTime: 0, days: 0, rewardSite: 0, rewardInfo: [] };
}

export const SevenSignInfoRsp: MessageFns<SevenSignInfoRsp> = {
  encode(message: SevenSignInfoRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.startTime !== 0) {
      writer.uint32(8).int32(message.startTime);
    }
    if (message.days !== 0) {
      writer.uint32(16).int32(message.days);
    }
    if (message.rewardSite !== 0) {
      writer.uint32(24).int32(message.rewardSite);
    }
    writer.uint32(34).fork();
    for (const v of message.rewardInfo) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SevenSignInfoRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSevenSignInfoRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.startTime = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.days = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.rewardSite = reader.int32();
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.rewardInfo.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.rewardInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SevenSignInfoRsp {
    return {
      startTime: isSet(object.startTime) ? globalThis.Number(object.startTime) : 0,
      days: isSet(object.days) ? globalThis.Number(object.days) : 0,
      rewardSite: isSet(object.rewardSite) ? globalThis.Number(object.rewardSite) : 0,
      rewardInfo: globalThis.Array.isArray(object?.rewardInfo)
        ? object.rewardInfo.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: SevenSignInfoRsp): unknown {
    const obj: any = {};
    if (message.startTime !== 0) {
      obj.startTime = Math.round(message.startTime);
    }
    if (message.days !== 0) {
      obj.days = Math.round(message.days);
    }
    if (message.rewardSite !== 0) {
      obj.rewardSite = Math.round(message.rewardSite);
    }
    if (message.rewardInfo?.length) {
      obj.rewardInfo = message.rewardInfo.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SevenSignInfoRsp>, I>>(base?: I): SevenSignInfoRsp {
    return SevenSignInfoRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SevenSignInfoRsp>, I>>(object: I): SevenSignInfoRsp {
    const message = createBaseSevenSignInfoRsp();
    message.startTime = object.startTime ?? 0;
    message.days = object.days ?? 0;
    message.rewardSite = object.rewardSite ?? 0;
    message.rewardInfo = object.rewardInfo?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
