// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: simple/AuctionItemsMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.simple";

export interface AuctionItemsMsg {
  pageIdx: number;
  pageCount: number;
  itemBuy: number[];
  sellItem: number[];
}

function createBaseAuctionItemsMsg(): AuctionItemsMsg {
  return { pageIdx: 0, pageCount: 0, itemBuy: [], sellItem: [] };
}

export const AuctionItemsMsg: MessageFns<AuctionItemsMsg> = {
  encode(message: AuctionItemsMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pageIdx !== 0) {
      writer.uint32(8).int32(message.pageIdx);
    }
    if (message.pageCount !== 0) {
      writer.uint32(16).int32(message.pageCount);
    }
    writer.uint32(26).fork();
    for (const v of message.itemBuy) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(34).fork();
    for (const v of message.sellItem) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuctionItemsMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuctionItemsMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pageIdx = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pageCount = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.itemBuy.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.itemBuy.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag === 32) {
            message.sellItem.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.sellItem.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuctionItemsMsg {
    return {
      pageIdx: isSet(object.pageIdx) ? globalThis.Number(object.pageIdx) : 0,
      pageCount: isSet(object.pageCount) ? globalThis.Number(object.pageCount) : 0,
      itemBuy: globalThis.Array.isArray(object?.itemBuy) ? object.itemBuy.map((e: any) => globalThis.Number(e)) : [],
      sellItem: globalThis.Array.isArray(object?.sellItem) ? object.sellItem.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: AuctionItemsMsg): unknown {
    const obj: any = {};
    if (message.pageIdx !== 0) {
      obj.pageIdx = Math.round(message.pageIdx);
    }
    if (message.pageCount !== 0) {
      obj.pageCount = Math.round(message.pageCount);
    }
    if (message.itemBuy?.length) {
      obj.itemBuy = message.itemBuy.map((e) => Math.round(e));
    }
    if (message.sellItem?.length) {
      obj.sellItem = message.sellItem.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuctionItemsMsg>, I>>(base?: I): AuctionItemsMsg {
    return AuctionItemsMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuctionItemsMsg>, I>>(object: I): AuctionItemsMsg {
    const message = createBaseAuctionItemsMsg();
    message.pageIdx = object.pageIdx ?? 0;
    message.pageCount = object.pageCount ?? 0;
    message.itemBuy = object.itemBuy?.map((e) => e) || [];
    message.sellItem = object.sellItem?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
