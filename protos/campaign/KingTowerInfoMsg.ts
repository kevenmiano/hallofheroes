// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/KingTowerInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface KingTowerInfoMsg {
  enterCount: number;
  currentIndex: number;
  maxIndex: number;
  campaignId: number;
  currentStep: number;
  rewardCount: number;
}

function createBaseKingTowerInfoMsg(): KingTowerInfoMsg {
  return { enterCount: 0, currentIndex: 0, maxIndex: 0, campaignId: 0, currentStep: 0, rewardCount: 0 };
}

export const KingTowerInfoMsg: MessageFns<KingTowerInfoMsg> = {
  encode(message: KingTowerInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enterCount !== 0) {
      writer.uint32(8).int32(message.enterCount);
    }
    if (message.currentIndex !== 0) {
      writer.uint32(16).int32(message.currentIndex);
    }
    if (message.maxIndex !== 0) {
      writer.uint32(24).int32(message.maxIndex);
    }
    if (message.campaignId !== 0) {
      writer.uint32(32).int32(message.campaignId);
    }
    if (message.currentStep !== 0) {
      writer.uint32(40).int32(message.currentStep);
    }
    if (message.rewardCount !== 0) {
      writer.uint32(48).int32(message.rewardCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KingTowerInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKingTowerInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enterCount = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.currentIndex = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxIndex = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.campaignId = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.currentStep = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.rewardCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KingTowerInfoMsg {
    return {
      enterCount: isSet(object.enterCount) ? globalThis.Number(object.enterCount) : 0,
      currentIndex: isSet(object.currentIndex) ? globalThis.Number(object.currentIndex) : 0,
      maxIndex: isSet(object.maxIndex) ? globalThis.Number(object.maxIndex) : 0,
      campaignId: isSet(object.campaignId) ? globalThis.Number(object.campaignId) : 0,
      currentStep: isSet(object.currentStep) ? globalThis.Number(object.currentStep) : 0,
      rewardCount: isSet(object.rewardCount) ? globalThis.Number(object.rewardCount) : 0,
    };
  },

  toJSON(message: KingTowerInfoMsg): unknown {
    const obj: any = {};
    if (message.enterCount !== 0) {
      obj.enterCount = Math.round(message.enterCount);
    }
    if (message.currentIndex !== 0) {
      obj.currentIndex = Math.round(message.currentIndex);
    }
    if (message.maxIndex !== 0) {
      obj.maxIndex = Math.round(message.maxIndex);
    }
    if (message.campaignId !== 0) {
      obj.campaignId = Math.round(message.campaignId);
    }
    if (message.currentStep !== 0) {
      obj.currentStep = Math.round(message.currentStep);
    }
    if (message.rewardCount !== 0) {
      obj.rewardCount = Math.round(message.rewardCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KingTowerInfoMsg>, I>>(base?: I): KingTowerInfoMsg {
    return KingTowerInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KingTowerInfoMsg>, I>>(object: I): KingTowerInfoMsg {
    const message = createBaseKingTowerInfoMsg();
    message.enterCount = object.enterCount ?? 0;
    message.currentIndex = object.currentIndex ?? 0;
    message.maxIndex = object.maxIndex ?? 0;
    message.campaignId = object.campaignId ?? 0;
    message.currentStep = object.currentStep ?? 0;
    message.rewardCount = object.rewardCount ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
