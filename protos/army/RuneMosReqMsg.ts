// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: army/RuneMosReqMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.army";

export interface RuneMosReqMsg {
  runeId: number;
  runeIndex: number;
  runeStoneId: number;
  beginPos: number;
  targetRuneHoleId: number;
  targetIndex: number;
}

function createBaseRuneMosReqMsg(): RuneMosReqMsg {
  return { runeId: 0, runeIndex: 0, runeStoneId: 0, beginPos: 0, targetRuneHoleId: 0, targetIndex: 0 };
}

export const RuneMosReqMsg: MessageFns<RuneMosReqMsg> = {
  encode(message: RuneMosReqMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runeId !== 0) {
      writer.uint32(8).int32(message.runeId);
    }
    if (message.runeIndex !== 0) {
      writer.uint32(16).int32(message.runeIndex);
    }
    if (message.runeStoneId !== 0) {
      writer.uint32(24).int32(message.runeStoneId);
    }
    if (message.beginPos !== 0) {
      writer.uint32(32).int32(message.beginPos);
    }
    if (message.targetRuneHoleId !== 0) {
      writer.uint32(40).int32(message.targetRuneHoleId);
    }
    if (message.targetIndex !== 0) {
      writer.uint32(48).int32(message.targetIndex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RuneMosReqMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRuneMosReqMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.runeId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.runeIndex = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.runeStoneId = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.beginPos = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.targetRuneHoleId = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.targetIndex = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RuneMosReqMsg {
    return {
      runeId: isSet(object.runeId) ? globalThis.Number(object.runeId) : 0,
      runeIndex: isSet(object.runeIndex) ? globalThis.Number(object.runeIndex) : 0,
      runeStoneId: isSet(object.runeStoneId) ? globalThis.Number(object.runeStoneId) : 0,
      beginPos: isSet(object.beginPos) ? globalThis.Number(object.beginPos) : 0,
      targetRuneHoleId: isSet(object.targetRuneHoleId) ? globalThis.Number(object.targetRuneHoleId) : 0,
      targetIndex: isSet(object.targetIndex) ? globalThis.Number(object.targetIndex) : 0,
    };
  },

  toJSON(message: RuneMosReqMsg): unknown {
    const obj: any = {};
    if (message.runeId !== 0) {
      obj.runeId = Math.round(message.runeId);
    }
    if (message.runeIndex !== 0) {
      obj.runeIndex = Math.round(message.runeIndex);
    }
    if (message.runeStoneId !== 0) {
      obj.runeStoneId = Math.round(message.runeStoneId);
    }
    if (message.beginPos !== 0) {
      obj.beginPos = Math.round(message.beginPos);
    }
    if (message.targetRuneHoleId !== 0) {
      obj.targetRuneHoleId = Math.round(message.targetRuneHoleId);
    }
    if (message.targetIndex !== 0) {
      obj.targetIndex = Math.round(message.targetIndex);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RuneMosReqMsg>, I>>(base?: I): RuneMosReqMsg {
    return RuneMosReqMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RuneMosReqMsg>, I>>(object: I): RuneMosReqMsg {
    const message = createBaseRuneMosReqMsg();
    message.runeId = object.runeId ?? 0;
    message.runeIndex = object.runeIndex ?? 0;
    message.runeStoneId = object.runeStoneId ?? 0;
    message.beginPos = object.beginPos ?? 0;
    message.targetRuneHoleId = object.targetRuneHoleId ?? 0;
    message.targetIndex = object.targetIndex ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
