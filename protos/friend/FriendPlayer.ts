// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: friend/FriendPlayer.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.friend";

export interface FriendPlayer {
  playerId: number;
  nickName: string;
  level: number;
  sex: number;
  attack: number;
  mapName: string;
  headId: number;
  job: number;
  frameId: number;
}

function createBaseFriendPlayer(): FriendPlayer {
  return { playerId: 0, nickName: "", level: 0, sex: 0, attack: 0, mapName: "", headId: 0, job: 0, frameId: 0 };
}

export const FriendPlayer: MessageFns<FriendPlayer> = {
  encode(message: FriendPlayer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerId !== 0) {
      writer.uint32(8).int32(message.playerId);
    }
    if (message.nickName !== "") {
      writer.uint32(18).string(message.nickName);
    }
    if (message.level !== 0) {
      writer.uint32(24).int32(message.level);
    }
    if (message.sex !== 0) {
      writer.uint32(32).int32(message.sex);
    }
    if (message.attack !== 0) {
      writer.uint32(40).int32(message.attack);
    }
    if (message.mapName !== "") {
      writer.uint32(50).string(message.mapName);
    }
    if (message.headId !== 0) {
      writer.uint32(56).int32(message.headId);
    }
    if (message.job !== 0) {
      writer.uint32(64).int32(message.job);
    }
    if (message.frameId !== 0) {
      writer.uint32(72).int32(message.frameId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FriendPlayer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendPlayer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.playerId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nickName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.level = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sex = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.attack = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.mapName = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.headId = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.job = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.frameId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendPlayer {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      nickName: isSet(object.nickName) ? globalThis.String(object.nickName) : "",
      level: isSet(object.level) ? globalThis.Number(object.level) : 0,
      sex: isSet(object.sex) ? globalThis.Number(object.sex) : 0,
      attack: isSet(object.attack) ? globalThis.Number(object.attack) : 0,
      mapName: isSet(object.mapName) ? globalThis.String(object.mapName) : "",
      headId: isSet(object.headId) ? globalThis.Number(object.headId) : 0,
      job: isSet(object.job) ? globalThis.Number(object.job) : 0,
      frameId: isSet(object.frameId) ? globalThis.Number(object.frameId) : 0,
    };
  },

  toJSON(message: FriendPlayer): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.nickName !== "") {
      obj.nickName = message.nickName;
    }
    if (message.level !== 0) {
      obj.level = Math.round(message.level);
    }
    if (message.sex !== 0) {
      obj.sex = Math.round(message.sex);
    }
    if (message.attack !== 0) {
      obj.attack = Math.round(message.attack);
    }
    if (message.mapName !== "") {
      obj.mapName = message.mapName;
    }
    if (message.headId !== 0) {
      obj.headId = Math.round(message.headId);
    }
    if (message.job !== 0) {
      obj.job = Math.round(message.job);
    }
    if (message.frameId !== 0) {
      obj.frameId = Math.round(message.frameId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendPlayer>, I>>(base?: I): FriendPlayer {
    return FriendPlayer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendPlayer>, I>>(object: I): FriendPlayer {
    const message = createBaseFriendPlayer();
    message.playerId = object.playerId ?? 0;
    message.nickName = object.nickName ?? "";
    message.level = object.level ?? 0;
    message.sex = object.sex ?? 0;
    message.attack = object.attack ?? 0;
    message.mapName = object.mapName ?? "";
    message.headId = object.headId ?? 0;
    message.job = object.job ?? 0;
    message.frameId = object.frameId ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
