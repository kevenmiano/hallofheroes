// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: outcitywar/BuildStateMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.outcitywar";

export interface BuildStateMsg {
  nodeId: number;
  defenseSite: number;
  oneBuildStateMsg: number[];
  buffType: number;
  buffCondition: number;
}

function createBaseBuildStateMsg(): BuildStateMsg {
  return { nodeId: 0, defenseSite: 0, oneBuildStateMsg: [], buffType: 0, buffCondition: 0 };
}

export const BuildStateMsg: MessageFns<BuildStateMsg> = {
  encode(message: BuildStateMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeId !== 0) {
      writer.uint32(8).int32(message.nodeId);
    }
    if (message.defenseSite !== 0) {
      writer.uint32(16).int32(message.defenseSite);
    }
    writer.uint32(26).fork();
    for (const v of message.oneBuildStateMsg) {
      writer.int32(v);
    }
    writer.join();
    if (message.buffType !== 0) {
      writer.uint32(32).int32(message.buffType);
    }
    if (message.buffCondition !== 0) {
      writer.uint32(40).int32(message.buffCondition);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildStateMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuildStateMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.nodeId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.defenseSite = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.oneBuildStateMsg.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.oneBuildStateMsg.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.buffType = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.buffCondition = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuildStateMsg {
    return {
      nodeId: isSet(object.nodeId) ? globalThis.Number(object.nodeId) : 0,
      defenseSite: isSet(object.defenseSite) ? globalThis.Number(object.defenseSite) : 0,
      oneBuildStateMsg: globalThis.Array.isArray(object?.oneBuildStateMsg)
        ? object.oneBuildStateMsg.map((e: any) => globalThis.Number(e))
        : [],
      buffType: isSet(object.buffType) ? globalThis.Number(object.buffType) : 0,
      buffCondition: isSet(object.buffCondition) ? globalThis.Number(object.buffCondition) : 0,
    };
  },

  toJSON(message: BuildStateMsg): unknown {
    const obj: any = {};
    if (message.nodeId !== 0) {
      obj.nodeId = Math.round(message.nodeId);
    }
    if (message.defenseSite !== 0) {
      obj.defenseSite = Math.round(message.defenseSite);
    }
    if (message.oneBuildStateMsg?.length) {
      obj.oneBuildStateMsg = message.oneBuildStateMsg.map((e) => Math.round(e));
    }
    if (message.buffType !== 0) {
      obj.buffType = Math.round(message.buffType);
    }
    if (message.buffCondition !== 0) {
      obj.buffCondition = Math.round(message.buffCondition);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuildStateMsg>, I>>(base?: I): BuildStateMsg {
    return BuildStateMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuildStateMsg>, I>>(object: I): BuildStateMsg {
    const message = createBaseBuildStateMsg();
    message.nodeId = object.nodeId ?? 0;
    message.defenseSite = object.defenseSite ?? 0;
    message.oneBuildStateMsg = object.oneBuildStateMsg?.map((e) => e) || [];
    message.buffType = object.buffType ?? 0;
    message.buffCondition = object.buffCondition ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
