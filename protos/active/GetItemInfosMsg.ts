// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: active/GetItemInfosMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.active";

export interface GetItemInfosMsg {
  interval: number;
  times: number;
  itemInfo: number[];
}

function createBaseGetItemInfosMsg(): GetItemInfosMsg {
  return { interval: 0, times: 0, itemInfo: [] };
}

export const GetItemInfosMsg: MessageFns<GetItemInfosMsg> = {
  encode(message: GetItemInfosMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.interval !== 0) {
      writer.uint32(8).int32(message.interval);
    }
    if (message.times !== 0) {
      writer.uint32(16).int32(message.times);
    }
    writer.uint32(26).fork();
    for (const v of message.itemInfo) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetItemInfosMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetItemInfosMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.interval = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.times = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.itemInfo.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.itemInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetItemInfosMsg {
    return {
      interval: isSet(object.interval) ? globalThis.Number(object.interval) : 0,
      times: isSet(object.times) ? globalThis.Number(object.times) : 0,
      itemInfo: globalThis.Array.isArray(object?.itemInfo) ? object.itemInfo.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: GetItemInfosMsg): unknown {
    const obj: any = {};
    if (message.interval !== 0) {
      obj.interval = Math.round(message.interval);
    }
    if (message.times !== 0) {
      obj.times = Math.round(message.times);
    }
    if (message.itemInfo?.length) {
      obj.itemInfo = message.itemInfo.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetItemInfosMsg>, I>>(base?: I): GetItemInfosMsg {
    return GetItemInfosMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetItemInfosMsg>, I>>(object: I): GetItemInfosMsg {
    const message = createBaseGetItemInfosMsg();
    message.interval = object.interval ?? 0;
    message.times = object.times ?? 0;
    message.itemInfo = object.itemInfo?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
