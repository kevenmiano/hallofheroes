// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: minigame/GemMoveInviteReqMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.minigame";

export interface GemMoveInviteReqMsg {
  friendId: number;
  inviteTimes: number;
  helpNums: number;
}

function createBaseGemMoveInviteReqMsg(): GemMoveInviteReqMsg {
  return { friendId: 0, inviteTimes: 0, helpNums: 0 };
}

export const GemMoveInviteReqMsg: MessageFns<GemMoveInviteReqMsg> = {
  encode(message: GemMoveInviteReqMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.friendId !== 0) {
      writer.uint32(8).int32(message.friendId);
    }
    if (message.inviteTimes !== 0) {
      writer.uint32(16).int32(message.inviteTimes);
    }
    if (message.helpNums !== 0) {
      writer.uint32(24).int32(message.helpNums);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GemMoveInviteReqMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGemMoveInviteReqMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.friendId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.inviteTimes = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.helpNums = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GemMoveInviteReqMsg {
    return {
      friendId: isSet(object.friendId) ? globalThis.Number(object.friendId) : 0,
      inviteTimes: isSet(object.inviteTimes) ? globalThis.Number(object.inviteTimes) : 0,
      helpNums: isSet(object.helpNums) ? globalThis.Number(object.helpNums) : 0,
    };
  },

  toJSON(message: GemMoveInviteReqMsg): unknown {
    const obj: any = {};
    if (message.friendId !== 0) {
      obj.friendId = Math.round(message.friendId);
    }
    if (message.inviteTimes !== 0) {
      obj.inviteTimes = Math.round(message.inviteTimes);
    }
    if (message.helpNums !== 0) {
      obj.helpNums = Math.round(message.helpNums);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GemMoveInviteReqMsg>, I>>(base?: I): GemMoveInviteReqMsg {
    return GemMoveInviteReqMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GemMoveInviteReqMsg>, I>>(object: I): GemMoveInviteReqMsg {
    const message = createBaseGemMoveInviteReqMsg();
    message.friendId = object.friendId ?? 0;
    message.inviteTimes = object.inviteTimes ?? 0;
    message.helpNums = object.helpNums ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
