// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: item/ArrangeReq.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.item";

export interface ArrangeReq {
  isStackItem: boolean;
  bagType: number;
  arrangeInfo: number[];
}

function createBaseArrangeReq(): ArrangeReq {
  return { isStackItem: false, bagType: 0, arrangeInfo: [] };
}

export const ArrangeReq: MessageFns<ArrangeReq> = {
  encode(message: ArrangeReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isStackItem !== false) {
      writer.uint32(8).bool(message.isStackItem);
    }
    if (message.bagType !== 0) {
      writer.uint32(16).int32(message.bagType);
    }
    writer.uint32(26).fork();
    for (const v of message.arrangeInfo) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArrangeReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArrangeReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isStackItem = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.bagType = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.arrangeInfo.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.arrangeInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArrangeReq {
    return {
      isStackItem: isSet(object.isStackItem) ? globalThis.Boolean(object.isStackItem) : false,
      bagType: isSet(object.bagType) ? globalThis.Number(object.bagType) : 0,
      arrangeInfo: globalThis.Array.isArray(object?.arrangeInfo)
        ? object.arrangeInfo.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: ArrangeReq): unknown {
    const obj: any = {};
    if (message.isStackItem !== false) {
      obj.isStackItem = message.isStackItem;
    }
    if (message.bagType !== 0) {
      obj.bagType = Math.round(message.bagType);
    }
    if (message.arrangeInfo?.length) {
      obj.arrangeInfo = message.arrangeInfo.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArrangeReq>, I>>(base?: I): ArrangeReq {
    return ArrangeReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArrangeReq>, I>>(object: I): ArrangeReq {
    const message = createBaseArrangeReq();
    message.isStackItem = object.isStackItem ?? false;
    message.bagType = object.bagType ?? 0;
    message.arrangeInfo = object.arrangeInfo?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
