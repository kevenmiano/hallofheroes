// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/HangupStateListMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface HangupStateListMsg {
  maxCount: number;
  hangupState: number[];
}

function createBaseHangupStateListMsg(): HangupStateListMsg {
  return { maxCount: 0, hangupState: [] };
}

export const HangupStateListMsg: MessageFns<HangupStateListMsg> = {
  encode(message: HangupStateListMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxCount !== 0) {
      writer.uint32(8).int32(message.maxCount);
    }
    writer.uint32(18).fork();
    for (const v of message.hangupState) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HangupStateListMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHangupStateListMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxCount = reader.int32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.hangupState.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.hangupState.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HangupStateListMsg {
    return {
      maxCount: isSet(object.maxCount) ? globalThis.Number(object.maxCount) : 0,
      hangupState: globalThis.Array.isArray(object?.hangupState)
        ? object.hangupState.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: HangupStateListMsg): unknown {
    const obj: any = {};
    if (message.maxCount !== 0) {
      obj.maxCount = Math.round(message.maxCount);
    }
    if (message.hangupState?.length) {
      obj.hangupState = message.hangupState.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HangupStateListMsg>, I>>(base?: I): HangupStateListMsg {
    return HangupStateListMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HangupStateListMsg>, I>>(object: I): HangupStateListMsg {
    const message = createBaseHangupStateListMsg();
    message.maxCount = object.maxCount ?? 0;
    message.hangupState = object.hangupState?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
