// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: consortia/ConsortiaContributeMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.consortia";

export interface ConsortiaContributeMsg {
  nickName: string;
  totalOffer: number;
  todayOffer: number;
}

function createBaseConsortiaContributeMsg(): ConsortiaContributeMsg {
  return { nickName: "", totalOffer: 0, todayOffer: 0 };
}

export const ConsortiaContributeMsg: MessageFns<ConsortiaContributeMsg> = {
  encode(message: ConsortiaContributeMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nickName !== "") {
      writer.uint32(10).string(message.nickName);
    }
    if (message.totalOffer !== 0) {
      writer.uint32(16).int32(message.totalOffer);
    }
    if (message.todayOffer !== 0) {
      writer.uint32(24).int32(message.todayOffer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConsortiaContributeMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConsortiaContributeMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nickName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalOffer = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.todayOffer = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConsortiaContributeMsg {
    return {
      nickName: isSet(object.nickName) ? globalThis.String(object.nickName) : "",
      totalOffer: isSet(object.totalOffer) ? globalThis.Number(object.totalOffer) : 0,
      todayOffer: isSet(object.todayOffer) ? globalThis.Number(object.todayOffer) : 0,
    };
  },

  toJSON(message: ConsortiaContributeMsg): unknown {
    const obj: any = {};
    if (message.nickName !== "") {
      obj.nickName = message.nickName;
    }
    if (message.totalOffer !== 0) {
      obj.totalOffer = Math.round(message.totalOffer);
    }
    if (message.todayOffer !== 0) {
      obj.todayOffer = Math.round(message.todayOffer);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConsortiaContributeMsg>, I>>(base?: I): ConsortiaContributeMsg {
    return ConsortiaContributeMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConsortiaContributeMsg>, I>>(object: I): ConsortiaContributeMsg {
    const message = createBaseConsortiaContributeMsg();
    message.nickName = object.nickName ?? "";
    message.totalOffer = object.totalOffer ?? 0;
    message.todayOffer = object.todayOffer ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
