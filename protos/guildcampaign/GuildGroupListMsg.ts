// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: guildcampaign/GuildGroupListMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.guildcampaign";

export interface GuildGroupListMsg {
  group: number[];
  orderTime: string;
  isGuildSize: boolean;
}

function createBaseGuildGroupListMsg(): GuildGroupListMsg {
  return { group: [], orderTime: "", isGuildSize: false };
}

export const GuildGroupListMsg: MessageFns<GuildGroupListMsg> = {
  encode(message: GuildGroupListMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.group) {
      writer.int32(v);
    }
    writer.join();
    if (message.orderTime !== "") {
      writer.uint32(18).string(message.orderTime);
    }
    if (message.isGuildSize !== false) {
      writer.uint32(24).bool(message.isGuildSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GuildGroupListMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGuildGroupListMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.group.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.group.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orderTime = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isGuildSize = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GuildGroupListMsg {
    return {
      group: globalThis.Array.isArray(object?.group) ? object.group.map((e: any) => globalThis.Number(e)) : [],
      orderTime: isSet(object.orderTime) ? globalThis.String(object.orderTime) : "",
      isGuildSize: isSet(object.isGuildSize) ? globalThis.Boolean(object.isGuildSize) : false,
    };
  },

  toJSON(message: GuildGroupListMsg): unknown {
    const obj: any = {};
    if (message.group?.length) {
      obj.group = message.group.map((e) => Math.round(e));
    }
    if (message.orderTime !== "") {
      obj.orderTime = message.orderTime;
    }
    if (message.isGuildSize !== false) {
      obj.isGuildSize = message.isGuildSize;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GuildGroupListMsg>, I>>(base?: I): GuildGroupListMsg {
    return GuildGroupListMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GuildGroupListMsg>, I>>(object: I): GuildGroupListMsg {
    const message = createBaseGuildGroupListMsg();
    message.group = object.group?.map((e) => e) || [];
    message.orderTime = object.orderTime ?? "";
    message.isGuildSize = object.isGuildSize ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
