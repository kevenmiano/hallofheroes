// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: army/PawnChangeMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.army";

export interface PawnChangeMsg {
  curPawnTempId: number;
  tarPawnTempId: number;
  payType: number;
}

function createBasePawnChangeMsg(): PawnChangeMsg {
  return { curPawnTempId: 0, tarPawnTempId: 0, payType: 0 };
}

export const PawnChangeMsg: MessageFns<PawnChangeMsg> = {
  encode(message: PawnChangeMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.curPawnTempId !== 0) {
      writer.uint32(8).int32(message.curPawnTempId);
    }
    if (message.tarPawnTempId !== 0) {
      writer.uint32(16).int32(message.tarPawnTempId);
    }
    if (message.payType !== 0) {
      writer.uint32(24).int32(message.payType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PawnChangeMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePawnChangeMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.curPawnTempId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tarPawnTempId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.payType = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PawnChangeMsg {
    return {
      curPawnTempId: isSet(object.curPawnTempId) ? globalThis.Number(object.curPawnTempId) : 0,
      tarPawnTempId: isSet(object.tarPawnTempId) ? globalThis.Number(object.tarPawnTempId) : 0,
      payType: isSet(object.payType) ? globalThis.Number(object.payType) : 0,
    };
  },

  toJSON(message: PawnChangeMsg): unknown {
    const obj: any = {};
    if (message.curPawnTempId !== 0) {
      obj.curPawnTempId = Math.round(message.curPawnTempId);
    }
    if (message.tarPawnTempId !== 0) {
      obj.tarPawnTempId = Math.round(message.tarPawnTempId);
    }
    if (message.payType !== 0) {
      obj.payType = Math.round(message.payType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PawnChangeMsg>, I>>(base?: I): PawnChangeMsg {
    return PawnChangeMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PawnChangeMsg>, I>>(object: I): PawnChangeMsg {
    const message = createBasePawnChangeMsg();
    message.curPawnTempId = object.curPawnTempId ?? 0;
    message.tarPawnTempId = object.tarPawnTempId ?? 0;
    message.payType = object.payType ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
