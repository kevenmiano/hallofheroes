// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: vehicle/VehiclePickPropMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.vehicle";

export interface VehiclePickPropMsg {
  propNodeId: string;
  playerId: string;
  serverName: string;
}

function createBaseVehiclePickPropMsg(): VehiclePickPropMsg {
  return { propNodeId: "", playerId: "", serverName: "" };
}

export const VehiclePickPropMsg: MessageFns<VehiclePickPropMsg> = {
  encode(message: VehiclePickPropMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.propNodeId !== "") {
      writer.uint32(10).string(message.propNodeId);
    }
    if (message.playerId !== "") {
      writer.uint32(18).string(message.playerId);
    }
    if (message.serverName !== "") {
      writer.uint32(26).string(message.serverName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VehiclePickPropMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVehiclePickPropMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.propNodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.playerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.serverName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VehiclePickPropMsg {
    return {
      propNodeId: isSet(object.propNodeId) ? globalThis.String(object.propNodeId) : "",
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      serverName: isSet(object.serverName) ? globalThis.String(object.serverName) : "",
    };
  },

  toJSON(message: VehiclePickPropMsg): unknown {
    const obj: any = {};
    if (message.propNodeId !== "") {
      obj.propNodeId = message.propNodeId;
    }
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.serverName !== "") {
      obj.serverName = message.serverName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VehiclePickPropMsg>, I>>(base?: I): VehiclePickPropMsg {
    return VehiclePickPropMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VehiclePickPropMsg>, I>>(object: I): VehiclePickPropMsg {
    const message = createBaseVehiclePickPropMsg();
    message.propNodeId = object.propNodeId ?? "";
    message.playerId = object.playerId ?? "";
    message.serverName = object.serverName ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
