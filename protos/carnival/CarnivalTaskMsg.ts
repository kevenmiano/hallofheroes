// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: carnival/CarnivalTaskMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.carnival";

export interface CarnivalTaskMsg {
  taskId: number;
  data: number;
  isReward: boolean;
}

function createBaseCarnivalTaskMsg(): CarnivalTaskMsg {
  return { taskId: 0, data: 0, isReward: false };
}

export const CarnivalTaskMsg: MessageFns<CarnivalTaskMsg> = {
  encode(message: CarnivalTaskMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== 0) {
      writer.uint32(8).int32(message.taskId);
    }
    if (message.data !== 0) {
      writer.uint32(16).int32(message.data);
    }
    if (message.isReward !== false) {
      writer.uint32(24).bool(message.isReward);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CarnivalTaskMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCarnivalTaskMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.taskId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.data = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isReward = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CarnivalTaskMsg {
    return {
      taskId: isSet(object.taskId) ? globalThis.Number(object.taskId) : 0,
      data: isSet(object.data) ? globalThis.Number(object.data) : 0,
      isReward: isSet(object.isReward) ? globalThis.Boolean(object.isReward) : false,
    };
  },

  toJSON(message: CarnivalTaskMsg): unknown {
    const obj: any = {};
    if (message.taskId !== 0) {
      obj.taskId = Math.round(message.taskId);
    }
    if (message.data !== 0) {
      obj.data = Math.round(message.data);
    }
    if (message.isReward !== false) {
      obj.isReward = message.isReward;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CarnivalTaskMsg>, I>>(base?: I): CarnivalTaskMsg {
    return CarnivalTaskMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CarnivalTaskMsg>, I>>(object: I): CarnivalTaskMsg {
    const message = createBaseCarnivalTaskMsg();
    message.taskId = object.taskId ?? 0;
    message.data = object.data ?? 0;
    message.isReward = object.isReward ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
