// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: crossguild/CrossGuildEndMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.crossguild";

export interface CrossGuildEndMsg {
  redScore: number;
  blueScore: number;
  redName: string;
  blueName: string;
  isWin: boolean;
  redRankList: number[];
  blueRankList: number[];
  rank: number;
}

function createBaseCrossGuildEndMsg(): CrossGuildEndMsg {
  return {
    redScore: 0,
    blueScore: 0,
    redName: "",
    blueName: "",
    isWin: false,
    redRankList: [],
    blueRankList: [],
    rank: 0,
  };
}

export const CrossGuildEndMsg: MessageFns<CrossGuildEndMsg> = {
  encode(message: CrossGuildEndMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.redScore !== 0) {
      writer.uint32(8).int32(message.redScore);
    }
    if (message.blueScore !== 0) {
      writer.uint32(16).int32(message.blueScore);
    }
    if (message.redName !== "") {
      writer.uint32(26).string(message.redName);
    }
    if (message.blueName !== "") {
      writer.uint32(34).string(message.blueName);
    }
    if (message.isWin !== false) {
      writer.uint32(40).bool(message.isWin);
    }
    writer.uint32(50).fork();
    for (const v of message.redRankList) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(58).fork();
    for (const v of message.blueRankList) {
      writer.int32(v);
    }
    writer.join();
    if (message.rank !== 0) {
      writer.uint32(64).int32(message.rank);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CrossGuildEndMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCrossGuildEndMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.redScore = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.blueScore = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.redName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.blueName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isWin = reader.bool();
          continue;
        }
        case 6: {
          if (tag === 48) {
            message.redRankList.push(reader.int32());

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.redRankList.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 7: {
          if (tag === 56) {
            message.blueRankList.push(reader.int32());

            continue;
          }

          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.blueRankList.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.rank = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CrossGuildEndMsg {
    return {
      redScore: isSet(object.redScore) ? globalThis.Number(object.redScore) : 0,
      blueScore: isSet(object.blueScore) ? globalThis.Number(object.blueScore) : 0,
      redName: isSet(object.redName) ? globalThis.String(object.redName) : "",
      blueName: isSet(object.blueName) ? globalThis.String(object.blueName) : "",
      isWin: isSet(object.isWin) ? globalThis.Boolean(object.isWin) : false,
      redRankList: globalThis.Array.isArray(object?.redRankList)
        ? object.redRankList.map((e: any) => globalThis.Number(e))
        : [],
      blueRankList: globalThis.Array.isArray(object?.blueRankList)
        ? object.blueRankList.map((e: any) => globalThis.Number(e))
        : [],
      rank: isSet(object.rank) ? globalThis.Number(object.rank) : 0,
    };
  },

  toJSON(message: CrossGuildEndMsg): unknown {
    const obj: any = {};
    if (message.redScore !== 0) {
      obj.redScore = Math.round(message.redScore);
    }
    if (message.blueScore !== 0) {
      obj.blueScore = Math.round(message.blueScore);
    }
    if (message.redName !== "") {
      obj.redName = message.redName;
    }
    if (message.blueName !== "") {
      obj.blueName = message.blueName;
    }
    if (message.isWin !== false) {
      obj.isWin = message.isWin;
    }
    if (message.redRankList?.length) {
      obj.redRankList = message.redRankList.map((e) => Math.round(e));
    }
    if (message.blueRankList?.length) {
      obj.blueRankList = message.blueRankList.map((e) => Math.round(e));
    }
    if (message.rank !== 0) {
      obj.rank = Math.round(message.rank);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CrossGuildEndMsg>, I>>(base?: I): CrossGuildEndMsg {
    return CrossGuildEndMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CrossGuildEndMsg>, I>>(object: I): CrossGuildEndMsg {
    const message = createBaseCrossGuildEndMsg();
    message.redScore = object.redScore ?? 0;
    message.blueScore = object.blueScore ?? 0;
    message.redName = object.redName ?? "";
    message.blueName = object.blueName ?? "";
    message.isWin = object.isWin ?? false;
    message.redRankList = object.redRankList?.map((e) => e) || [];
    message.blueRankList = object.blueRankList?.map((e) => e) || [];
    message.rank = object.rank ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
