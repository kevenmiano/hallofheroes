// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: simple/FriendRelationList.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.simple";

export interface FriendRelationList {
  relationList: number[];
  group: number[];
  maxPage: number;
  curPage: number;
}

function createBaseFriendRelationList(): FriendRelationList {
  return { relationList: [], group: [], maxPage: 0, curPage: 0 };
}

export const FriendRelationList: MessageFns<FriendRelationList> = {
  encode(message: FriendRelationList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.relationList) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.group) {
      writer.int32(v);
    }
    writer.join();
    if (message.maxPage !== 0) {
      writer.uint32(24).int32(message.maxPage);
    }
    if (message.curPage !== 0) {
      writer.uint32(32).int32(message.curPage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FriendRelationList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendRelationList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.relationList.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.relationList.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.group.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.group.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxPage = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.curPage = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendRelationList {
    return {
      relationList: globalThis.Array.isArray(object?.relationList)
        ? object.relationList.map((e: any) => globalThis.Number(e))
        : [],
      group: globalThis.Array.isArray(object?.group) ? object.group.map((e: any) => globalThis.Number(e)) : [],
      maxPage: isSet(object.maxPage) ? globalThis.Number(object.maxPage) : 0,
      curPage: isSet(object.curPage) ? globalThis.Number(object.curPage) : 0,
    };
  },

  toJSON(message: FriendRelationList): unknown {
    const obj: any = {};
    if (message.relationList?.length) {
      obj.relationList = message.relationList.map((e) => Math.round(e));
    }
    if (message.group?.length) {
      obj.group = message.group.map((e) => Math.round(e));
    }
    if (message.maxPage !== 0) {
      obj.maxPage = Math.round(message.maxPage);
    }
    if (message.curPage !== 0) {
      obj.curPage = Math.round(message.curPage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendRelationList>, I>>(base?: I): FriendRelationList {
    return FriendRelationList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendRelationList>, I>>(object: I): FriendRelationList {
    const message = createBaseFriendRelationList();
    message.relationList = object.relationList?.map((e) => e) || [];
    message.group = object.group?.map((e) => e) || [];
    message.maxPage = object.maxPage ?? 0;
    message.curPage = object.curPage ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
