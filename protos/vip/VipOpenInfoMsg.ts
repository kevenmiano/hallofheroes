// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: vip/VipOpenInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.vip";

export interface VipOpenInfoMsg {
  isFriend: boolean;
  friendId: number;
  friendNickName: string;
  days: number;
}

function createBaseVipOpenInfoMsg(): VipOpenInfoMsg {
  return { isFriend: false, friendId: 0, friendNickName: "", days: 0 };
}

export const VipOpenInfoMsg: MessageFns<VipOpenInfoMsg> = {
  encode(message: VipOpenInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isFriend !== false) {
      writer.uint32(8).bool(message.isFriend);
    }
    if (message.friendId !== 0) {
      writer.uint32(16).int32(message.friendId);
    }
    if (message.friendNickName !== "") {
      writer.uint32(26).string(message.friendNickName);
    }
    if (message.days !== 0) {
      writer.uint32(32).int32(message.days);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VipOpenInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVipOpenInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isFriend = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.friendId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.friendNickName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.days = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VipOpenInfoMsg {
    return {
      isFriend: isSet(object.isFriend) ? globalThis.Boolean(object.isFriend) : false,
      friendId: isSet(object.friendId) ? globalThis.Number(object.friendId) : 0,
      friendNickName: isSet(object.friendNickName) ? globalThis.String(object.friendNickName) : "",
      days: isSet(object.days) ? globalThis.Number(object.days) : 0,
    };
  },

  toJSON(message: VipOpenInfoMsg): unknown {
    const obj: any = {};
    if (message.isFriend !== false) {
      obj.isFriend = message.isFriend;
    }
    if (message.friendId !== 0) {
      obj.friendId = Math.round(message.friendId);
    }
    if (message.friendNickName !== "") {
      obj.friendNickName = message.friendNickName;
    }
    if (message.days !== 0) {
      obj.days = Math.round(message.days);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VipOpenInfoMsg>, I>>(base?: I): VipOpenInfoMsg {
    return VipOpenInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VipOpenInfoMsg>, I>>(object: I): VipOpenInfoMsg {
    const message = createBaseVipOpenInfoMsg();
    message.isFriend = object.isFriend ?? false;
    message.friendId = object.friendId ?? 0;
    message.friendNickName = object.friendNickName ?? "";
    message.days = object.days ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
