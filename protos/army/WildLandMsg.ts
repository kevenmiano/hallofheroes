// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: army/WildLandMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.army";

export interface WildLandMsg {
  id: number;
  mapId: number;
  posX: number;
  posY: number;
  templateId: number;
  type: number;
  occupyPlayerId: number;
  occupyPlayerName: string;
  occupyLeagueName: string;
  state: number;
  refreshTime: number;
  simpleHeroInfo: string;
  armyPawn: number[];
  curArmyId: number[];
  allNodeInfo: number[];
  level: number;
  occupyNodeInfo: number[];
  occupyGuildId: number;
  startFightTime: string;
}

function createBaseWildLandMsg(): WildLandMsg {
  return {
    id: 0,
    mapId: 0,
    posX: 0,
    posY: 0,
    templateId: 0,
    type: 0,
    occupyPlayerId: 0,
    occupyPlayerName: "",
    occupyLeagueName: "",
    state: 0,
    refreshTime: 0,
    simpleHeroInfo: "",
    armyPawn: [],
    curArmyId: [],
    allNodeInfo: [],
    level: 0,
    occupyNodeInfo: [],
    occupyGuildId: 0,
    startFightTime: "",
  };
}

export const WildLandMsg: MessageFns<WildLandMsg> = {
  encode(message: WildLandMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.mapId !== 0) {
      writer.uint32(16).int32(message.mapId);
    }
    if (message.posX !== 0) {
      writer.uint32(24).int32(message.posX);
    }
    if (message.posY !== 0) {
      writer.uint32(32).int32(message.posY);
    }
    if (message.templateId !== 0) {
      writer.uint32(40).int32(message.templateId);
    }
    if (message.type !== 0) {
      writer.uint32(48).int32(message.type);
    }
    if (message.occupyPlayerId !== 0) {
      writer.uint32(56).int32(message.occupyPlayerId);
    }
    if (message.occupyPlayerName !== "") {
      writer.uint32(66).string(message.occupyPlayerName);
    }
    if (message.occupyLeagueName !== "") {
      writer.uint32(74).string(message.occupyLeagueName);
    }
    if (message.state !== 0) {
      writer.uint32(80).int32(message.state);
    }
    if (message.refreshTime !== 0) {
      writer.uint32(88).int32(message.refreshTime);
    }
    if (message.simpleHeroInfo !== "") {
      writer.uint32(98).string(message.simpleHeroInfo);
    }
    writer.uint32(106).fork();
    for (const v of message.armyPawn) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(114).fork();
    for (const v of message.curArmyId) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(122).fork();
    for (const v of message.allNodeInfo) {
      writer.int32(v);
    }
    writer.join();
    if (message.level !== 0) {
      writer.uint32(128).int32(message.level);
    }
    writer.uint32(138).fork();
    for (const v of message.occupyNodeInfo) {
      writer.int32(v);
    }
    writer.join();
    if (message.occupyGuildId !== 0) {
      writer.uint32(144).int32(message.occupyGuildId);
    }
    if (message.startFightTime !== "") {
      writer.uint32(154).string(message.startFightTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WildLandMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWildLandMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.mapId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.posX = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.posY = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.templateId = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.type = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.occupyPlayerId = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.occupyPlayerName = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.occupyLeagueName = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.state = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.refreshTime = reader.int32();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.simpleHeroInfo = reader.string();
          continue;
        }
        case 13: {
          if (tag === 104) {
            message.armyPawn.push(reader.int32());

            continue;
          }

          if (tag === 106) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.armyPawn.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 14: {
          if (tag === 112) {
            message.curArmyId.push(reader.int32());

            continue;
          }

          if (tag === 114) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.curArmyId.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 15: {
          if (tag === 120) {
            message.allNodeInfo.push(reader.int32());

            continue;
          }

          if (tag === 122) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.allNodeInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.level = reader.int32();
          continue;
        }
        case 17: {
          if (tag === 136) {
            message.occupyNodeInfo.push(reader.int32());

            continue;
          }

          if (tag === 138) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.occupyNodeInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 18: {
          if (tag !== 144) {
            break;
          }

          message.occupyGuildId = reader.int32();
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.startFightTime = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WildLandMsg {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      mapId: isSet(object.mapId) ? globalThis.Number(object.mapId) : 0,
      posX: isSet(object.posX) ? globalThis.Number(object.posX) : 0,
      posY: isSet(object.posY) ? globalThis.Number(object.posY) : 0,
      templateId: isSet(object.templateId) ? globalThis.Number(object.templateId) : 0,
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
      occupyPlayerId: isSet(object.occupyPlayerId) ? globalThis.Number(object.occupyPlayerId) : 0,
      occupyPlayerName: isSet(object.occupyPlayerName) ? globalThis.String(object.occupyPlayerName) : "",
      occupyLeagueName: isSet(object.occupyLeagueName) ? globalThis.String(object.occupyLeagueName) : "",
      state: isSet(object.state) ? globalThis.Number(object.state) : 0,
      refreshTime: isSet(object.refreshTime) ? globalThis.Number(object.refreshTime) : 0,
      simpleHeroInfo: isSet(object.simpleHeroInfo) ? globalThis.String(object.simpleHeroInfo) : "",
      armyPawn: globalThis.Array.isArray(object?.armyPawn) ? object.armyPawn.map((e: any) => globalThis.Number(e)) : [],
      curArmyId: globalThis.Array.isArray(object?.curArmyId)
        ? object.curArmyId.map((e: any) => globalThis.Number(e))
        : [],
      allNodeInfo: globalThis.Array.isArray(object?.allNodeInfo)
        ? object.allNodeInfo.map((e: any) => globalThis.Number(e))
        : [],
      level: isSet(object.level) ? globalThis.Number(object.level) : 0,
      occupyNodeInfo: globalThis.Array.isArray(object?.occupyNodeInfo)
        ? object.occupyNodeInfo.map((e: any) => globalThis.Number(e))
        : [],
      occupyGuildId: isSet(object.occupyGuildId) ? globalThis.Number(object.occupyGuildId) : 0,
      startFightTime: isSet(object.startFightTime) ? globalThis.String(object.startFightTime) : "",
    };
  },

  toJSON(message: WildLandMsg): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.mapId !== 0) {
      obj.mapId = Math.round(message.mapId);
    }
    if (message.posX !== 0) {
      obj.posX = Math.round(message.posX);
    }
    if (message.posY !== 0) {
      obj.posY = Math.round(message.posY);
    }
    if (message.templateId !== 0) {
      obj.templateId = Math.round(message.templateId);
    }
    if (message.type !== 0) {
      obj.type = Math.round(message.type);
    }
    if (message.occupyPlayerId !== 0) {
      obj.occupyPlayerId = Math.round(message.occupyPlayerId);
    }
    if (message.occupyPlayerName !== "") {
      obj.occupyPlayerName = message.occupyPlayerName;
    }
    if (message.occupyLeagueName !== "") {
      obj.occupyLeagueName = message.occupyLeagueName;
    }
    if (message.state !== 0) {
      obj.state = Math.round(message.state);
    }
    if (message.refreshTime !== 0) {
      obj.refreshTime = Math.round(message.refreshTime);
    }
    if (message.simpleHeroInfo !== "") {
      obj.simpleHeroInfo = message.simpleHeroInfo;
    }
    if (message.armyPawn?.length) {
      obj.armyPawn = message.armyPawn.map((e) => Math.round(e));
    }
    if (message.curArmyId?.length) {
      obj.curArmyId = message.curArmyId.map((e) => Math.round(e));
    }
    if (message.allNodeInfo?.length) {
      obj.allNodeInfo = message.allNodeInfo.map((e) => Math.round(e));
    }
    if (message.level !== 0) {
      obj.level = Math.round(message.level);
    }
    if (message.occupyNodeInfo?.length) {
      obj.occupyNodeInfo = message.occupyNodeInfo.map((e) => Math.round(e));
    }
    if (message.occupyGuildId !== 0) {
      obj.occupyGuildId = Math.round(message.occupyGuildId);
    }
    if (message.startFightTime !== "") {
      obj.startFightTime = message.startFightTime;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WildLandMsg>, I>>(base?: I): WildLandMsg {
    return WildLandMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WildLandMsg>, I>>(object: I): WildLandMsg {
    const message = createBaseWildLandMsg();
    message.id = object.id ?? 0;
    message.mapId = object.mapId ?? 0;
    message.posX = object.posX ?? 0;
    message.posY = object.posY ?? 0;
    message.templateId = object.templateId ?? 0;
    message.type = object.type ?? 0;
    message.occupyPlayerId = object.occupyPlayerId ?? 0;
    message.occupyPlayerName = object.occupyPlayerName ?? "";
    message.occupyLeagueName = object.occupyLeagueName ?? "";
    message.state = object.state ?? 0;
    message.refreshTime = object.refreshTime ?? 0;
    message.simpleHeroInfo = object.simpleHeroInfo ?? "";
    message.armyPawn = object.armyPawn?.map((e) => e) || [];
    message.curArmyId = object.curArmyId?.map((e) => e) || [];
    message.allNodeInfo = object.allNodeInfo?.map((e) => e) || [];
    message.level = object.level ?? 0;
    message.occupyNodeInfo = object.occupyNodeInfo?.map((e) => e) || [];
    message.occupyGuildId = object.occupyGuildId ?? 0;
    message.startFightTime = object.startFightTime ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
