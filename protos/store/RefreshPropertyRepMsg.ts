// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: store/RefreshPropertyRepMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.store";

export interface RefreshPropertyRepMsg {
  randomSkill: number[];
  lockPos: number[];
  result: number;
}

function createBaseRefreshPropertyRepMsg(): RefreshPropertyRepMsg {
  return { randomSkill: [], lockPos: [], result: 0 };
}

export const RefreshPropertyRepMsg: MessageFns<RefreshPropertyRepMsg> = {
  encode(message: RefreshPropertyRepMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.randomSkill) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.lockPos) {
      writer.int32(v);
    }
    writer.join();
    if (message.result !== 0) {
      writer.uint32(24).int32(message.result);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshPropertyRepMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshPropertyRepMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.randomSkill.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.randomSkill.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.lockPos.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.lockPos.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.result = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshPropertyRepMsg {
    return {
      randomSkill: globalThis.Array.isArray(object?.randomSkill)
        ? object.randomSkill.map((e: any) => globalThis.Number(e))
        : [],
      lockPos: globalThis.Array.isArray(object?.lockPos) ? object.lockPos.map((e: any) => globalThis.Number(e)) : [],
      result: isSet(object.result) ? globalThis.Number(object.result) : 0,
    };
  },

  toJSON(message: RefreshPropertyRepMsg): unknown {
    const obj: any = {};
    if (message.randomSkill?.length) {
      obj.randomSkill = message.randomSkill.map((e) => Math.round(e));
    }
    if (message.lockPos?.length) {
      obj.lockPos = message.lockPos.map((e) => Math.round(e));
    }
    if (message.result !== 0) {
      obj.result = Math.round(message.result);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RefreshPropertyRepMsg>, I>>(base?: I): RefreshPropertyRepMsg {
    return RefreshPropertyRepMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RefreshPropertyRepMsg>, I>>(object: I): RefreshPropertyRepMsg {
    const message = createBaseRefreshPropertyRepMsg();
    message.randomSkill = object.randomSkill?.map((e) => e) || [];
    message.lockPos = object.lockPos?.map((e) => e) || [];
    message.result = object.result ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
