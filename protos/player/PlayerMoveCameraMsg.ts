// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: player/PlayerMoveCameraMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.player";

export interface PlayerMoveCameraMsg {
  scene: string;
  targetX: number;
  targetY: number;
  delay: number;
}

function createBasePlayerMoveCameraMsg(): PlayerMoveCameraMsg {
  return { scene: "", targetX: 0, targetY: 0, delay: 0 };
}

export const PlayerMoveCameraMsg: MessageFns<PlayerMoveCameraMsg> = {
  encode(message: PlayerMoveCameraMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scene !== "") {
      writer.uint32(10).string(message.scene);
    }
    if (message.targetX !== 0) {
      writer.uint32(16).int32(message.targetX);
    }
    if (message.targetY !== 0) {
      writer.uint32(24).int32(message.targetY);
    }
    if (message.delay !== 0) {
      writer.uint32(32).int32(message.delay);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerMoveCameraMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerMoveCameraMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.scene = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.targetX = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.targetY = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.delay = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerMoveCameraMsg {
    return {
      scene: isSet(object.scene) ? globalThis.String(object.scene) : "",
      targetX: isSet(object.targetX) ? globalThis.Number(object.targetX) : 0,
      targetY: isSet(object.targetY) ? globalThis.Number(object.targetY) : 0,
      delay: isSet(object.delay) ? globalThis.Number(object.delay) : 0,
    };
  },

  toJSON(message: PlayerMoveCameraMsg): unknown {
    const obj: any = {};
    if (message.scene !== "") {
      obj.scene = message.scene;
    }
    if (message.targetX !== 0) {
      obj.targetX = Math.round(message.targetX);
    }
    if (message.targetY !== 0) {
      obj.targetY = Math.round(message.targetY);
    }
    if (message.delay !== 0) {
      obj.delay = Math.round(message.delay);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerMoveCameraMsg>, I>>(base?: I): PlayerMoveCameraMsg {
    return PlayerMoveCameraMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerMoveCameraMsg>, I>>(object: I): PlayerMoveCameraMsg {
    const message = createBasePlayerMoveCameraMsg();
    message.scene = object.scene ?? "";
    message.targetX = object.targetX ?? 0;
    message.targetY = object.targetY ?? 0;
    message.delay = object.delay ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
