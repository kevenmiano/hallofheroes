// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/BattleDetailMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface BattleDetailMsg {
  battleId: number;
  bossId: number;
  bossHp: number;
  fightPlayer: number[];
}

function createBaseBattleDetailMsg(): BattleDetailMsg {
  return { battleId: 0, bossId: 0, bossHp: 0, fightPlayer: [] };
}

export const BattleDetailMsg: MessageFns<BattleDetailMsg> = {
  encode(message: BattleDetailMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.battleId !== 0) {
      writer.uint32(8).int32(message.battleId);
    }
    if (message.bossId !== 0) {
      writer.uint32(16).int32(message.bossId);
    }
    if (message.bossHp !== 0) {
      writer.uint32(24).int32(message.bossHp);
    }
    writer.uint32(34).fork();
    for (const v of message.fightPlayer) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BattleDetailMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBattleDetailMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.battleId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.bossId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.bossHp = reader.int32();
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.fightPlayer.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.fightPlayer.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BattleDetailMsg {
    return {
      battleId: isSet(object.battleId) ? globalThis.Number(object.battleId) : 0,
      bossId: isSet(object.bossId) ? globalThis.Number(object.bossId) : 0,
      bossHp: isSet(object.bossHp) ? globalThis.Number(object.bossHp) : 0,
      fightPlayer: globalThis.Array.isArray(object?.fightPlayer)
        ? object.fightPlayer.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: BattleDetailMsg): unknown {
    const obj: any = {};
    if (message.battleId !== 0) {
      obj.battleId = Math.round(message.battleId);
    }
    if (message.bossId !== 0) {
      obj.bossId = Math.round(message.bossId);
    }
    if (message.bossHp !== 0) {
      obj.bossHp = Math.round(message.bossHp);
    }
    if (message.fightPlayer?.length) {
      obj.fightPlayer = message.fightPlayer.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BattleDetailMsg>, I>>(base?: I): BattleDetailMsg {
    return BattleDetailMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BattleDetailMsg>, I>>(object: I): BattleDetailMsg {
    const message = createBaseBattleDetailMsg();
    message.battleId = object.battleId ?? 0;
    message.bossId = object.bossId ?? 0;
    message.bossHp = object.bossHp ?? 0;
    message.fightPlayer = object.fightPlayer?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
