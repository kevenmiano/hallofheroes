// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: friend/MoveFriendReqMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.friend";

export interface MoveFriendReqMsg {
  firendId: number;
  groupId: number;
  result: number;
}

function createBaseMoveFriendReqMsg(): MoveFriendReqMsg {
  return { firendId: 0, groupId: 0, result: 0 };
}

export const MoveFriendReqMsg: MessageFns<MoveFriendReqMsg> = {
  encode(message: MoveFriendReqMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.firendId !== 0) {
      writer.uint32(8).int32(message.firendId);
    }
    if (message.groupId !== 0) {
      writer.uint32(16).int32(message.groupId);
    }
    if (message.result !== 0) {
      writer.uint32(24).int32(message.result);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MoveFriendReqMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoveFriendReqMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.firendId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.groupId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.result = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MoveFriendReqMsg {
    return {
      firendId: isSet(object.firendId) ? globalThis.Number(object.firendId) : 0,
      groupId: isSet(object.groupId) ? globalThis.Number(object.groupId) : 0,
      result: isSet(object.result) ? globalThis.Number(object.result) : 0,
    };
  },

  toJSON(message: MoveFriendReqMsg): unknown {
    const obj: any = {};
    if (message.firendId !== 0) {
      obj.firendId = Math.round(message.firendId);
    }
    if (message.groupId !== 0) {
      obj.groupId = Math.round(message.groupId);
    }
    if (message.result !== 0) {
      obj.result = Math.round(message.result);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MoveFriendReqMsg>, I>>(base?: I): MoveFriendReqMsg {
    return MoveFriendReqMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MoveFriendReqMsg>, I>>(object: I): MoveFriendReqMsg {
    const message = createBaseMoveFriendReqMsg();
    message.firendId = object.firendId ?? 0;
    message.groupId = object.groupId ?? 0;
    message.result = object.result ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
