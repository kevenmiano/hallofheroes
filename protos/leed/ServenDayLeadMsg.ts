// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: leed/ServenDayLeadMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.leed";

export interface ServenDayLeadMsg {
  beginTime: string;
  statu: number;
  singleDay: number[];
}

function createBaseServenDayLeadMsg(): ServenDayLeadMsg {
  return { beginTime: "", statu: 0, singleDay: [] };
}

export const ServenDayLeadMsg: MessageFns<ServenDayLeadMsg> = {
  encode(message: ServenDayLeadMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.beginTime !== "") {
      writer.uint32(10).string(message.beginTime);
    }
    if (message.statu !== 0) {
      writer.uint32(16).int32(message.statu);
    }
    writer.uint32(26).fork();
    for (const v of message.singleDay) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServenDayLeadMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServenDayLeadMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.beginTime = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.statu = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.singleDay.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.singleDay.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServenDayLeadMsg {
    return {
      beginTime: isSet(object.beginTime) ? globalThis.String(object.beginTime) : "",
      statu: isSet(object.statu) ? globalThis.Number(object.statu) : 0,
      singleDay: globalThis.Array.isArray(object?.singleDay)
        ? object.singleDay.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: ServenDayLeadMsg): unknown {
    const obj: any = {};
    if (message.beginTime !== "") {
      obj.beginTime = message.beginTime;
    }
    if (message.statu !== 0) {
      obj.statu = Math.round(message.statu);
    }
    if (message.singleDay?.length) {
      obj.singleDay = message.singleDay.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServenDayLeadMsg>, I>>(base?: I): ServenDayLeadMsg {
    return ServenDayLeadMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServenDayLeadMsg>, I>>(object: I): ServenDayLeadMsg {
    const message = createBaseServenDayLeadMsg();
    message.beginTime = object.beginTime ?? "";
    message.statu = object.statu ?? 0;
    message.singleDay = object.singleDay?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
