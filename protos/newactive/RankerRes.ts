// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: newactive/RankerRes.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.newactive";

export interface RankerRes {
  rankers: number[];
  self: string;
  rankType: number;
}

function createBaseRankerRes(): RankerRes {
  return { rankers: [], self: "", rankType: 0 };
}

export const RankerRes: MessageFns<RankerRes> = {
  encode(message: RankerRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.rankers) {
      writer.int32(v);
    }
    writer.join();
    if (message.self !== "") {
      writer.uint32(18).string(message.self);
    }
    if (message.rankType !== 0) {
      writer.uint32(24).int32(message.rankType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RankerRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRankerRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.rankers.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.rankers.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.self = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.rankType = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RankerRes {
    return {
      rankers: globalThis.Array.isArray(object?.rankers) ? object.rankers.map((e: any) => globalThis.Number(e)) : [],
      self: isSet(object.self) ? globalThis.String(object.self) : "",
      rankType: isSet(object.rankType) ? globalThis.Number(object.rankType) : 0,
    };
  },

  toJSON(message: RankerRes): unknown {
    const obj: any = {};
    if (message.rankers?.length) {
      obj.rankers = message.rankers.map((e) => Math.round(e));
    }
    if (message.self !== "") {
      obj.self = message.self;
    }
    if (message.rankType !== 0) {
      obj.rankType = Math.round(message.rankType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RankerRes>, I>>(base?: I): RankerRes {
    return RankerRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RankerRes>, I>>(object: I): RankerRes {
    const message = createBaseRankerRes();
    message.rankers = object.rankers?.map((e) => e) || [];
    message.self = object.self ?? "";
    message.rankType = object.rankType ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
