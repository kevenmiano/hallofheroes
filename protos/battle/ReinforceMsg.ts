// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/ReinforceMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface ReinforceMsg {
  side: number;
  hero: number[];
  soldiers: number[];
  reinforceWave: number;
  pets: number[];
}

function createBaseReinforceMsg(): ReinforceMsg {
  return { side: 0, hero: [], soldiers: [], reinforceWave: 0, pets: [] };
}

export const ReinforceMsg: MessageFns<ReinforceMsg> = {
  encode(message: ReinforceMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.side !== 0) {
      writer.uint32(8).int32(message.side);
    }
    writer.uint32(18).fork();
    for (const v of message.hero) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.soldiers) {
      writer.int32(v);
    }
    writer.join();
    if (message.reinforceWave !== 0) {
      writer.uint32(32).int32(message.reinforceWave);
    }
    writer.uint32(42).fork();
    for (const v of message.pets) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReinforceMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReinforceMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.side = reader.int32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.hero.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.hero.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.soldiers.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.soldiers.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.reinforceWave = reader.int32();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.pets.push(reader.int32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.pets.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReinforceMsg {
    return {
      side: isSet(object.side) ? globalThis.Number(object.side) : 0,
      hero: globalThis.Array.isArray(object?.hero) ? object.hero.map((e: any) => globalThis.Number(e)) : [],
      soldiers: globalThis.Array.isArray(object?.soldiers) ? object.soldiers.map((e: any) => globalThis.Number(e)) : [],
      reinforceWave: isSet(object.reinforceWave) ? globalThis.Number(object.reinforceWave) : 0,
      pets: globalThis.Array.isArray(object?.pets) ? object.pets.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: ReinforceMsg): unknown {
    const obj: any = {};
    if (message.side !== 0) {
      obj.side = Math.round(message.side);
    }
    if (message.hero?.length) {
      obj.hero = message.hero.map((e) => Math.round(e));
    }
    if (message.soldiers?.length) {
      obj.soldiers = message.soldiers.map((e) => Math.round(e));
    }
    if (message.reinforceWave !== 0) {
      obj.reinforceWave = Math.round(message.reinforceWave);
    }
    if (message.pets?.length) {
      obj.pets = message.pets.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReinforceMsg>, I>>(base?: I): ReinforceMsg {
    return ReinforceMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReinforceMsg>, I>>(object: I): ReinforceMsg {
    const message = createBaseReinforceMsg();
    message.side = object.side ?? 0;
    message.hero = object.hero?.map((e) => e) || [];
    message.soldiers = object.soldiers?.map((e) => e) || [];
    message.reinforceWave = object.reinforceWave ?? 0;
    message.pets = object.pets?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
