// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: active/PlanInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.active";

export interface PlanInfoMsg {
  plan: number;
  chargeMoney: number;
  chargeTypeInfo: number[];
}

function createBasePlanInfoMsg(): PlanInfoMsg {
  return { plan: 0, chargeMoney: 0, chargeTypeInfo: [] };
}

export const PlanInfoMsg: MessageFns<PlanInfoMsg> = {
  encode(message: PlanInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.plan !== 0) {
      writer.uint32(8).int32(message.plan);
    }
    if (message.chargeMoney !== 0) {
      writer.uint32(16).int32(message.chargeMoney);
    }
    writer.uint32(26).fork();
    for (const v of message.chargeTypeInfo) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlanInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlanInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.plan = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.chargeMoney = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.chargeTypeInfo.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.chargeTypeInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlanInfoMsg {
    return {
      plan: isSet(object.plan) ? globalThis.Number(object.plan) : 0,
      chargeMoney: isSet(object.chargeMoney) ? globalThis.Number(object.chargeMoney) : 0,
      chargeTypeInfo: globalThis.Array.isArray(object?.chargeTypeInfo)
        ? object.chargeTypeInfo.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: PlanInfoMsg): unknown {
    const obj: any = {};
    if (message.plan !== 0) {
      obj.plan = Math.round(message.plan);
    }
    if (message.chargeMoney !== 0) {
      obj.chargeMoney = Math.round(message.chargeMoney);
    }
    if (message.chargeTypeInfo?.length) {
      obj.chargeTypeInfo = message.chargeTypeInfo.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlanInfoMsg>, I>>(base?: I): PlanInfoMsg {
    return PlanInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlanInfoMsg>, I>>(object: I): PlanInfoMsg {
    const message = createBasePlanInfoMsg();
    message.plan = object.plan ?? 0;
    message.chargeMoney = object.chargeMoney ?? 0;
    message.chargeTypeInfo = object.chargeTypeInfo?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
