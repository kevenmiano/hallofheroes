// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/PlayerMoveMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface PlayerMoveMsg {
  userId: number;
  mapId: number;
  tarX: number;
  tarY: number;
  ismoveCamera: boolean;
}

function createBasePlayerMoveMsg(): PlayerMoveMsg {
  return { userId: 0, mapId: 0, tarX: 0, tarY: 0, ismoveCamera: false };
}

export const PlayerMoveMsg: MessageFns<PlayerMoveMsg> = {
  encode(message: PlayerMoveMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).int32(message.userId);
    }
    if (message.mapId !== 0) {
      writer.uint32(16).int32(message.mapId);
    }
    if (message.tarX !== 0) {
      writer.uint32(24).int32(message.tarX);
    }
    if (message.tarY !== 0) {
      writer.uint32(32).int32(message.tarY);
    }
    if (message.ismoveCamera !== false) {
      writer.uint32(40).bool(message.ismoveCamera);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerMoveMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerMoveMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.mapId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tarX = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.tarY = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.ismoveCamera = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerMoveMsg {
    return {
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      mapId: isSet(object.mapId) ? globalThis.Number(object.mapId) : 0,
      tarX: isSet(object.tarX) ? globalThis.Number(object.tarX) : 0,
      tarY: isSet(object.tarY) ? globalThis.Number(object.tarY) : 0,
      ismoveCamera: isSet(object.ismoveCamera) ? globalThis.Boolean(object.ismoveCamera) : false,
    };
  },

  toJSON(message: PlayerMoveMsg): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.mapId !== 0) {
      obj.mapId = Math.round(message.mapId);
    }
    if (message.tarX !== 0) {
      obj.tarX = Math.round(message.tarX);
    }
    if (message.tarY !== 0) {
      obj.tarY = Math.round(message.tarY);
    }
    if (message.ismoveCamera !== false) {
      obj.ismoveCamera = message.ismoveCamera;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerMoveMsg>, I>>(base?: I): PlayerMoveMsg {
    return PlayerMoveMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerMoveMsg>, I>>(object: I): PlayerMoveMsg {
    const message = createBasePlayerMoveMsg();
    message.userId = object.userId ?? 0;
    message.mapId = object.mapId ?? 0;
    message.tarX = object.tarX ?? 0;
    message.tarY = object.tarY ?? 0;
    message.ismoveCamera = object.ismoveCamera ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
