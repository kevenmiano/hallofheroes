// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: quest/QuestFinishedReqMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.quest";

export interface QuestFinishedReqMsg {
  templateId: number;
  rewardItemId: number;
  isPass: boolean;
}

function createBaseQuestFinishedReqMsg(): QuestFinishedReqMsg {
  return { templateId: 0, rewardItemId: 0, isPass: false };
}

export const QuestFinishedReqMsg: MessageFns<QuestFinishedReqMsg> = {
  encode(message: QuestFinishedReqMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.templateId !== 0) {
      writer.uint32(8).int32(message.templateId);
    }
    if (message.rewardItemId !== 0) {
      writer.uint32(16).int32(message.rewardItemId);
    }
    if (message.isPass !== false) {
      writer.uint32(24).bool(message.isPass);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuestFinishedReqMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuestFinishedReqMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.templateId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rewardItemId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isPass = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuestFinishedReqMsg {
    return {
      templateId: isSet(object.templateId) ? globalThis.Number(object.templateId) : 0,
      rewardItemId: isSet(object.rewardItemId) ? globalThis.Number(object.rewardItemId) : 0,
      isPass: isSet(object.isPass) ? globalThis.Boolean(object.isPass) : false,
    };
  },

  toJSON(message: QuestFinishedReqMsg): unknown {
    const obj: any = {};
    if (message.templateId !== 0) {
      obj.templateId = Math.round(message.templateId);
    }
    if (message.rewardItemId !== 0) {
      obj.rewardItemId = Math.round(message.rewardItemId);
    }
    if (message.isPass !== false) {
      obj.isPass = message.isPass;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuestFinishedReqMsg>, I>>(base?: I): QuestFinishedReqMsg {
    return QuestFinishedReqMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuestFinishedReqMsg>, I>>(object: I): QuestFinishedReqMsg {
    const message = createBaseQuestFinishedReqMsg();
    message.templateId = object.templateId ?? 0;
    message.rewardItemId = object.rewardItemId ?? 0;
    message.isPass = object.isPass ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
