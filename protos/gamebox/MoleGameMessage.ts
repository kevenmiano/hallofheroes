// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: gamebox/MoleGameMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.gamebox";

export interface MoleGameMessage {
  total: number;
  gridInfo: string;
}

function createBaseMoleGameMessage(): MoleGameMessage {
  return { total: 0, gridInfo: "" };
}

export const MoleGameMessage: MessageFns<MoleGameMessage> = {
  encode(message: MoleGameMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.total !== 0) {
      writer.uint32(8).int32(message.total);
    }
    if (message.gridInfo !== "") {
      writer.uint32(18).string(message.gridInfo);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MoleGameMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoleGameMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.gridInfo = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MoleGameMessage {
    return {
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
      gridInfo: isSet(object.gridInfo) ? globalThis.String(object.gridInfo) : "",
    };
  },

  toJSON(message: MoleGameMessage): unknown {
    const obj: any = {};
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    if (message.gridInfo !== "") {
      obj.gridInfo = message.gridInfo;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MoleGameMessage>, I>>(base?: I): MoleGameMessage {
    return MoleGameMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MoleGameMessage>, I>>(object: I): MoleGameMessage {
    const message = createBaseMoleGameMessage();
    message.total = object.total ?? 0;
    message.gridInfo = object.gridInfo ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
