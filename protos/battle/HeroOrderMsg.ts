// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/HeroOrderMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface HeroOrderMsg {
  battleId: string;
  heroIndexId: number;
  skillId: number;
  result: number;
  castTime: number;
  qteTime: number;
  targetIndexs: number[];
}

function createBaseHeroOrderMsg(): HeroOrderMsg {
  return { battleId: "", heroIndexId: 0, skillId: 0, result: 0, castTime: 0, qteTime: 0, targetIndexs: [] };
}

export const HeroOrderMsg: MessageFns<HeroOrderMsg> = {
  encode(message: HeroOrderMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.battleId !== "") {
      writer.uint32(10).string(message.battleId);
    }
    if (message.heroIndexId !== 0) {
      writer.uint32(16).int32(message.heroIndexId);
    }
    if (message.skillId !== 0) {
      writer.uint32(24).int32(message.skillId);
    }
    if (message.result !== 0) {
      writer.uint32(32).int32(message.result);
    }
    if (message.castTime !== 0) {
      writer.uint32(40).int32(message.castTime);
    }
    if (message.qteTime !== 0) {
      writer.uint32(48).int32(message.qteTime);
    }
    writer.uint32(58).fork();
    for (const v of message.targetIndexs) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeroOrderMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeroOrderMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.battleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.heroIndexId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.skillId = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.result = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.castTime = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.qteTime = reader.int32();
          continue;
        }
        case 7: {
          if (tag === 56) {
            message.targetIndexs.push(reader.int32());

            continue;
          }

          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.targetIndexs.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HeroOrderMsg {
    return {
      battleId: isSet(object.battleId) ? globalThis.String(object.battleId) : "",
      heroIndexId: isSet(object.heroIndexId) ? globalThis.Number(object.heroIndexId) : 0,
      skillId: isSet(object.skillId) ? globalThis.Number(object.skillId) : 0,
      result: isSet(object.result) ? globalThis.Number(object.result) : 0,
      castTime: isSet(object.castTime) ? globalThis.Number(object.castTime) : 0,
      qteTime: isSet(object.qteTime) ? globalThis.Number(object.qteTime) : 0,
      targetIndexs: globalThis.Array.isArray(object?.targetIndexs)
        ? object.targetIndexs.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: HeroOrderMsg): unknown {
    const obj: any = {};
    if (message.battleId !== "") {
      obj.battleId = message.battleId;
    }
    if (message.heroIndexId !== 0) {
      obj.heroIndexId = Math.round(message.heroIndexId);
    }
    if (message.skillId !== 0) {
      obj.skillId = Math.round(message.skillId);
    }
    if (message.result !== 0) {
      obj.result = Math.round(message.result);
    }
    if (message.castTime !== 0) {
      obj.castTime = Math.round(message.castTime);
    }
    if (message.qteTime !== 0) {
      obj.qteTime = Math.round(message.qteTime);
    }
    if (message.targetIndexs?.length) {
      obj.targetIndexs = message.targetIndexs.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HeroOrderMsg>, I>>(base?: I): HeroOrderMsg {
    return HeroOrderMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HeroOrderMsg>, I>>(object: I): HeroOrderMsg {
    const message = createBaseHeroOrderMsg();
    message.battleId = object.battleId ?? "";
    message.heroIndexId = object.heroIndexId ?? 0;
    message.skillId = object.skillId ?? 0;
    message.result = object.result ?? 0;
    message.castTime = object.castTime ?? 0;
    message.qteTime = object.qteTime ?? 0;
    message.targetIndexs = object.targetIndexs?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
