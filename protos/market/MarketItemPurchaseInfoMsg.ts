// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: market/MarketItemPurchaseInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.market";

export interface MarketItemPurchaseInfoMsg {
  templateId: number;
  sellTotal: string;
  purchaseTotal: string;
  sellPriceList: number[];
  purchasePriceList: number[];
}

function createBaseMarketItemPurchaseInfoMsg(): MarketItemPurchaseInfoMsg {
  return { templateId: 0, sellTotal: "", purchaseTotal: "", sellPriceList: [], purchasePriceList: [] };
}

export const MarketItemPurchaseInfoMsg: MessageFns<MarketItemPurchaseInfoMsg> = {
  encode(message: MarketItemPurchaseInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.templateId !== 0) {
      writer.uint32(8).int32(message.templateId);
    }
    if (message.sellTotal !== "") {
      writer.uint32(18).string(message.sellTotal);
    }
    if (message.purchaseTotal !== "") {
      writer.uint32(26).string(message.purchaseTotal);
    }
    writer.uint32(34).fork();
    for (const v of message.sellPriceList) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(42).fork();
    for (const v of message.purchasePriceList) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketItemPurchaseInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketItemPurchaseInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.templateId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sellTotal = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.purchaseTotal = reader.string();
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.sellPriceList.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.sellPriceList.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag === 40) {
            message.purchasePriceList.push(reader.int32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.purchasePriceList.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketItemPurchaseInfoMsg {
    return {
      templateId: isSet(object.templateId) ? globalThis.Number(object.templateId) : 0,
      sellTotal: isSet(object.sellTotal) ? globalThis.String(object.sellTotal) : "",
      purchaseTotal: isSet(object.purchaseTotal) ? globalThis.String(object.purchaseTotal) : "",
      sellPriceList: globalThis.Array.isArray(object?.sellPriceList)
        ? object.sellPriceList.map((e: any) => globalThis.Number(e))
        : [],
      purchasePriceList: globalThis.Array.isArray(object?.purchasePriceList)
        ? object.purchasePriceList.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: MarketItemPurchaseInfoMsg): unknown {
    const obj: any = {};
    if (message.templateId !== 0) {
      obj.templateId = Math.round(message.templateId);
    }
    if (message.sellTotal !== "") {
      obj.sellTotal = message.sellTotal;
    }
    if (message.purchaseTotal !== "") {
      obj.purchaseTotal = message.purchaseTotal;
    }
    if (message.sellPriceList?.length) {
      obj.sellPriceList = message.sellPriceList.map((e) => Math.round(e));
    }
    if (message.purchasePriceList?.length) {
      obj.purchasePriceList = message.purchasePriceList.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarketItemPurchaseInfoMsg>, I>>(base?: I): MarketItemPurchaseInfoMsg {
    return MarketItemPurchaseInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarketItemPurchaseInfoMsg>, I>>(object: I): MarketItemPurchaseInfoMsg {
    const message = createBaseMarketItemPurchaseInfoMsg();
    message.templateId = object.templateId ?? 0;
    message.sellTotal = object.sellTotal ?? "";
    message.purchaseTotal = object.purchaseTotal ?? "";
    message.sellPriceList = object.sellPriceList?.map((e) => e) || [];
    message.purchasePriceList = object.purchasePriceList?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
