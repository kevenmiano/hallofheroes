// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: shop/ShopMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.shop";

export interface ShopMsg {
  buyInfo: string;
  hasDataInfo: number[];
  refreshDate: string;
  payType: number;
  weeklyDataInfo: number[];
  discountTempId: number;
}

function createBaseShopMsg(): ShopMsg {
  return { buyInfo: "", hasDataInfo: [], refreshDate: "", payType: 0, weeklyDataInfo: [], discountTempId: 0 };
}

export const ShopMsg: MessageFns<ShopMsg> = {
  encode(message: ShopMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.buyInfo !== "") {
      writer.uint32(10).string(message.buyInfo);
    }
    writer.uint32(18).fork();
    for (const v of message.hasDataInfo) {
      writer.int32(v);
    }
    writer.join();
    if (message.refreshDate !== "") {
      writer.uint32(26).string(message.refreshDate);
    }
    if (message.payType !== 0) {
      writer.uint32(32).int32(message.payType);
    }
    writer.uint32(42).fork();
    for (const v of message.weeklyDataInfo) {
      writer.int32(v);
    }
    writer.join();
    if (message.discountTempId !== 0) {
      writer.uint32(48).int32(message.discountTempId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShopMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShopMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.buyInfo = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.hasDataInfo.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.hasDataInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.refreshDate = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.payType = reader.int32();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.weeklyDataInfo.push(reader.int32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.weeklyDataInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.discountTempId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShopMsg {
    return {
      buyInfo: isSet(object.buyInfo) ? globalThis.String(object.buyInfo) : "",
      hasDataInfo: globalThis.Array.isArray(object?.hasDataInfo)
        ? object.hasDataInfo.map((e: any) => globalThis.Number(e))
        : [],
      refreshDate: isSet(object.refreshDate) ? globalThis.String(object.refreshDate) : "",
      payType: isSet(object.payType) ? globalThis.Number(object.payType) : 0,
      weeklyDataInfo: globalThis.Array.isArray(object?.weeklyDataInfo)
        ? object.weeklyDataInfo.map((e: any) => globalThis.Number(e))
        : [],
      discountTempId: isSet(object.discountTempId) ? globalThis.Number(object.discountTempId) : 0,
    };
  },

  toJSON(message: ShopMsg): unknown {
    const obj: any = {};
    if (message.buyInfo !== "") {
      obj.buyInfo = message.buyInfo;
    }
    if (message.hasDataInfo?.length) {
      obj.hasDataInfo = message.hasDataInfo.map((e) => Math.round(e));
    }
    if (message.refreshDate !== "") {
      obj.refreshDate = message.refreshDate;
    }
    if (message.payType !== 0) {
      obj.payType = Math.round(message.payType);
    }
    if (message.weeklyDataInfo?.length) {
      obj.weeklyDataInfo = message.weeklyDataInfo.map((e) => Math.round(e));
    }
    if (message.discountTempId !== 0) {
      obj.discountTempId = Math.round(message.discountTempId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShopMsg>, I>>(base?: I): ShopMsg {
    return ShopMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShopMsg>, I>>(object: I): ShopMsg {
    const message = createBaseShopMsg();
    message.buyInfo = object.buyInfo ?? "";
    message.hasDataInfo = object.hasDataInfo?.map((e) => e) || [];
    message.refreshDate = object.refreshDate ?? "";
    message.payType = object.payType ?? 0;
    message.weeklyDataInfo = object.weeklyDataInfo?.map((e) => e) || [];
    message.discountTempId = object.discountTempId ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
