// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: rebate/UserRebateMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.rebate";

export interface UserRebateMsg {
  id: string;
  userid: number;
  point: number;
  type: number;
  packagestates: number[];
}

function createBaseUserRebateMsg(): UserRebateMsg {
  return { id: "", userid: 0, point: 0, type: 0, packagestates: [] };
}

export const UserRebateMsg: MessageFns<UserRebateMsg> = {
  encode(message: UserRebateMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.userid !== 0) {
      writer.uint32(16).int32(message.userid);
    }
    if (message.point !== 0) {
      writer.uint32(24).int32(message.point);
    }
    if (message.type !== 0) {
      writer.uint32(32).int32(message.type);
    }
    writer.uint32(42).fork();
    for (const v of message.packagestates) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserRebateMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserRebateMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.userid = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.point = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.type = reader.int32();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.packagestates.push(reader.int32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.packagestates.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserRebateMsg {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      userid: isSet(object.userid) ? globalThis.Number(object.userid) : 0,
      point: isSet(object.point) ? globalThis.Number(object.point) : 0,
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
      packagestates: globalThis.Array.isArray(object?.packagestates)
        ? object.packagestates.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: UserRebateMsg): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.userid !== 0) {
      obj.userid = Math.round(message.userid);
    }
    if (message.point !== 0) {
      obj.point = Math.round(message.point);
    }
    if (message.type !== 0) {
      obj.type = Math.round(message.type);
    }
    if (message.packagestates?.length) {
      obj.packagestates = message.packagestates.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserRebateMsg>, I>>(base?: I): UserRebateMsg {
    return UserRebateMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserRebateMsg>, I>>(object: I): UserRebateMsg {
    const message = createBaseUserRebateMsg();
    message.id = object.id ?? "";
    message.userid = object.userid ?? 0;
    message.point = object.point ?? 0;
    message.type = object.type ?? 0;
    message.packagestates = object.packagestates?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
