// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/WithdrawRespMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface WithdrawRespMsg {
  info: number[];
  result: number;
  countdown: number;
}

function createBaseWithdrawRespMsg(): WithdrawRespMsg {
  return { info: [], result: 0, countdown: 0 };
}

export const WithdrawRespMsg: MessageFns<WithdrawRespMsg> = {
  encode(message: WithdrawRespMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.info) {
      writer.int32(v);
    }
    writer.join();
    if (message.result !== 0) {
      writer.uint32(16).int32(message.result);
    }
    if (message.countdown !== 0) {
      writer.uint32(24).int32(message.countdown);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WithdrawRespMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWithdrawRespMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.info.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.info.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.result = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.countdown = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WithdrawRespMsg {
    return {
      info: globalThis.Array.isArray(object?.info) ? object.info.map((e: any) => globalThis.Number(e)) : [],
      result: isSet(object.result) ? globalThis.Number(object.result) : 0,
      countdown: isSet(object.countdown) ? globalThis.Number(object.countdown) : 0,
    };
  },

  toJSON(message: WithdrawRespMsg): unknown {
    const obj: any = {};
    if (message.info?.length) {
      obj.info = message.info.map((e) => Math.round(e));
    }
    if (message.result !== 0) {
      obj.result = Math.round(message.result);
    }
    if (message.countdown !== 0) {
      obj.countdown = Math.round(message.countdown);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WithdrawRespMsg>, I>>(base?: I): WithdrawRespMsg {
    return WithdrawRespMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WithdrawRespMsg>, I>>(object: I): WithdrawRespMsg {
    const message = createBaseWithdrawRespMsg();
    message.info = object.info?.map((e) => e) || [];
    message.result = object.result ?? 0;
    message.countdown = object.countdown ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
