// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: fish/FishPlayer.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.fish";

export interface FishPlayer {
  helpId: number;
  nickName: string;
}

function createBaseFishPlayer(): FishPlayer {
  return { helpId: 0, nickName: "" };
}

export const FishPlayer: MessageFns<FishPlayer> = {
  encode(message: FishPlayer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.helpId !== 0) {
      writer.uint32(8).int32(message.helpId);
    }
    if (message.nickName !== "") {
      writer.uint32(18).string(message.nickName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FishPlayer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFishPlayer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.helpId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nickName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FishPlayer {
    return {
      helpId: isSet(object.helpId) ? globalThis.Number(object.helpId) : 0,
      nickName: isSet(object.nickName) ? globalThis.String(object.nickName) : "",
    };
  },

  toJSON(message: FishPlayer): unknown {
    const obj: any = {};
    if (message.helpId !== 0) {
      obj.helpId = Math.round(message.helpId);
    }
    if (message.nickName !== "") {
      obj.nickName = message.nickName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FishPlayer>, I>>(base?: I): FishPlayer {
    return FishPlayer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FishPlayer>, I>>(object: I): FishPlayer {
    const message = createBaseFishPlayer();
    message.helpId = object.helpId ?? 0;
    message.nickName = object.nickName ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
