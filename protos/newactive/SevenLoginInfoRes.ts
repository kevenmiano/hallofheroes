// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: newactive/SevenLoginInfoRes.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.newactive";

export interface SevenLoginInfoRes {
  loginDays: number;
  awardRecord: number[];
  stage: number;
}

function createBaseSevenLoginInfoRes(): SevenLoginInfoRes {
  return { loginDays: 0, awardRecord: [], stage: 0 };
}

export const SevenLoginInfoRes: MessageFns<SevenLoginInfoRes> = {
  encode(message: SevenLoginInfoRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.loginDays !== 0) {
      writer.uint32(8).int32(message.loginDays);
    }
    writer.uint32(18).fork();
    for (const v of message.awardRecord) {
      writer.int32(v);
    }
    writer.join();
    if (message.stage !== 0) {
      writer.uint32(24).int32(message.stage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SevenLoginInfoRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSevenLoginInfoRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.loginDays = reader.int32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.awardRecord.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.awardRecord.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.stage = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SevenLoginInfoRes {
    return {
      loginDays: isSet(object.loginDays) ? globalThis.Number(object.loginDays) : 0,
      awardRecord: globalThis.Array.isArray(object?.awardRecord)
        ? object.awardRecord.map((e: any) => globalThis.Number(e))
        : [],
      stage: isSet(object.stage) ? globalThis.Number(object.stage) : 0,
    };
  },

  toJSON(message: SevenLoginInfoRes): unknown {
    const obj: any = {};
    if (message.loginDays !== 0) {
      obj.loginDays = Math.round(message.loginDays);
    }
    if (message.awardRecord?.length) {
      obj.awardRecord = message.awardRecord.map((e) => Math.round(e));
    }
    if (message.stage !== 0) {
      obj.stage = Math.round(message.stage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SevenLoginInfoRes>, I>>(base?: I): SevenLoginInfoRes {
    return SevenLoginInfoRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SevenLoginInfoRes>, I>>(object: I): SevenLoginInfoRes {
    const message = createBaseSevenLoginInfoRes();
    message.loginDays = object.loginDays ?? 0;
    message.awardRecord = object.awardRecord?.map((e) => e) || [];
    message.stage = object.stage ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
