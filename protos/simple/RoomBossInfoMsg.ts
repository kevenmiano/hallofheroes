// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: simple/RoomBossInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.simple";

export interface RoomBossInfoMsg {
  bossId: number;
  bossType: number;
  curHp: string;
  totalHp: string;
  currentMillis: string;
  woundInfo: number[];
}

function createBaseRoomBossInfoMsg(): RoomBossInfoMsg {
  return { bossId: 0, bossType: 0, curHp: "", totalHp: "", currentMillis: "", woundInfo: [] };
}

export const RoomBossInfoMsg: MessageFns<RoomBossInfoMsg> = {
  encode(message: RoomBossInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bossId !== 0) {
      writer.uint32(8).int32(message.bossId);
    }
    if (message.bossType !== 0) {
      writer.uint32(16).int32(message.bossType);
    }
    if (message.curHp !== "") {
      writer.uint32(26).string(message.curHp);
    }
    if (message.totalHp !== "") {
      writer.uint32(34).string(message.totalHp);
    }
    if (message.currentMillis !== "") {
      writer.uint32(42).string(message.currentMillis);
    }
    writer.uint32(50).fork();
    for (const v of message.woundInfo) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomBossInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomBossInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.bossId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.bossType = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.curHp = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.totalHp = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.currentMillis = reader.string();
          continue;
        }
        case 6: {
          if (tag === 48) {
            message.woundInfo.push(reader.int32());

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.woundInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomBossInfoMsg {
    return {
      bossId: isSet(object.bossId) ? globalThis.Number(object.bossId) : 0,
      bossType: isSet(object.bossType) ? globalThis.Number(object.bossType) : 0,
      curHp: isSet(object.curHp) ? globalThis.String(object.curHp) : "",
      totalHp: isSet(object.totalHp) ? globalThis.String(object.totalHp) : "",
      currentMillis: isSet(object.currentMillis) ? globalThis.String(object.currentMillis) : "",
      woundInfo: globalThis.Array.isArray(object?.woundInfo)
        ? object.woundInfo.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: RoomBossInfoMsg): unknown {
    const obj: any = {};
    if (message.bossId !== 0) {
      obj.bossId = Math.round(message.bossId);
    }
    if (message.bossType !== 0) {
      obj.bossType = Math.round(message.bossType);
    }
    if (message.curHp !== "") {
      obj.curHp = message.curHp;
    }
    if (message.totalHp !== "") {
      obj.totalHp = message.totalHp;
    }
    if (message.currentMillis !== "") {
      obj.currentMillis = message.currentMillis;
    }
    if (message.woundInfo?.length) {
      obj.woundInfo = message.woundInfo.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoomBossInfoMsg>, I>>(base?: I): RoomBossInfoMsg {
    return RoomBossInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoomBossInfoMsg>, I>>(object: I): RoomBossInfoMsg {
    const message = createBaseRoomBossInfoMsg();
    message.bossId = object.bossId ?? 0;
    message.bossType = object.bossType ?? 0;
    message.curHp = object.curHp ?? "";
    message.totalHp = object.totalHp ?? "";
    message.currentMillis = object.currentMillis ?? "";
    message.woundInfo = object.woundInfo?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
