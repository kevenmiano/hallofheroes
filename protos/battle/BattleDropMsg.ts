// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/BattleDropMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface BattleDropMsg {
  failId: number;
  templateId: number;
  count: number;
  winIds: number[];
}

function createBaseBattleDropMsg(): BattleDropMsg {
  return { failId: 0, templateId: 0, count: 0, winIds: [] };
}

export const BattleDropMsg: MessageFns<BattleDropMsg> = {
  encode(message: BattleDropMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.failId !== 0) {
      writer.uint32(8).int32(message.failId);
    }
    if (message.templateId !== 0) {
      writer.uint32(16).int32(message.templateId);
    }
    if (message.count !== 0) {
      writer.uint32(24).int32(message.count);
    }
    writer.uint32(34).fork();
    for (const v of message.winIds) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BattleDropMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBattleDropMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.failId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.templateId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.winIds.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.winIds.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BattleDropMsg {
    return {
      failId: isSet(object.failId) ? globalThis.Number(object.failId) : 0,
      templateId: isSet(object.templateId) ? globalThis.Number(object.templateId) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      winIds: globalThis.Array.isArray(object?.winIds) ? object.winIds.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: BattleDropMsg): unknown {
    const obj: any = {};
    if (message.failId !== 0) {
      obj.failId = Math.round(message.failId);
    }
    if (message.templateId !== 0) {
      obj.templateId = Math.round(message.templateId);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.winIds?.length) {
      obj.winIds = message.winIds.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BattleDropMsg>, I>>(base?: I): BattleDropMsg {
    return BattleDropMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BattleDropMsg>, I>>(object: I): BattleDropMsg {
    const message = createBaseBattleDropMsg();
    message.failId = object.failId ?? 0;
    message.templateId = object.templateId ?? 0;
    message.count = object.count ?? 0;
    message.winIds = object.winIds?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
