// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/CampaignCardsMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface CampaignCardsMsg {
  sceneType: string;
  cardCount: number;
  payCardCount: number;
  price: number;
  dropItems: number[];
}

function createBaseCampaignCardsMsg(): CampaignCardsMsg {
  return { sceneType: "", cardCount: 0, payCardCount: 0, price: 0, dropItems: [] };
}

export const CampaignCardsMsg: MessageFns<CampaignCardsMsg> = {
  encode(message: CampaignCardsMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sceneType !== "") {
      writer.uint32(10).string(message.sceneType);
    }
    if (message.cardCount !== 0) {
      writer.uint32(16).int32(message.cardCount);
    }
    if (message.payCardCount !== 0) {
      writer.uint32(24).int32(message.payCardCount);
    }
    if (message.price !== 0) {
      writer.uint32(32).int32(message.price);
    }
    writer.uint32(42).fork();
    for (const v of message.dropItems) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CampaignCardsMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCampaignCardsMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sceneType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.cardCount = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.payCardCount = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.price = reader.int32();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.dropItems.push(reader.int32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.dropItems.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CampaignCardsMsg {
    return {
      sceneType: isSet(object.sceneType) ? globalThis.String(object.sceneType) : "",
      cardCount: isSet(object.cardCount) ? globalThis.Number(object.cardCount) : 0,
      payCardCount: isSet(object.payCardCount) ? globalThis.Number(object.payCardCount) : 0,
      price: isSet(object.price) ? globalThis.Number(object.price) : 0,
      dropItems: globalThis.Array.isArray(object?.dropItems)
        ? object.dropItems.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: CampaignCardsMsg): unknown {
    const obj: any = {};
    if (message.sceneType !== "") {
      obj.sceneType = message.sceneType;
    }
    if (message.cardCount !== 0) {
      obj.cardCount = Math.round(message.cardCount);
    }
    if (message.payCardCount !== 0) {
      obj.payCardCount = Math.round(message.payCardCount);
    }
    if (message.price !== 0) {
      obj.price = Math.round(message.price);
    }
    if (message.dropItems?.length) {
      obj.dropItems = message.dropItems.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CampaignCardsMsg>, I>>(base?: I): CampaignCardsMsg {
    return CampaignCardsMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CampaignCardsMsg>, I>>(object: I): CampaignCardsMsg {
    const message = createBaseCampaignCardsMsg();
    message.sceneType = object.sceneType ?? "";
    message.cardCount = object.cardCount ?? 0;
    message.payCardCount = object.payCardCount ?? 0;
    message.price = object.price ?? 0;
    message.dropItems = object.dropItems?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
