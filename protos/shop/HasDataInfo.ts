// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: shop/HasDataInfo.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.shop";

export interface HasDataInfo {
  userId: number;
  itemId: number;
  counts: number;
  buyDate: string;
  isDiscount: boolean;
}

function createBaseHasDataInfo(): HasDataInfo {
  return { userId: 0, itemId: 0, counts: 0, buyDate: "", isDiscount: false };
}

export const HasDataInfo: MessageFns<HasDataInfo> = {
  encode(message: HasDataInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).int32(message.userId);
    }
    if (message.itemId !== 0) {
      writer.uint32(16).int32(message.itemId);
    }
    if (message.counts !== 0) {
      writer.uint32(24).int32(message.counts);
    }
    if (message.buyDate !== "") {
      writer.uint32(34).string(message.buyDate);
    }
    if (message.isDiscount !== false) {
      writer.uint32(40).bool(message.isDiscount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HasDataInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHasDataInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.itemId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.counts = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.buyDate = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isDiscount = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HasDataInfo {
    return {
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      itemId: isSet(object.itemId) ? globalThis.Number(object.itemId) : 0,
      counts: isSet(object.counts) ? globalThis.Number(object.counts) : 0,
      buyDate: isSet(object.buyDate) ? globalThis.String(object.buyDate) : "",
      isDiscount: isSet(object.isDiscount) ? globalThis.Boolean(object.isDiscount) : false,
    };
  },

  toJSON(message: HasDataInfo): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.itemId !== 0) {
      obj.itemId = Math.round(message.itemId);
    }
    if (message.counts !== 0) {
      obj.counts = Math.round(message.counts);
    }
    if (message.buyDate !== "") {
      obj.buyDate = message.buyDate;
    }
    if (message.isDiscount !== false) {
      obj.isDiscount = message.isDiscount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HasDataInfo>, I>>(base?: I): HasDataInfo {
    return HasDataInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HasDataInfo>, I>>(object: I): HasDataInfo {
    const message = createBaseHasDataInfo();
    message.userId = object.userId ?? 0;
    message.itemId = object.itemId ?? 0;
    message.counts = object.counts ?? 0;
    message.buyDate = object.buyDate ?? "";
    message.isDiscount = object.isDiscount ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
