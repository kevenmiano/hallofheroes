// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/PlayerNodeStateMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface PlayerNodeStateMsg {
  mapId: number;
  nodeId: number;
  isStand: boolean;
  armyId: number;
}

function createBasePlayerNodeStateMsg(): PlayerNodeStateMsg {
  return { mapId: 0, nodeId: 0, isStand: false, armyId: 0 };
}

export const PlayerNodeStateMsg: MessageFns<PlayerNodeStateMsg> = {
  encode(message: PlayerNodeStateMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mapId !== 0) {
      writer.uint32(8).int32(message.mapId);
    }
    if (message.nodeId !== 0) {
      writer.uint32(16).int32(message.nodeId);
    }
    if (message.isStand !== false) {
      writer.uint32(24).bool(message.isStand);
    }
    if (message.armyId !== 0) {
      writer.uint32(32).int32(message.armyId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerNodeStateMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerNodeStateMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.mapId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nodeId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isStand = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.armyId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerNodeStateMsg {
    return {
      mapId: isSet(object.mapId) ? globalThis.Number(object.mapId) : 0,
      nodeId: isSet(object.nodeId) ? globalThis.Number(object.nodeId) : 0,
      isStand: isSet(object.isStand) ? globalThis.Boolean(object.isStand) : false,
      armyId: isSet(object.armyId) ? globalThis.Number(object.armyId) : 0,
    };
  },

  toJSON(message: PlayerNodeStateMsg): unknown {
    const obj: any = {};
    if (message.mapId !== 0) {
      obj.mapId = Math.round(message.mapId);
    }
    if (message.nodeId !== 0) {
      obj.nodeId = Math.round(message.nodeId);
    }
    if (message.isStand !== false) {
      obj.isStand = message.isStand;
    }
    if (message.armyId !== 0) {
      obj.armyId = Math.round(message.armyId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerNodeStateMsg>, I>>(base?: I): PlayerNodeStateMsg {
    return PlayerNodeStateMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerNodeStateMsg>, I>>(object: I): PlayerNodeStateMsg {
    const message = createBasePlayerNodeStateMsg();
    message.mapId = object.mapId ?? 0;
    message.nodeId = object.nodeId ?? 0;
    message.isStand = object.isStand ?? false;
    message.armyId = object.armyId ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
