// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: simple/SimplePlayerListRspMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.simple";

export interface SimplePlayerListRspMsg {
  totalRows: number;
  playerInfo: number[];
  uuid: string;
}

function createBaseSimplePlayerListRspMsg(): SimplePlayerListRspMsg {
  return { totalRows: 0, playerInfo: [], uuid: "" };
}

export const SimplePlayerListRspMsg: MessageFns<SimplePlayerListRspMsg> = {
  encode(message: SimplePlayerListRspMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalRows !== 0) {
      writer.uint32(8).int32(message.totalRows);
    }
    writer.uint32(18).fork();
    for (const v of message.playerInfo) {
      writer.int32(v);
    }
    writer.join();
    if (message.uuid !== "") {
      writer.uint32(26).string(message.uuid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SimplePlayerListRspMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSimplePlayerListRspMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.totalRows = reader.int32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.playerInfo.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.playerInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.uuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SimplePlayerListRspMsg {
    return {
      totalRows: isSet(object.totalRows) ? globalThis.Number(object.totalRows) : 0,
      playerInfo: globalThis.Array.isArray(object?.playerInfo)
        ? object.playerInfo.map((e: any) => globalThis.Number(e))
        : [],
      uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "",
    };
  },

  toJSON(message: SimplePlayerListRspMsg): unknown {
    const obj: any = {};
    if (message.totalRows !== 0) {
      obj.totalRows = Math.round(message.totalRows);
    }
    if (message.playerInfo?.length) {
      obj.playerInfo = message.playerInfo.map((e) => Math.round(e));
    }
    if (message.uuid !== "") {
      obj.uuid = message.uuid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SimplePlayerListRspMsg>, I>>(base?: I): SimplePlayerListRspMsg {
    return SimplePlayerListRspMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SimplePlayerListRspMsg>, I>>(object: I): SimplePlayerListRspMsg {
    const message = createBaseSimplePlayerListRspMsg();
    message.totalRows = object.totalRows ?? 0;
    message.playerInfo = object.playerInfo?.map((e) => e) || [];
    message.uuid = object.uuid ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
