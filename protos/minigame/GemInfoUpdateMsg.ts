// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: minigame/GemInfoUpdateMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.minigame";

export interface GemInfoUpdateMsg {
  gemDel: number[];
  gemAdd: number[];
  score: number[];
  curExp: number;
  maxExp: number;
  gemLevel: number;
  scoreOrder: number;
  rewardInfo: number[];
}

function createBaseGemInfoUpdateMsg(): GemInfoUpdateMsg {
  return { gemDel: [], gemAdd: [], score: [], curExp: 0, maxExp: 0, gemLevel: 0, scoreOrder: 0, rewardInfo: [] };
}

export const GemInfoUpdateMsg: MessageFns<GemInfoUpdateMsg> = {
  encode(message: GemInfoUpdateMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.gemDel) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.gemAdd) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.score) {
      writer.int32(v);
    }
    writer.join();
    if (message.curExp !== 0) {
      writer.uint32(32).int32(message.curExp);
    }
    if (message.maxExp !== 0) {
      writer.uint32(40).int32(message.maxExp);
    }
    if (message.gemLevel !== 0) {
      writer.uint32(48).int32(message.gemLevel);
    }
    if (message.scoreOrder !== 0) {
      writer.uint32(56).int32(message.scoreOrder);
    }
    writer.uint32(66).fork();
    for (const v of message.rewardInfo) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GemInfoUpdateMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGemInfoUpdateMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.gemDel.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.gemDel.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.gemAdd.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.gemAdd.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.score.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.score.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.curExp = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxExp = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.gemLevel = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.scoreOrder = reader.int32();
          continue;
        }
        case 8: {
          if (tag === 64) {
            message.rewardInfo.push(reader.int32());

            continue;
          }

          if (tag === 66) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.rewardInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GemInfoUpdateMsg {
    return {
      gemDel: globalThis.Array.isArray(object?.gemDel) ? object.gemDel.map((e: any) => globalThis.Number(e)) : [],
      gemAdd: globalThis.Array.isArray(object?.gemAdd) ? object.gemAdd.map((e: any) => globalThis.Number(e)) : [],
      score: globalThis.Array.isArray(object?.score) ? object.score.map((e: any) => globalThis.Number(e)) : [],
      curExp: isSet(object.curExp) ? globalThis.Number(object.curExp) : 0,
      maxExp: isSet(object.maxExp) ? globalThis.Number(object.maxExp) : 0,
      gemLevel: isSet(object.gemLevel) ? globalThis.Number(object.gemLevel) : 0,
      scoreOrder: isSet(object.scoreOrder) ? globalThis.Number(object.scoreOrder) : 0,
      rewardInfo: globalThis.Array.isArray(object?.rewardInfo)
        ? object.rewardInfo.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: GemInfoUpdateMsg): unknown {
    const obj: any = {};
    if (message.gemDel?.length) {
      obj.gemDel = message.gemDel.map((e) => Math.round(e));
    }
    if (message.gemAdd?.length) {
      obj.gemAdd = message.gemAdd.map((e) => Math.round(e));
    }
    if (message.score?.length) {
      obj.score = message.score.map((e) => Math.round(e));
    }
    if (message.curExp !== 0) {
      obj.curExp = Math.round(message.curExp);
    }
    if (message.maxExp !== 0) {
      obj.maxExp = Math.round(message.maxExp);
    }
    if (message.gemLevel !== 0) {
      obj.gemLevel = Math.round(message.gemLevel);
    }
    if (message.scoreOrder !== 0) {
      obj.scoreOrder = Math.round(message.scoreOrder);
    }
    if (message.rewardInfo?.length) {
      obj.rewardInfo = message.rewardInfo.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GemInfoUpdateMsg>, I>>(base?: I): GemInfoUpdateMsg {
    return GemInfoUpdateMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GemInfoUpdateMsg>, I>>(object: I): GemInfoUpdateMsg {
    const message = createBaseGemInfoUpdateMsg();
    message.gemDel = object.gemDel?.map((e) => e) || [];
    message.gemAdd = object.gemAdd?.map((e) => e) || [];
    message.score = object.score?.map((e) => e) || [];
    message.curExp = object.curExp ?? 0;
    message.maxExp = object.maxExp ?? 0;
    message.gemLevel = object.gemLevel ?? 0;
    message.scoreOrder = object.scoreOrder ?? 0;
    message.rewardInfo = object.rewardInfo?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
