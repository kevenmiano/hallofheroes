// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: item/BottleMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.item";

export interface BottleMsg {
  playerBottle: number[];
  total: number;
  isNewRound: boolean;
}

function createBaseBottleMsg(): BottleMsg {
  return { playerBottle: [], total: 0, isNewRound: false };
}

export const BottleMsg: MessageFns<BottleMsg> = {
  encode(message: BottleMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.playerBottle) {
      writer.int32(v);
    }
    writer.join();
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    if (message.isNewRound !== false) {
      writer.uint32(24).bool(message.isNewRound);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BottleMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBottleMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.playerBottle.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.playerBottle.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isNewRound = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BottleMsg {
    return {
      playerBottle: globalThis.Array.isArray(object?.playerBottle)
        ? object.playerBottle.map((e: any) => globalThis.Number(e))
        : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
      isNewRound: isSet(object.isNewRound) ? globalThis.Boolean(object.isNewRound) : false,
    };
  },

  toJSON(message: BottleMsg): unknown {
    const obj: any = {};
    if (message.playerBottle?.length) {
      obj.playerBottle = message.playerBottle.map((e) => Math.round(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    if (message.isNewRound !== false) {
      obj.isNewRound = message.isNewRound;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BottleMsg>, I>>(base?: I): BottleMsg {
    return BottleMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BottleMsg>, I>>(object: I): BottleMsg {
    const message = createBaseBottleMsg();
    message.playerBottle = object.playerBottle?.map((e) => e) || [];
    message.total = object.total ?? 0;
    message.isNewRound = object.isNewRound ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
