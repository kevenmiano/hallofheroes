// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: qq/QQZoneGiftBagState.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.qq";

export interface QQZoneGiftBagState {
  novicesGift: boolean;
  gradeGifts: number[];
}

function createBaseQQZoneGiftBagState(): QQZoneGiftBagState {
  return { novicesGift: false, gradeGifts: [] };
}

export const QQZoneGiftBagState: MessageFns<QQZoneGiftBagState> = {
  encode(message: QQZoneGiftBagState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.novicesGift !== false) {
      writer.uint32(8).bool(message.novicesGift);
    }
    writer.uint32(18).fork();
    for (const v of message.gradeGifts) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QQZoneGiftBagState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQQZoneGiftBagState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.novicesGift = reader.bool();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.gradeGifts.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.gradeGifts.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QQZoneGiftBagState {
    return {
      novicesGift: isSet(object.novicesGift) ? globalThis.Boolean(object.novicesGift) : false,
      gradeGifts: globalThis.Array.isArray(object?.gradeGifts)
        ? object.gradeGifts.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: QQZoneGiftBagState): unknown {
    const obj: any = {};
    if (message.novicesGift !== false) {
      obj.novicesGift = message.novicesGift;
    }
    if (message.gradeGifts?.length) {
      obj.gradeGifts = message.gradeGifts.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QQZoneGiftBagState>, I>>(base?: I): QQZoneGiftBagState {
    return QQZoneGiftBagState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QQZoneGiftBagState>, I>>(object: I): QQZoneGiftBagState {
    const message = createBaseQQZoneGiftBagState();
    message.novicesGift = object.novicesGift ?? false;
    message.gradeGifts = object.gradeGifts?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
