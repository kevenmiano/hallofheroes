// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: goldenroad/GoldenRoadMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.goldenroad";

export interface GoldenRoadMsg {
  playerStatue: string;
  score: string;
  cellInfo: number[];
}

function createBaseGoldenRoadMsg(): GoldenRoadMsg {
  return { playerStatue: "", score: "", cellInfo: [] };
}

export const GoldenRoadMsg: MessageFns<GoldenRoadMsg> = {
  encode(message: GoldenRoadMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerStatue !== "") {
      writer.uint32(10).string(message.playerStatue);
    }
    if (message.score !== "") {
      writer.uint32(18).string(message.score);
    }
    writer.uint32(26).fork();
    for (const v of message.cellInfo) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GoldenRoadMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGoldenRoadMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playerStatue = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.score = reader.string();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.cellInfo.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cellInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GoldenRoadMsg {
    return {
      playerStatue: isSet(object.playerStatue) ? globalThis.String(object.playerStatue) : "",
      score: isSet(object.score) ? globalThis.String(object.score) : "",
      cellInfo: globalThis.Array.isArray(object?.cellInfo) ? object.cellInfo.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: GoldenRoadMsg): unknown {
    const obj: any = {};
    if (message.playerStatue !== "") {
      obj.playerStatue = message.playerStatue;
    }
    if (message.score !== "") {
      obj.score = message.score;
    }
    if (message.cellInfo?.length) {
      obj.cellInfo = message.cellInfo.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GoldenRoadMsg>, I>>(base?: I): GoldenRoadMsg {
    return GoldenRoadMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GoldenRoadMsg>, I>>(object: I): GoldenRoadMsg {
    const message = createBaseGoldenRoadMsg();
    message.playerStatue = object.playerStatue ?? "";
    message.score = object.score ?? "";
    message.cellInfo = object.cellInfo?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
