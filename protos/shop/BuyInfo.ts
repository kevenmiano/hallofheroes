// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: shop/BuyInfo.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.shop";

export interface BuyInfo {
  isDiscount: boolean;
  goodId: number;
  count: number;
  receiveId: number;
  result: number;
  type: number;
}

function createBaseBuyInfo(): BuyInfo {
  return { isDiscount: false, goodId: 0, count: 0, receiveId: 0, result: 0, type: 0 };
}

export const BuyInfo: MessageFns<BuyInfo> = {
  encode(message: BuyInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isDiscount !== false) {
      writer.uint32(8).bool(message.isDiscount);
    }
    if (message.goodId !== 0) {
      writer.uint32(16).int32(message.goodId);
    }
    if (message.count !== 0) {
      writer.uint32(24).int32(message.count);
    }
    if (message.receiveId !== 0) {
      writer.uint32(32).int32(message.receiveId);
    }
    if (message.result !== 0) {
      writer.uint32(40).int32(message.result);
    }
    if (message.type !== 0) {
      writer.uint32(48).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuyInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuyInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isDiscount = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.goodId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.receiveId = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.result = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.type = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuyInfo {
    return {
      isDiscount: isSet(object.isDiscount) ? globalThis.Boolean(object.isDiscount) : false,
      goodId: isSet(object.goodId) ? globalThis.Number(object.goodId) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      receiveId: isSet(object.receiveId) ? globalThis.Number(object.receiveId) : 0,
      result: isSet(object.result) ? globalThis.Number(object.result) : 0,
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
    };
  },

  toJSON(message: BuyInfo): unknown {
    const obj: any = {};
    if (message.isDiscount !== false) {
      obj.isDiscount = message.isDiscount;
    }
    if (message.goodId !== 0) {
      obj.goodId = Math.round(message.goodId);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.receiveId !== 0) {
      obj.receiveId = Math.round(message.receiveId);
    }
    if (message.result !== 0) {
      obj.result = Math.round(message.result);
    }
    if (message.type !== 0) {
      obj.type = Math.round(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuyInfo>, I>>(base?: I): BuyInfo {
    return BuyInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuyInfo>, I>>(object: I): BuyInfo {
    const message = createBaseBuyInfo();
    message.isDiscount = object.isDiscount ?? false;
    message.goodId = object.goodId ?? 0;
    message.count = object.count ?? 0;
    message.receiveId = object.receiveId ?? 0;
    message.result = object.result ?? 0;
    message.type = object.type ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
