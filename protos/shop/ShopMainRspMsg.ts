// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: shop/ShopMainRspMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.shop";

export interface ShopMainRspMsg {
  urlInfo: number[];
  goodInfo: number[];
  discountInfo: number[];
}

function createBaseShopMainRspMsg(): ShopMainRspMsg {
  return { urlInfo: [], goodInfo: [], discountInfo: [] };
}

export const ShopMainRspMsg: MessageFns<ShopMainRspMsg> = {
  encode(message: ShopMainRspMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.urlInfo) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.goodInfo) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.discountInfo) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShopMainRspMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShopMainRspMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.urlInfo.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.urlInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.goodInfo.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.goodInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.discountInfo.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.discountInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShopMainRspMsg {
    return {
      urlInfo: globalThis.Array.isArray(object?.urlInfo) ? object.urlInfo.map((e: any) => globalThis.Number(e)) : [],
      goodInfo: globalThis.Array.isArray(object?.goodInfo) ? object.goodInfo.map((e: any) => globalThis.Number(e)) : [],
      discountInfo: globalThis.Array.isArray(object?.discountInfo)
        ? object.discountInfo.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: ShopMainRspMsg): unknown {
    const obj: any = {};
    if (message.urlInfo?.length) {
      obj.urlInfo = message.urlInfo.map((e) => Math.round(e));
    }
    if (message.goodInfo?.length) {
      obj.goodInfo = message.goodInfo.map((e) => Math.round(e));
    }
    if (message.discountInfo?.length) {
      obj.discountInfo = message.discountInfo.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShopMainRspMsg>, I>>(base?: I): ShopMainRspMsg {
    return ShopMainRspMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShopMainRspMsg>, I>>(object: I): ShopMainRspMsg {
    const message = createBaseShopMainRspMsg();
    message.urlInfo = object.urlInfo?.map((e) => e) || [];
    message.goodInfo = object.goodInfo?.map((e) => e) || [];
    message.discountInfo = object.discountInfo?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
