// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: item/BottleItemInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.item";

export interface BottleItemInfoMsg {
  tempId: number;
  count: number;
  isShow: boolean;
  isNewItems: boolean;
}

function createBaseBottleItemInfoMsg(): BottleItemInfoMsg {
  return { tempId: 0, count: 0, isShow: false, isNewItems: false };
}

export const BottleItemInfoMsg: MessageFns<BottleItemInfoMsg> = {
  encode(message: BottleItemInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tempId !== 0) {
      writer.uint32(8).int32(message.tempId);
    }
    if (message.count !== 0) {
      writer.uint32(16).int32(message.count);
    }
    if (message.isShow !== false) {
      writer.uint32(24).bool(message.isShow);
    }
    if (message.isNewItems !== false) {
      writer.uint32(32).bool(message.isNewItems);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BottleItemInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBottleItemInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.tempId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isShow = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isNewItems = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BottleItemInfoMsg {
    return {
      tempId: isSet(object.tempId) ? globalThis.Number(object.tempId) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      isShow: isSet(object.isShow) ? globalThis.Boolean(object.isShow) : false,
      isNewItems: isSet(object.isNewItems) ? globalThis.Boolean(object.isNewItems) : false,
    };
  },

  toJSON(message: BottleItemInfoMsg): unknown {
    const obj: any = {};
    if (message.tempId !== 0) {
      obj.tempId = Math.round(message.tempId);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.isShow !== false) {
      obj.isShow = message.isShow;
    }
    if (message.isNewItems !== false) {
      obj.isNewItems = message.isNewItems;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BottleItemInfoMsg>, I>>(base?: I): BottleItemInfoMsg {
    return BottleItemInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BottleItemInfoMsg>, I>>(object: I): BottleItemInfoMsg {
    const message = createBaseBottleItemInfoMsg();
    message.tempId = object.tempId ?? 0;
    message.count = object.count ?? 0;
    message.isShow = object.isShow ?? false;
    message.isNewItems = object.isNewItems ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
