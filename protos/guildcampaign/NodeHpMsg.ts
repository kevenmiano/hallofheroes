// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: guildcampaign/NodeHpMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.guildcampaign";

export interface NodeHpMsg {
  nodeId: number;
  currentHp: number;
  totalHp: number;
  param1: number;
}

function createBaseNodeHpMsg(): NodeHpMsg {
  return { nodeId: 0, currentHp: 0, totalHp: 0, param1: 0 };
}

export const NodeHpMsg: MessageFns<NodeHpMsg> = {
  encode(message: NodeHpMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeId !== 0) {
      writer.uint32(8).int32(message.nodeId);
    }
    if (message.currentHp !== 0) {
      writer.uint32(16).int32(message.currentHp);
    }
    if (message.totalHp !== 0) {
      writer.uint32(24).int32(message.totalHp);
    }
    if (message.param1 !== 0) {
      writer.uint32(32).int32(message.param1);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodeHpMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodeHpMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.nodeId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.currentHp = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalHp = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.param1 = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodeHpMsg {
    return {
      nodeId: isSet(object.nodeId) ? globalThis.Number(object.nodeId) : 0,
      currentHp: isSet(object.currentHp) ? globalThis.Number(object.currentHp) : 0,
      totalHp: isSet(object.totalHp) ? globalThis.Number(object.totalHp) : 0,
      param1: isSet(object.param1) ? globalThis.Number(object.param1) : 0,
    };
  },

  toJSON(message: NodeHpMsg): unknown {
    const obj: any = {};
    if (message.nodeId !== 0) {
      obj.nodeId = Math.round(message.nodeId);
    }
    if (message.currentHp !== 0) {
      obj.currentHp = Math.round(message.currentHp);
    }
    if (message.totalHp !== 0) {
      obj.totalHp = Math.round(message.totalHp);
    }
    if (message.param1 !== 0) {
      obj.param1 = Math.round(message.param1);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodeHpMsg>, I>>(base?: I): NodeHpMsg {
    return NodeHpMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodeHpMsg>, I>>(object: I): NodeHpMsg {
    const message = createBaseNodeHpMsg();
    message.nodeId = object.nodeId ?? 0;
    message.currentHp = object.currentHp ?? 0;
    message.totalHp = object.totalHp ?? 0;
    message.param1 = object.param1 ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
