// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: stackhead/StackHeadGuildMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.stackhead";

export interface StackHeadGuildMsg {
  op: number;
  current: number;
  total: number;
  guildInfo: string;
  userList: number[];
}

function createBaseStackHeadGuildMsg(): StackHeadGuildMsg {
  return { op: 0, current: 0, total: 0, guildInfo: "", userList: [] };
}

export const StackHeadGuildMsg: MessageFns<StackHeadGuildMsg> = {
  encode(message: StackHeadGuildMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.op !== 0) {
      writer.uint32(8).int32(message.op);
    }
    if (message.current !== 0) {
      writer.uint32(16).int32(message.current);
    }
    if (message.total !== 0) {
      writer.uint32(24).int32(message.total);
    }
    if (message.guildInfo !== "") {
      writer.uint32(34).string(message.guildInfo);
    }
    writer.uint32(42).fork();
    for (const v of message.userList) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StackHeadGuildMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStackHeadGuildMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.op = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.current = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.guildInfo = reader.string();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.userList.push(reader.int32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.userList.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StackHeadGuildMsg {
    return {
      op: isSet(object.op) ? globalThis.Number(object.op) : 0,
      current: isSet(object.current) ? globalThis.Number(object.current) : 0,
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
      guildInfo: isSet(object.guildInfo) ? globalThis.String(object.guildInfo) : "",
      userList: globalThis.Array.isArray(object?.userList) ? object.userList.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: StackHeadGuildMsg): unknown {
    const obj: any = {};
    if (message.op !== 0) {
      obj.op = Math.round(message.op);
    }
    if (message.current !== 0) {
      obj.current = Math.round(message.current);
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    if (message.guildInfo !== "") {
      obj.guildInfo = message.guildInfo;
    }
    if (message.userList?.length) {
      obj.userList = message.userList.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StackHeadGuildMsg>, I>>(base?: I): StackHeadGuildMsg {
    return StackHeadGuildMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StackHeadGuildMsg>, I>>(object: I): StackHeadGuildMsg {
    const message = createBaseStackHeadGuildMsg();
    message.op = object.op ?? 0;
    message.current = object.current ?? 0;
    message.total = object.total ?? 0;
    message.guildInfo = object.guildInfo ?? "";
    message.userList = object.userList?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
