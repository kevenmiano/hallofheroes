// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/CoolDownMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface CoolDownMsg {
  templatedId: number;
  coolDown: number;
  coolType: number;
  appearCoolDown: number;
}

function createBaseCoolDownMsg(): CoolDownMsg {
  return { templatedId: 0, coolDown: 0, coolType: 0, appearCoolDown: 0 };
}

export const CoolDownMsg: MessageFns<CoolDownMsg> = {
  encode(message: CoolDownMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.templatedId !== 0) {
      writer.uint32(8).int32(message.templatedId);
    }
    if (message.coolDown !== 0) {
      writer.uint32(16).int32(message.coolDown);
    }
    if (message.coolType !== 0) {
      writer.uint32(24).int32(message.coolType);
    }
    if (message.appearCoolDown !== 0) {
      writer.uint32(32).int32(message.appearCoolDown);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CoolDownMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCoolDownMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.templatedId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.coolDown = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.coolType = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.appearCoolDown = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CoolDownMsg {
    return {
      templatedId: isSet(object.templatedId) ? globalThis.Number(object.templatedId) : 0,
      coolDown: isSet(object.coolDown) ? globalThis.Number(object.coolDown) : 0,
      coolType: isSet(object.coolType) ? globalThis.Number(object.coolType) : 0,
      appearCoolDown: isSet(object.appearCoolDown) ? globalThis.Number(object.appearCoolDown) : 0,
    };
  },

  toJSON(message: CoolDownMsg): unknown {
    const obj: any = {};
    if (message.templatedId !== 0) {
      obj.templatedId = Math.round(message.templatedId);
    }
    if (message.coolDown !== 0) {
      obj.coolDown = Math.round(message.coolDown);
    }
    if (message.coolType !== 0) {
      obj.coolType = Math.round(message.coolType);
    }
    if (message.appearCoolDown !== 0) {
      obj.appearCoolDown = Math.round(message.appearCoolDown);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CoolDownMsg>, I>>(base?: I): CoolDownMsg {
    return CoolDownMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CoolDownMsg>, I>>(object: I): CoolDownMsg {
    const message = createBaseCoolDownMsg();
    message.templatedId = object.templatedId ?? 0;
    message.coolDown = object.coolDown ?? 0;
    message.coolType = object.coolType ?? 0;
    message.appearCoolDown = object.appearCoolDown ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
