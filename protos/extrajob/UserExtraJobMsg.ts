// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: extrajob/UserExtraJobMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.extrajob";

export interface UserExtraJobMsg {
  targetUserid: number;
  activeList: number[];
  equipList: number[];
}

function createBaseUserExtraJobMsg(): UserExtraJobMsg {
  return { targetUserid: 0, activeList: [], equipList: [] };
}

export const UserExtraJobMsg: MessageFns<UserExtraJobMsg> = {
  encode(message: UserExtraJobMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.targetUserid !== 0) {
      writer.uint32(8).int32(message.targetUserid);
    }
    writer.uint32(18).fork();
    for (const v of message.activeList) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.equipList) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserExtraJobMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserExtraJobMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.targetUserid = reader.int32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.activeList.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.activeList.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.equipList.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.equipList.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserExtraJobMsg {
    return {
      targetUserid: isSet(object.targetUserid) ? globalThis.Number(object.targetUserid) : 0,
      activeList: globalThis.Array.isArray(object?.activeList)
        ? object.activeList.map((e: any) => globalThis.Number(e))
        : [],
      equipList: globalThis.Array.isArray(object?.equipList)
        ? object.equipList.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: UserExtraJobMsg): unknown {
    const obj: any = {};
    if (message.targetUserid !== 0) {
      obj.targetUserid = Math.round(message.targetUserid);
    }
    if (message.activeList?.length) {
      obj.activeList = message.activeList.map((e) => Math.round(e));
    }
    if (message.equipList?.length) {
      obj.equipList = message.equipList.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserExtraJobMsg>, I>>(base?: I): UserExtraJobMsg {
    return UserExtraJobMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserExtraJobMsg>, I>>(object: I): UserExtraJobMsg {
    const message = createBaseUserExtraJobMsg();
    message.targetUserid = object.targetUserid ?? 0;
    message.activeList = object.activeList?.map((e) => e) || [];
    message.equipList = object.equipList?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
