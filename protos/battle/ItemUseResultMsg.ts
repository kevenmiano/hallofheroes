// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/ItemUseResultMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface ItemUseResultMsg {
  uItemId: number;
  bagPos: number;
  count: number;
  result: number;
  livingId: number;
  tempId: number;
  type: number;
}

function createBaseItemUseResultMsg(): ItemUseResultMsg {
  return { uItemId: 0, bagPos: 0, count: 0, result: 0, livingId: 0, tempId: 0, type: 0 };
}

export const ItemUseResultMsg: MessageFns<ItemUseResultMsg> = {
  encode(message: ItemUseResultMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uItemId !== 0) {
      writer.uint32(8).int32(message.uItemId);
    }
    if (message.bagPos !== 0) {
      writer.uint32(16).int32(message.bagPos);
    }
    if (message.count !== 0) {
      writer.uint32(24).int32(message.count);
    }
    if (message.result !== 0) {
      writer.uint32(32).int32(message.result);
    }
    if (message.livingId !== 0) {
      writer.uint32(40).int32(message.livingId);
    }
    if (message.tempId !== 0) {
      writer.uint32(48).int32(message.tempId);
    }
    if (message.type !== 0) {
      writer.uint32(56).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ItemUseResultMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseItemUseResultMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.uItemId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.bagPos = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.result = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.livingId = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.tempId = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.type = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ItemUseResultMsg {
    return {
      uItemId: isSet(object.uItemId) ? globalThis.Number(object.uItemId) : 0,
      bagPos: isSet(object.bagPos) ? globalThis.Number(object.bagPos) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      result: isSet(object.result) ? globalThis.Number(object.result) : 0,
      livingId: isSet(object.livingId) ? globalThis.Number(object.livingId) : 0,
      tempId: isSet(object.tempId) ? globalThis.Number(object.tempId) : 0,
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
    };
  },

  toJSON(message: ItemUseResultMsg): unknown {
    const obj: any = {};
    if (message.uItemId !== 0) {
      obj.uItemId = Math.round(message.uItemId);
    }
    if (message.bagPos !== 0) {
      obj.bagPos = Math.round(message.bagPos);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.result !== 0) {
      obj.result = Math.round(message.result);
    }
    if (message.livingId !== 0) {
      obj.livingId = Math.round(message.livingId);
    }
    if (message.tempId !== 0) {
      obj.tempId = Math.round(message.tempId);
    }
    if (message.type !== 0) {
      obj.type = Math.round(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ItemUseResultMsg>, I>>(base?: I): ItemUseResultMsg {
    return ItemUseResultMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ItemUseResultMsg>, I>>(object: I): ItemUseResultMsg {
    const message = createBaseItemUseResultMsg();
    message.uItemId = object.uItemId ?? 0;
    message.bagPos = object.bagPos ?? 0;
    message.count = object.count ?? 0;
    message.result = object.result ?? 0;
    message.livingId = object.livingId ?? 0;
    message.tempId = object.tempId ?? 0;
    message.type = object.type ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
