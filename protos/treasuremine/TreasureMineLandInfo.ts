// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: treasuremine/TreasureMineLandInfo.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.treasuremine";

export interface TreasureMineLandInfo {
  id: number;
  mapId: number;
  posX: number;
  posY: number;
  templateId: number;
  type: number;
  occupyPlayerId: number;
  occupyPlayerName: string;
  occupyGuildId: number;
  occupyGuildName: string;
  state: number;
  startFightTime: string;
}

function createBaseTreasureMineLandInfo(): TreasureMineLandInfo {
  return {
    id: 0,
    mapId: 0,
    posX: 0,
    posY: 0,
    templateId: 0,
    type: 0,
    occupyPlayerId: 0,
    occupyPlayerName: "",
    occupyGuildId: 0,
    occupyGuildName: "",
    state: 0,
    startFightTime: "",
  };
}

export const TreasureMineLandInfo: MessageFns<TreasureMineLandInfo> = {
  encode(message: TreasureMineLandInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.mapId !== 0) {
      writer.uint32(16).int32(message.mapId);
    }
    if (message.posX !== 0) {
      writer.uint32(24).int32(message.posX);
    }
    if (message.posY !== 0) {
      writer.uint32(32).int32(message.posY);
    }
    if (message.templateId !== 0) {
      writer.uint32(40).int32(message.templateId);
    }
    if (message.type !== 0) {
      writer.uint32(48).int32(message.type);
    }
    if (message.occupyPlayerId !== 0) {
      writer.uint32(56).int32(message.occupyPlayerId);
    }
    if (message.occupyPlayerName !== "") {
      writer.uint32(66).string(message.occupyPlayerName);
    }
    if (message.occupyGuildId !== 0) {
      writer.uint32(72).int32(message.occupyGuildId);
    }
    if (message.occupyGuildName !== "") {
      writer.uint32(82).string(message.occupyGuildName);
    }
    if (message.state !== 0) {
      writer.uint32(88).int32(message.state);
    }
    if (message.startFightTime !== "") {
      writer.uint32(98).string(message.startFightTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TreasureMineLandInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTreasureMineLandInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.mapId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.posX = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.posY = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.templateId = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.type = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.occupyPlayerId = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.occupyPlayerName = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.occupyGuildId = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.occupyGuildName = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.state = reader.int32();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.startFightTime = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TreasureMineLandInfo {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      mapId: isSet(object.mapId) ? globalThis.Number(object.mapId) : 0,
      posX: isSet(object.posX) ? globalThis.Number(object.posX) : 0,
      posY: isSet(object.posY) ? globalThis.Number(object.posY) : 0,
      templateId: isSet(object.templateId) ? globalThis.Number(object.templateId) : 0,
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
      occupyPlayerId: isSet(object.occupyPlayerId) ? globalThis.Number(object.occupyPlayerId) : 0,
      occupyPlayerName: isSet(object.occupyPlayerName) ? globalThis.String(object.occupyPlayerName) : "",
      occupyGuildId: isSet(object.occupyGuildId) ? globalThis.Number(object.occupyGuildId) : 0,
      occupyGuildName: isSet(object.occupyGuildName) ? globalThis.String(object.occupyGuildName) : "",
      state: isSet(object.state) ? globalThis.Number(object.state) : 0,
      startFightTime: isSet(object.startFightTime) ? globalThis.String(object.startFightTime) : "",
    };
  },

  toJSON(message: TreasureMineLandInfo): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.mapId !== 0) {
      obj.mapId = Math.round(message.mapId);
    }
    if (message.posX !== 0) {
      obj.posX = Math.round(message.posX);
    }
    if (message.posY !== 0) {
      obj.posY = Math.round(message.posY);
    }
    if (message.templateId !== 0) {
      obj.templateId = Math.round(message.templateId);
    }
    if (message.type !== 0) {
      obj.type = Math.round(message.type);
    }
    if (message.occupyPlayerId !== 0) {
      obj.occupyPlayerId = Math.round(message.occupyPlayerId);
    }
    if (message.occupyPlayerName !== "") {
      obj.occupyPlayerName = message.occupyPlayerName;
    }
    if (message.occupyGuildId !== 0) {
      obj.occupyGuildId = Math.round(message.occupyGuildId);
    }
    if (message.occupyGuildName !== "") {
      obj.occupyGuildName = message.occupyGuildName;
    }
    if (message.state !== 0) {
      obj.state = Math.round(message.state);
    }
    if (message.startFightTime !== "") {
      obj.startFightTime = message.startFightTime;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TreasureMineLandInfo>, I>>(base?: I): TreasureMineLandInfo {
    return TreasureMineLandInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TreasureMineLandInfo>, I>>(object: I): TreasureMineLandInfo {
    const message = createBaseTreasureMineLandInfo();
    message.id = object.id ?? 0;
    message.mapId = object.mapId ?? 0;
    message.posX = object.posX ?? 0;
    message.posY = object.posY ?? 0;
    message.templateId = object.templateId ?? 0;
    message.type = object.type ?? 0;
    message.occupyPlayerId = object.occupyPlayerId ?? 0;
    message.occupyPlayerName = object.occupyPlayerName ?? "";
    message.occupyGuildId = object.occupyGuildId ?? 0;
    message.occupyGuildName = object.occupyGuildName ?? "";
    message.state = object.state ?? 0;
    message.startFightTime = object.startFightTime ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
