// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: marriage/MarriageHeartMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.marriage";

export interface MarriageHeartMsg {
  curQuestion: string;
  lastQuestions: number[];
  syncRate: number;
  maxOrder: number;
}

function createBaseMarriageHeartMsg(): MarriageHeartMsg {
  return { curQuestion: "", lastQuestions: [], syncRate: 0, maxOrder: 0 };
}

export const MarriageHeartMsg: MessageFns<MarriageHeartMsg> = {
  encode(message: MarriageHeartMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.curQuestion !== "") {
      writer.uint32(10).string(message.curQuestion);
    }
    writer.uint32(18).fork();
    for (const v of message.lastQuestions) {
      writer.int32(v);
    }
    writer.join();
    if (message.syncRate !== 0) {
      writer.uint32(24).int32(message.syncRate);
    }
    if (message.maxOrder !== 0) {
      writer.uint32(32).int32(message.maxOrder);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarriageHeartMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarriageHeartMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.curQuestion = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.lastQuestions.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.lastQuestions.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.syncRate = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxOrder = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarriageHeartMsg {
    return {
      curQuestion: isSet(object.curQuestion) ? globalThis.String(object.curQuestion) : "",
      lastQuestions: globalThis.Array.isArray(object?.lastQuestions)
        ? object.lastQuestions.map((e: any) => globalThis.Number(e))
        : [],
      syncRate: isSet(object.syncRate) ? globalThis.Number(object.syncRate) : 0,
      maxOrder: isSet(object.maxOrder) ? globalThis.Number(object.maxOrder) : 0,
    };
  },

  toJSON(message: MarriageHeartMsg): unknown {
    const obj: any = {};
    if (message.curQuestion !== "") {
      obj.curQuestion = message.curQuestion;
    }
    if (message.lastQuestions?.length) {
      obj.lastQuestions = message.lastQuestions.map((e) => Math.round(e));
    }
    if (message.syncRate !== 0) {
      obj.syncRate = Math.round(message.syncRate);
    }
    if (message.maxOrder !== 0) {
      obj.maxOrder = Math.round(message.maxOrder);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarriageHeartMsg>, I>>(base?: I): MarriageHeartMsg {
    return MarriageHeartMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarriageHeartMsg>, I>>(object: I): MarriageHeartMsg {
    const message = createBaseMarriageHeartMsg();
    message.curQuestion = object.curQuestion ?? "";
    message.lastQuestions = object.lastQuestions?.map((e) => e) || [];
    message.syncRate = object.syncRate ?? 0;
    message.maxOrder = object.maxOrder ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
