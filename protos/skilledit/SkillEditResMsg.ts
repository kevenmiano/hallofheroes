// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: skilledit/SkillEditResMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.skilledit";

export interface SkillEditResMsg {
  skillInfo: number[];
  isOpen: boolean;
}

function createBaseSkillEditResMsg(): SkillEditResMsg {
  return { skillInfo: [], isOpen: false };
}

export const SkillEditResMsg: MessageFns<SkillEditResMsg> = {
  encode(message: SkillEditResMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.skillInfo) {
      writer.int32(v);
    }
    writer.join();
    if (message.isOpen !== false) {
      writer.uint32(16).bool(message.isOpen);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkillEditResMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkillEditResMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.skillInfo.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.skillInfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isOpen = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkillEditResMsg {
    return {
      skillInfo: globalThis.Array.isArray(object?.skillInfo)
        ? object.skillInfo.map((e: any) => globalThis.Number(e))
        : [],
      isOpen: isSet(object.isOpen) ? globalThis.Boolean(object.isOpen) : false,
    };
  },

  toJSON(message: SkillEditResMsg): unknown {
    const obj: any = {};
    if (message.skillInfo?.length) {
      obj.skillInfo = message.skillInfo.map((e) => Math.round(e));
    }
    if (message.isOpen !== false) {
      obj.isOpen = message.isOpen;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SkillEditResMsg>, I>>(base?: I): SkillEditResMsg {
    return SkillEditResMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SkillEditResMsg>, I>>(object: I): SkillEditResMsg {
    const message = createBaseSkillEditResMsg();
    message.skillInfo = object.skillInfo?.map((e) => e) || [];
    message.isOpen = object.isOpen ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
