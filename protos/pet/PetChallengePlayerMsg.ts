// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: pet/PetChallengePlayerMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.pet";

export interface PetChallengePlayerMsg {
  leftTime: number;
  challengeCount: number;
  totalCount: number;
  score: number;
  order: number;
}

function createBasePetChallengePlayerMsg(): PetChallengePlayerMsg {
  return { leftTime: 0, challengeCount: 0, totalCount: 0, score: 0, order: 0 };
}

export const PetChallengePlayerMsg: MessageFns<PetChallengePlayerMsg> = {
  encode(message: PetChallengePlayerMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.leftTime !== 0) {
      writer.uint32(8).int32(message.leftTime);
    }
    if (message.challengeCount !== 0) {
      writer.uint32(16).int32(message.challengeCount);
    }
    if (message.totalCount !== 0) {
      writer.uint32(24).int32(message.totalCount);
    }
    if (message.score !== 0) {
      writer.uint32(32).int32(message.score);
    }
    if (message.order !== 0) {
      writer.uint32(40).int32(message.order);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PetChallengePlayerMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePetChallengePlayerMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.leftTime = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.challengeCount = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalCount = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.score = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.order = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PetChallengePlayerMsg {
    return {
      leftTime: isSet(object.leftTime) ? globalThis.Number(object.leftTime) : 0,
      challengeCount: isSet(object.challengeCount) ? globalThis.Number(object.challengeCount) : 0,
      totalCount: isSet(object.totalCount) ? globalThis.Number(object.totalCount) : 0,
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      order: isSet(object.order) ? globalThis.Number(object.order) : 0,
    };
  },

  toJSON(message: PetChallengePlayerMsg): unknown {
    const obj: any = {};
    if (message.leftTime !== 0) {
      obj.leftTime = Math.round(message.leftTime);
    }
    if (message.challengeCount !== 0) {
      obj.challengeCount = Math.round(message.challengeCount);
    }
    if (message.totalCount !== 0) {
      obj.totalCount = Math.round(message.totalCount);
    }
    if (message.score !== 0) {
      obj.score = Math.round(message.score);
    }
    if (message.order !== 0) {
      obj.order = Math.round(message.order);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PetChallengePlayerMsg>, I>>(base?: I): PetChallengePlayerMsg {
    return PetChallengePlayerMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PetChallengePlayerMsg>, I>>(object: I): PetChallengePlayerMsg {
    const message = createBasePetChallengePlayerMsg();
    message.leftTime = object.leftTime ?? 0;
    message.challengeCount = object.challengeCount ?? 0;
    message.totalCount = object.totalCount ?? 0;
    message.score = object.score ?? 0;
    message.order = object.order ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
