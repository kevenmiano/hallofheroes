// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: guildcampaign/NodeHpListMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.guildcampaign";

export interface NodeHpListMsg {
  campaignId: number;
  campaignType: number;
  nodeHp: number[];
  userId: number[];
}

function createBaseNodeHpListMsg(): NodeHpListMsg {
  return { campaignId: 0, campaignType: 0, nodeHp: [], userId: [] };
}

export const NodeHpListMsg: MessageFns<NodeHpListMsg> = {
  encode(message: NodeHpListMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.campaignId !== 0) {
      writer.uint32(8).int32(message.campaignId);
    }
    if (message.campaignType !== 0) {
      writer.uint32(16).int32(message.campaignType);
    }
    writer.uint32(26).fork();
    for (const v of message.nodeHp) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(34).fork();
    for (const v of message.userId) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodeHpListMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodeHpListMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.campaignId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.campaignType = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.nodeHp.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.nodeHp.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag === 32) {
            message.userId.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.userId.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodeHpListMsg {
    return {
      campaignId: isSet(object.campaignId) ? globalThis.Number(object.campaignId) : 0,
      campaignType: isSet(object.campaignType) ? globalThis.Number(object.campaignType) : 0,
      nodeHp: globalThis.Array.isArray(object?.nodeHp) ? object.nodeHp.map((e: any) => globalThis.Number(e)) : [],
      userId: globalThis.Array.isArray(object?.userId) ? object.userId.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: NodeHpListMsg): unknown {
    const obj: any = {};
    if (message.campaignId !== 0) {
      obj.campaignId = Math.round(message.campaignId);
    }
    if (message.campaignType !== 0) {
      obj.campaignType = Math.round(message.campaignType);
    }
    if (message.nodeHp?.length) {
      obj.nodeHp = message.nodeHp.map((e) => Math.round(e));
    }
    if (message.userId?.length) {
      obj.userId = message.userId.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodeHpListMsg>, I>>(base?: I): NodeHpListMsg {
    return NodeHpListMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodeHpListMsg>, I>>(object: I): NodeHpListMsg {
    const message = createBaseNodeHpListMsg();
    message.campaignId = object.campaignId ?? 0;
    message.campaignType = object.campaignType ?? 0;
    message.nodeHp = object.nodeHp?.map((e) => e) || [];
    message.userId = object.userId?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
