// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: carnival/FiveCardPokerMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.carnival";

export interface FiveCardPokerMsg {
  op: number;
  curTurn: number;
  score: number;
  addScore: number;
  pokers: number[];
  holdList: number[];
}

function createBaseFiveCardPokerMsg(): FiveCardPokerMsg {
  return { op: 0, curTurn: 0, score: 0, addScore: 0, pokers: [], holdList: [] };
}

export const FiveCardPokerMsg: MessageFns<FiveCardPokerMsg> = {
  encode(message: FiveCardPokerMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.op !== 0) {
      writer.uint32(8).int32(message.op);
    }
    if (message.curTurn !== 0) {
      writer.uint32(16).int32(message.curTurn);
    }
    if (message.score !== 0) {
      writer.uint32(24).int32(message.score);
    }
    if (message.addScore !== 0) {
      writer.uint32(32).int32(message.addScore);
    }
    writer.uint32(42).fork();
    for (const v of message.pokers) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(50).fork();
    for (const v of message.holdList) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FiveCardPokerMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFiveCardPokerMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.op = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.curTurn = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.score = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.addScore = reader.int32();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.pokers.push(reader.int32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.pokers.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag === 48) {
            message.holdList.push(reader.int32());

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.holdList.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FiveCardPokerMsg {
    return {
      op: isSet(object.op) ? globalThis.Number(object.op) : 0,
      curTurn: isSet(object.curTurn) ? globalThis.Number(object.curTurn) : 0,
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      addScore: isSet(object.addScore) ? globalThis.Number(object.addScore) : 0,
      pokers: globalThis.Array.isArray(object?.pokers) ? object.pokers.map((e: any) => globalThis.Number(e)) : [],
      holdList: globalThis.Array.isArray(object?.holdList) ? object.holdList.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: FiveCardPokerMsg): unknown {
    const obj: any = {};
    if (message.op !== 0) {
      obj.op = Math.round(message.op);
    }
    if (message.curTurn !== 0) {
      obj.curTurn = Math.round(message.curTurn);
    }
    if (message.score !== 0) {
      obj.score = Math.round(message.score);
    }
    if (message.addScore !== 0) {
      obj.addScore = Math.round(message.addScore);
    }
    if (message.pokers?.length) {
      obj.pokers = message.pokers.map((e) => Math.round(e));
    }
    if (message.holdList?.length) {
      obj.holdList = message.holdList.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FiveCardPokerMsg>, I>>(base?: I): FiveCardPokerMsg {
    return FiveCardPokerMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FiveCardPokerMsg>, I>>(object: I): FiveCardPokerMsg {
    const message = createBaseFiveCardPokerMsg();
    message.op = object.op ?? 0;
    message.curTurn = object.curTurn ?? 0;
    message.score = object.score ?? 0;
    message.addScore = object.addScore ?? 0;
    message.pokers = object.pokers?.map((e) => e) || [];
    message.holdList = object.holdList?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
