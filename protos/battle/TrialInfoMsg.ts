// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/TrialInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface TrialInfoMsg {
  index: number;
  skillId: number;
  count: number;
  param1: number;
  param2: number;
  param3: number;
  param4: number;
}

function createBaseTrialInfoMsg(): TrialInfoMsg {
  return { index: 0, skillId: 0, count: 0, param1: 0, param2: 0, param3: 0, param4: 0 };
}

export const TrialInfoMsg: MessageFns<TrialInfoMsg> = {
  encode(message: TrialInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).int32(message.index);
    }
    if (message.skillId !== 0) {
      writer.uint32(16).int32(message.skillId);
    }
    if (message.count !== 0) {
      writer.uint32(24).int32(message.count);
    }
    if (message.param1 !== 0) {
      writer.uint32(32).int32(message.param1);
    }
    if (message.param2 !== 0) {
      writer.uint32(40).int32(message.param2);
    }
    if (message.param3 !== 0) {
      writer.uint32(48).int32(message.param3);
    }
    if (message.param4 !== 0) {
      writer.uint32(56).int32(message.param4);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrialInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrialInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.skillId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.param1 = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.param2 = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.param3 = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.param4 = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrialInfoMsg {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      skillId: isSet(object.skillId) ? globalThis.Number(object.skillId) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      param1: isSet(object.param1) ? globalThis.Number(object.param1) : 0,
      param2: isSet(object.param2) ? globalThis.Number(object.param2) : 0,
      param3: isSet(object.param3) ? globalThis.Number(object.param3) : 0,
      param4: isSet(object.param4) ? globalThis.Number(object.param4) : 0,
    };
  },

  toJSON(message: TrialInfoMsg): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.skillId !== 0) {
      obj.skillId = Math.round(message.skillId);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.param1 !== 0) {
      obj.param1 = Math.round(message.param1);
    }
    if (message.param2 !== 0) {
      obj.param2 = Math.round(message.param2);
    }
    if (message.param3 !== 0) {
      obj.param3 = Math.round(message.param3);
    }
    if (message.param4 !== 0) {
      obj.param4 = Math.round(message.param4);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrialInfoMsg>, I>>(base?: I): TrialInfoMsg {
    return TrialInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrialInfoMsg>, I>>(object: I): TrialInfoMsg {
    const message = createBaseTrialInfoMsg();
    message.index = object.index ?? 0;
    message.skillId = object.skillId ?? 0;
    message.count = object.count ?? 0;
    message.param1 = object.param1 ?? 0;
    message.param2 = object.param2 ?? 0;
    message.param3 = object.param3 ?? 0;
    message.param4 = object.param4 ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
