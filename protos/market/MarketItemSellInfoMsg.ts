// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: market/MarketItemSellInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.market";

export interface MarketItemSellInfoMsg {
  templateId: number;
  sellTotal: string;
  sellMinPrice: number;
  purchaseMaxPrice: number;
}

function createBaseMarketItemSellInfoMsg(): MarketItemSellInfoMsg {
  return { templateId: 0, sellTotal: "", sellMinPrice: 0, purchaseMaxPrice: 0 };
}

export const MarketItemSellInfoMsg: MessageFns<MarketItemSellInfoMsg> = {
  encode(message: MarketItemSellInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.templateId !== 0) {
      writer.uint32(8).int32(message.templateId);
    }
    if (message.sellTotal !== "") {
      writer.uint32(18).string(message.sellTotal);
    }
    if (message.sellMinPrice !== 0) {
      writer.uint32(24).int32(message.sellMinPrice);
    }
    if (message.purchaseMaxPrice !== 0) {
      writer.uint32(32).int32(message.purchaseMaxPrice);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketItemSellInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketItemSellInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.templateId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sellTotal = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sellMinPrice = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.purchaseMaxPrice = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketItemSellInfoMsg {
    return {
      templateId: isSet(object.templateId) ? globalThis.Number(object.templateId) : 0,
      sellTotal: isSet(object.sellTotal) ? globalThis.String(object.sellTotal) : "",
      sellMinPrice: isSet(object.sellMinPrice) ? globalThis.Number(object.sellMinPrice) : 0,
      purchaseMaxPrice: isSet(object.purchaseMaxPrice) ? globalThis.Number(object.purchaseMaxPrice) : 0,
    };
  },

  toJSON(message: MarketItemSellInfoMsg): unknown {
    const obj: any = {};
    if (message.templateId !== 0) {
      obj.templateId = Math.round(message.templateId);
    }
    if (message.sellTotal !== "") {
      obj.sellTotal = message.sellTotal;
    }
    if (message.sellMinPrice !== 0) {
      obj.sellMinPrice = Math.round(message.sellMinPrice);
    }
    if (message.purchaseMaxPrice !== 0) {
      obj.purchaseMaxPrice = Math.round(message.purchaseMaxPrice);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarketItemSellInfoMsg>, I>>(base?: I): MarketItemSellInfoMsg {
    return MarketItemSellInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarketItemSellInfoMsg>, I>>(object: I): MarketItemSellInfoMsg {
    const message = createBaseMarketItemSellInfoMsg();
    message.templateId = object.templateId ?? 0;
    message.sellTotal = object.sellTotal ?? "";
    message.sellMinPrice = object.sellMinPrice ?? 0;
    message.purchaseMaxPrice = object.purchaseMaxPrice ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
