// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: simple/RoomBossInfoListMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.simple";

export interface RoomBossInfoListMsg {
  castleRoomId: number;
  campaignId: number;
  nodeId: number;
  roomBoss: number[];
}

function createBaseRoomBossInfoListMsg(): RoomBossInfoListMsg {
  return { castleRoomId: 0, campaignId: 0, nodeId: 0, roomBoss: [] };
}

export const RoomBossInfoListMsg: MessageFns<RoomBossInfoListMsg> = {
  encode(message: RoomBossInfoListMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.castleRoomId !== 0) {
      writer.uint32(8).int32(message.castleRoomId);
    }
    if (message.campaignId !== 0) {
      writer.uint32(16).int32(message.campaignId);
    }
    if (message.nodeId !== 0) {
      writer.uint32(24).int32(message.nodeId);
    }
    writer.uint32(34).fork();
    for (const v of message.roomBoss) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomBossInfoListMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomBossInfoListMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.castleRoomId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.campaignId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.nodeId = reader.int32();
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.roomBoss.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.roomBoss.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomBossInfoListMsg {
    return {
      castleRoomId: isSet(object.castleRoomId) ? globalThis.Number(object.castleRoomId) : 0,
      campaignId: isSet(object.campaignId) ? globalThis.Number(object.campaignId) : 0,
      nodeId: isSet(object.nodeId) ? globalThis.Number(object.nodeId) : 0,
      roomBoss: globalThis.Array.isArray(object?.roomBoss) ? object.roomBoss.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: RoomBossInfoListMsg): unknown {
    const obj: any = {};
    if (message.castleRoomId !== 0) {
      obj.castleRoomId = Math.round(message.castleRoomId);
    }
    if (message.campaignId !== 0) {
      obj.campaignId = Math.round(message.campaignId);
    }
    if (message.nodeId !== 0) {
      obj.nodeId = Math.round(message.nodeId);
    }
    if (message.roomBoss?.length) {
      obj.roomBoss = message.roomBoss.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoomBossInfoListMsg>, I>>(base?: I): RoomBossInfoListMsg {
    return RoomBossInfoListMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoomBossInfoListMsg>, I>>(object: I): RoomBossInfoListMsg {
    const message = createBaseRoomBossInfoListMsg();
    message.castleRoomId = object.castleRoomId ?? 0;
    message.campaignId = object.campaignId ?? 0;
    message.nodeId = object.nodeId ?? 0;
    message.roomBoss = object.roomBoss?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
