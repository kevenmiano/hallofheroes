// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: farm/TreeUpdateRsp.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.farm";

export interface TreeUpdateRsp {
  tree: number[];
  harvestMsg: number[];
  pickMsg: number[];
  isUpGrade: boolean;
}

function createBaseTreeUpdateRsp(): TreeUpdateRsp {
  return { tree: [], harvestMsg: [], pickMsg: [], isUpGrade: false };
}

export const TreeUpdateRsp: MessageFns<TreeUpdateRsp> = {
  encode(message: TreeUpdateRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.tree) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.harvestMsg) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.pickMsg) {
      writer.int32(v);
    }
    writer.join();
    if (message.isUpGrade !== false) {
      writer.uint32(32).bool(message.isUpGrade);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TreeUpdateRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTreeUpdateRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.tree.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.tree.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.harvestMsg.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.harvestMsg.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.pickMsg.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.pickMsg.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isUpGrade = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TreeUpdateRsp {
    return {
      tree: globalThis.Array.isArray(object?.tree) ? object.tree.map((e: any) => globalThis.Number(e)) : [],
      harvestMsg: globalThis.Array.isArray(object?.harvestMsg)
        ? object.harvestMsg.map((e: any) => globalThis.Number(e))
        : [],
      pickMsg: globalThis.Array.isArray(object?.pickMsg) ? object.pickMsg.map((e: any) => globalThis.Number(e)) : [],
      isUpGrade: isSet(object.isUpGrade) ? globalThis.Boolean(object.isUpGrade) : false,
    };
  },

  toJSON(message: TreeUpdateRsp): unknown {
    const obj: any = {};
    if (message.tree?.length) {
      obj.tree = message.tree.map((e) => Math.round(e));
    }
    if (message.harvestMsg?.length) {
      obj.harvestMsg = message.harvestMsg.map((e) => Math.round(e));
    }
    if (message.pickMsg?.length) {
      obj.pickMsg = message.pickMsg.map((e) => Math.round(e));
    }
    if (message.isUpGrade !== false) {
      obj.isUpGrade = message.isUpGrade;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TreeUpdateRsp>, I>>(base?: I): TreeUpdateRsp {
    return TreeUpdateRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TreeUpdateRsp>, I>>(object: I): TreeUpdateRsp {
    const message = createBaseTreeUpdateRsp();
    message.tree = object.tree?.map((e) => e) || [];
    message.harvestMsg = object.harvestMsg?.map((e) => e) || [];
    message.pickMsg = object.pickMsg?.map((e) => e) || [];
    message.isUpGrade = object.isUpGrade ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
