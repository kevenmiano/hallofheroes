// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: active/GameStoryMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.active";

export interface GameStoryMsg {
  isAccess: boolean;
  index: number;
  isDrop: boolean;
}

function createBaseGameStoryMsg(): GameStoryMsg {
  return { isAccess: false, index: 0, isDrop: false };
}

export const GameStoryMsg: MessageFns<GameStoryMsg> = {
  encode(message: GameStoryMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isAccess !== false) {
      writer.uint32(8).bool(message.isAccess);
    }
    if (message.index !== 0) {
      writer.uint32(16).int32(message.index);
    }
    if (message.isDrop !== false) {
      writer.uint32(24).bool(message.isDrop);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameStoryMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameStoryMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isAccess = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.index = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isDrop = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameStoryMsg {
    return {
      isAccess: isSet(object.isAccess) ? globalThis.Boolean(object.isAccess) : false,
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      isDrop: isSet(object.isDrop) ? globalThis.Boolean(object.isDrop) : false,
    };
  },

  toJSON(message: GameStoryMsg): unknown {
    const obj: any = {};
    if (message.isAccess !== false) {
      obj.isAccess = message.isAccess;
    }
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.isDrop !== false) {
      obj.isDrop = message.isDrop;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameStoryMsg>, I>>(base?: I): GameStoryMsg {
    return GameStoryMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameStoryMsg>, I>>(object: I): GameStoryMsg {
    const message = createBaseGameStoryMsg();
    message.isAccess = object.isAccess ?? false;
    message.index = object.index ?? 0;
    message.isDrop = object.isDrop ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
