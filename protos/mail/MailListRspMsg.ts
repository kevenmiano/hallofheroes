// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: mail/MailListRspMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.mail";

export interface MailListRspMsg {
  mailInfos: number[];
  curPage: number;
  maxPage: number;
}

function createBaseMailListRspMsg(): MailListRspMsg {
  return { mailInfos: [], curPage: 0, maxPage: 0 };
}

export const MailListRspMsg: MessageFns<MailListRspMsg> = {
  encode(message: MailListRspMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.mailInfos) {
      writer.int32(v);
    }
    writer.join();
    if (message.curPage !== 0) {
      writer.uint32(16).int32(message.curPage);
    }
    if (message.maxPage !== 0) {
      writer.uint32(24).int32(message.maxPage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MailListRspMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMailListRspMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.mailInfos.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.mailInfos.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.curPage = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxPage = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MailListRspMsg {
    return {
      mailInfos: globalThis.Array.isArray(object?.mailInfos)
        ? object.mailInfos.map((e: any) => globalThis.Number(e))
        : [],
      curPage: isSet(object.curPage) ? globalThis.Number(object.curPage) : 0,
      maxPage: isSet(object.maxPage) ? globalThis.Number(object.maxPage) : 0,
    };
  },

  toJSON(message: MailListRspMsg): unknown {
    const obj: any = {};
    if (message.mailInfos?.length) {
      obj.mailInfos = message.mailInfos.map((e) => Math.round(e));
    }
    if (message.curPage !== 0) {
      obj.curPage = Math.round(message.curPage);
    }
    if (message.maxPage !== 0) {
      obj.maxPage = Math.round(message.maxPage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MailListRspMsg>, I>>(base?: I): MailListRspMsg {
    return MailListRspMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MailListRspMsg>, I>>(object: I): MailListRspMsg {
    const message = createBaseMailListRspMsg();
    message.mailInfos = object.mailInfos?.map((e) => e) || [];
    message.curPage = object.curPage ?? 0;
    message.maxPage = object.maxPage ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
