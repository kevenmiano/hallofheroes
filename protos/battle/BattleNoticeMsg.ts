// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/BattleNoticeMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface BattleNoticeMsg {
  battleId: string;
  countDown: number;
  damageImprove: number;
  buffType: number;
}

function createBaseBattleNoticeMsg(): BattleNoticeMsg {
  return { battleId: "", countDown: 0, damageImprove: 0, buffType: 0 };
}

export const BattleNoticeMsg: MessageFns<BattleNoticeMsg> = {
  encode(message: BattleNoticeMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.battleId !== "") {
      writer.uint32(10).string(message.battleId);
    }
    if (message.countDown !== 0) {
      writer.uint32(16).int32(message.countDown);
    }
    if (message.damageImprove !== 0) {
      writer.uint32(24).int32(message.damageImprove);
    }
    if (message.buffType !== 0) {
      writer.uint32(32).int32(message.buffType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BattleNoticeMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBattleNoticeMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.battleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.countDown = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.damageImprove = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.buffType = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BattleNoticeMsg {
    return {
      battleId: isSet(object.battleId) ? globalThis.String(object.battleId) : "",
      countDown: isSet(object.countDown) ? globalThis.Number(object.countDown) : 0,
      damageImprove: isSet(object.damageImprove) ? globalThis.Number(object.damageImprove) : 0,
      buffType: isSet(object.buffType) ? globalThis.Number(object.buffType) : 0,
    };
  },

  toJSON(message: BattleNoticeMsg): unknown {
    const obj: any = {};
    if (message.battleId !== "") {
      obj.battleId = message.battleId;
    }
    if (message.countDown !== 0) {
      obj.countDown = Math.round(message.countDown);
    }
    if (message.damageImprove !== 0) {
      obj.damageImprove = Math.round(message.damageImprove);
    }
    if (message.buffType !== 0) {
      obj.buffType = Math.round(message.buffType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BattleNoticeMsg>, I>>(base?: I): BattleNoticeMsg {
    return BattleNoticeMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BattleNoticeMsg>, I>>(object: I): BattleNoticeMsg {
    const message = createBaseBattleNoticeMsg();
    message.battleId = object.battleId ?? "";
    message.countDown = object.countDown ?? 0;
    message.damageImprove = object.damageImprove ?? 0;
    message.buffType = object.buffType ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
