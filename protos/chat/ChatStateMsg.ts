// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: chat/ChatStateMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.chat";

export interface ChatStateMsg {
  userId: number;
  chatState: number;
  result: number;
  replayStr: string;
}

function createBaseChatStateMsg(): ChatStateMsg {
  return { userId: 0, chatState: 0, result: 0, replayStr: "" };
}

export const ChatStateMsg: MessageFns<ChatStateMsg> = {
  encode(message: ChatStateMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).int32(message.userId);
    }
    if (message.chatState !== 0) {
      writer.uint32(16).int32(message.chatState);
    }
    if (message.result !== 0) {
      writer.uint32(24).int32(message.result);
    }
    if (message.replayStr !== "") {
      writer.uint32(34).string(message.replayStr);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChatStateMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChatStateMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.chatState = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.result = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.replayStr = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChatStateMsg {
    return {
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      chatState: isSet(object.chatState) ? globalThis.Number(object.chatState) : 0,
      result: isSet(object.result) ? globalThis.Number(object.result) : 0,
      replayStr: isSet(object.replayStr) ? globalThis.String(object.replayStr) : "",
    };
  },

  toJSON(message: ChatStateMsg): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.chatState !== 0) {
      obj.chatState = Math.round(message.chatState);
    }
    if (message.result !== 0) {
      obj.result = Math.round(message.result);
    }
    if (message.replayStr !== "") {
      obj.replayStr = message.replayStr;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChatStateMsg>, I>>(base?: I): ChatStateMsg {
    return ChatStateMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChatStateMsg>, I>>(object: I): ChatStateMsg {
    const message = createBaseChatStateMsg();
    message.userId = object.userId ?? 0;
    message.chatState = object.chatState ?? 0;
    message.result = object.result ?? 0;
    message.replayStr = object.replayStr ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
