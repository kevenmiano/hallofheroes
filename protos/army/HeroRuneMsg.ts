// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: army/HeroRuneMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.army";

export interface HeroRuneMsg {
  userId: number;
  runeinfo: number[];
  runeKey: string;
}

function createBaseHeroRuneMsg(): HeroRuneMsg {
  return { userId: 0, runeinfo: [], runeKey: "" };
}

export const HeroRuneMsg: MessageFns<HeroRuneMsg> = {
  encode(message: HeroRuneMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).int32(message.userId);
    }
    writer.uint32(18).fork();
    for (const v of message.runeinfo) {
      writer.int32(v);
    }
    writer.join();
    if (message.runeKey !== "") {
      writer.uint32(26).string(message.runeKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeroRuneMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeroRuneMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.runeinfo.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.runeinfo.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.runeKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HeroRuneMsg {
    return {
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      runeinfo: globalThis.Array.isArray(object?.runeinfo) ? object.runeinfo.map((e: any) => globalThis.Number(e)) : [],
      runeKey: isSet(object.runeKey) ? globalThis.String(object.runeKey) : "",
    };
  },

  toJSON(message: HeroRuneMsg): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.runeinfo?.length) {
      obj.runeinfo = message.runeinfo.map((e) => Math.round(e));
    }
    if (message.runeKey !== "") {
      obj.runeKey = message.runeKey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HeroRuneMsg>, I>>(base?: I): HeroRuneMsg {
    return HeroRuneMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HeroRuneMsg>, I>>(object: I): HeroRuneMsg {
    const message = createBaseHeroRuneMsg();
    message.userId = object.userId ?? 0;
    message.runeinfo = object.runeinfo?.map((e) => e) || [];
    message.runeKey = object.runeKey ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
