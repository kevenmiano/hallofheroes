// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: army/PhysicListUpdateMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.army";

export interface PhysicListUpdateMsg {
  reqGrid: string;
  wildLand: number[];
  baseCastle: number[];
}

function createBasePhysicListUpdateMsg(): PhysicListUpdateMsg {
  return { reqGrid: "", wildLand: [], baseCastle: [] };
}

export const PhysicListUpdateMsg: MessageFns<PhysicListUpdateMsg> = {
  encode(message: PhysicListUpdateMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reqGrid !== "") {
      writer.uint32(10).string(message.reqGrid);
    }
    writer.uint32(18).fork();
    for (const v of message.wildLand) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.baseCastle) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PhysicListUpdateMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePhysicListUpdateMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reqGrid = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.wildLand.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.wildLand.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.baseCastle.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.baseCastle.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PhysicListUpdateMsg {
    return {
      reqGrid: isSet(object.reqGrid) ? globalThis.String(object.reqGrid) : "",
      wildLand: globalThis.Array.isArray(object?.wildLand) ? object.wildLand.map((e: any) => globalThis.Number(e)) : [],
      baseCastle: globalThis.Array.isArray(object?.baseCastle)
        ? object.baseCastle.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: PhysicListUpdateMsg): unknown {
    const obj: any = {};
    if (message.reqGrid !== "") {
      obj.reqGrid = message.reqGrid;
    }
    if (message.wildLand?.length) {
      obj.wildLand = message.wildLand.map((e) => Math.round(e));
    }
    if (message.baseCastle?.length) {
      obj.baseCastle = message.baseCastle.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PhysicListUpdateMsg>, I>>(base?: I): PhysicListUpdateMsg {
    return PhysicListUpdateMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PhysicListUpdateMsg>, I>>(object: I): PhysicListUpdateMsg {
    const message = createBasePhysicListUpdateMsg();
    message.reqGrid = object.reqGrid ?? "";
    message.wildLand = object.wildLand?.map((e) => e) || [];
    message.baseCastle = object.baseCastle?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
