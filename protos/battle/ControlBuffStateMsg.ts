// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/ControlBuffStateMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface ControlBuffStateMsg {
  controled: boolean;
  livingId: number;
  side: number;
  pos: number;
  controllerLivingId: number;
}

function createBaseControlBuffStateMsg(): ControlBuffStateMsg {
  return { controled: false, livingId: 0, side: 0, pos: 0, controllerLivingId: 0 };
}

export const ControlBuffStateMsg: MessageFns<ControlBuffStateMsg> = {
  encode(message: ControlBuffStateMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.controled !== false) {
      writer.uint32(8).bool(message.controled);
    }
    if (message.livingId !== 0) {
      writer.uint32(16).int32(message.livingId);
    }
    if (message.side !== 0) {
      writer.uint32(24).int32(message.side);
    }
    if (message.pos !== 0) {
      writer.uint32(32).int32(message.pos);
    }
    if (message.controllerLivingId !== 0) {
      writer.uint32(40).int32(message.controllerLivingId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ControlBuffStateMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseControlBuffStateMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.controled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.livingId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.side = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.pos = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.controllerLivingId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ControlBuffStateMsg {
    return {
      controled: isSet(object.controled) ? globalThis.Boolean(object.controled) : false,
      livingId: isSet(object.livingId) ? globalThis.Number(object.livingId) : 0,
      side: isSet(object.side) ? globalThis.Number(object.side) : 0,
      pos: isSet(object.pos) ? globalThis.Number(object.pos) : 0,
      controllerLivingId: isSet(object.controllerLivingId) ? globalThis.Number(object.controllerLivingId) : 0,
    };
  },

  toJSON(message: ControlBuffStateMsg): unknown {
    const obj: any = {};
    if (message.controled !== false) {
      obj.controled = message.controled;
    }
    if (message.livingId !== 0) {
      obj.livingId = Math.round(message.livingId);
    }
    if (message.side !== 0) {
      obj.side = Math.round(message.side);
    }
    if (message.pos !== 0) {
      obj.pos = Math.round(message.pos);
    }
    if (message.controllerLivingId !== 0) {
      obj.controllerLivingId = Math.round(message.controllerLivingId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ControlBuffStateMsg>, I>>(base?: I): ControlBuffStateMsg {
    return ControlBuffStateMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ControlBuffStateMsg>, I>>(object: I): ControlBuffStateMsg {
    const message = createBaseControlBuffStateMsg();
    message.controled = object.controled ?? false;
    message.livingId = object.livingId ?? 0;
    message.side = object.side ?? 0;
    message.pos = object.pos ?? 0;
    message.controllerLivingId = object.controllerLivingId ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
