// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: campaign/MultiCampaignHpSyncMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.campaign";

export interface MultiCampaignHpSyncMsg {
  proxyId: string;
  mapId: number;
  nodeId: number;
  nodeHp: number;
  nodeTotalHp: number;
  playerHp: number[];
}

function createBaseMultiCampaignHpSyncMsg(): MultiCampaignHpSyncMsg {
  return { proxyId: "", mapId: 0, nodeId: 0, nodeHp: 0, nodeTotalHp: 0, playerHp: [] };
}

export const MultiCampaignHpSyncMsg: MessageFns<MultiCampaignHpSyncMsg> = {
  encode(message: MultiCampaignHpSyncMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.proxyId !== "") {
      writer.uint32(10).string(message.proxyId);
    }
    if (message.mapId !== 0) {
      writer.uint32(16).int32(message.mapId);
    }
    if (message.nodeId !== 0) {
      writer.uint32(24).int32(message.nodeId);
    }
    if (message.nodeHp !== 0) {
      writer.uint32(32).int32(message.nodeHp);
    }
    if (message.nodeTotalHp !== 0) {
      writer.uint32(40).int32(message.nodeTotalHp);
    }
    writer.uint32(50).fork();
    for (const v of message.playerHp) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MultiCampaignHpSyncMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMultiCampaignHpSyncMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.proxyId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.mapId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.nodeId = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.nodeHp = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.nodeTotalHp = reader.int32();
          continue;
        }
        case 6: {
          if (tag === 48) {
            message.playerHp.push(reader.int32());

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.playerHp.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MultiCampaignHpSyncMsg {
    return {
      proxyId: isSet(object.proxyId) ? globalThis.String(object.proxyId) : "",
      mapId: isSet(object.mapId) ? globalThis.Number(object.mapId) : 0,
      nodeId: isSet(object.nodeId) ? globalThis.Number(object.nodeId) : 0,
      nodeHp: isSet(object.nodeHp) ? globalThis.Number(object.nodeHp) : 0,
      nodeTotalHp: isSet(object.nodeTotalHp) ? globalThis.Number(object.nodeTotalHp) : 0,
      playerHp: globalThis.Array.isArray(object?.playerHp) ? object.playerHp.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: MultiCampaignHpSyncMsg): unknown {
    const obj: any = {};
    if (message.proxyId !== "") {
      obj.proxyId = message.proxyId;
    }
    if (message.mapId !== 0) {
      obj.mapId = Math.round(message.mapId);
    }
    if (message.nodeId !== 0) {
      obj.nodeId = Math.round(message.nodeId);
    }
    if (message.nodeHp !== 0) {
      obj.nodeHp = Math.round(message.nodeHp);
    }
    if (message.nodeTotalHp !== 0) {
      obj.nodeTotalHp = Math.round(message.nodeTotalHp);
    }
    if (message.playerHp?.length) {
      obj.playerHp = message.playerHp.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MultiCampaignHpSyncMsg>, I>>(base?: I): MultiCampaignHpSyncMsg {
    return MultiCampaignHpSyncMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MultiCampaignHpSyncMsg>, I>>(object: I): MultiCampaignHpSyncMsg {
    const message = createBaseMultiCampaignHpSyncMsg();
    message.proxyId = object.proxyId ?? "";
    message.mapId = object.mapId ?? 0;
    message.nodeId = object.nodeId ?? 0;
    message.nodeHp = object.nodeHp ?? 0;
    message.nodeTotalHp = object.nodeTotalHp ?? 0;
    message.playerHp = object.playerHp?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
