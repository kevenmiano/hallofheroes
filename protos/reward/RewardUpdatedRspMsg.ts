// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: reward/RewardUpdatedRspMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.reward";

export interface RewardUpdatedRspMsg {
  reward: number[];
  statesLength: number;
  states: string;
}

function createBaseRewardUpdatedRspMsg(): RewardUpdatedRspMsg {
  return { reward: [], statesLength: 0, states: "" };
}

export const RewardUpdatedRspMsg: MessageFns<RewardUpdatedRspMsg> = {
  encode(message: RewardUpdatedRspMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.reward) {
      writer.int32(v);
    }
    writer.join();
    if (message.statesLength !== 0) {
      writer.uint32(16).int32(message.statesLength);
    }
    if (message.states !== "") {
      writer.uint32(26).string(message.states);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RewardUpdatedRspMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRewardUpdatedRspMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.reward.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.reward.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.statesLength = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.states = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RewardUpdatedRspMsg {
    return {
      reward: globalThis.Array.isArray(object?.reward) ? object.reward.map((e: any) => globalThis.Number(e)) : [],
      statesLength: isSet(object.statesLength) ? globalThis.Number(object.statesLength) : 0,
      states: isSet(object.states) ? globalThis.String(object.states) : "",
    };
  },

  toJSON(message: RewardUpdatedRspMsg): unknown {
    const obj: any = {};
    if (message.reward?.length) {
      obj.reward = message.reward.map((e) => Math.round(e));
    }
    if (message.statesLength !== 0) {
      obj.statesLength = Math.round(message.statesLength);
    }
    if (message.states !== "") {
      obj.states = message.states;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RewardUpdatedRspMsg>, I>>(base?: I): RewardUpdatedRspMsg {
    return RewardUpdatedRspMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RewardUpdatedRspMsg>, I>>(object: I): RewardUpdatedRspMsg {
    const message = createBaseRewardUpdatedRspMsg();
    message.reward = object.reward?.map((e) => e) || [];
    message.statesLength = object.statesLength ?? 0;
    message.states = object.states ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
