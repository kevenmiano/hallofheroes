// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: gameplot/PlotMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.gameplot";

export interface PlotMsg {
  loop: boolean;
  sceneType: number;
  info: number[];
}

function createBasePlotMsg(): PlotMsg {
  return { loop: false, sceneType: 0, info: [] };
}

export const PlotMsg: MessageFns<PlotMsg> = {
  encode(message: PlotMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.loop !== false) {
      writer.uint32(8).bool(message.loop);
    }
    if (message.sceneType !== 0) {
      writer.uint32(16).int32(message.sceneType);
    }
    writer.uint32(26).fork();
    for (const v of message.info) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlotMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlotMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.loop = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sceneType = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.info.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.info.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlotMsg {
    return {
      loop: isSet(object.loop) ? globalThis.Boolean(object.loop) : false,
      sceneType: isSet(object.sceneType) ? globalThis.Number(object.sceneType) : 0,
      info: globalThis.Array.isArray(object?.info) ? object.info.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: PlotMsg): unknown {
    const obj: any = {};
    if (message.loop !== false) {
      obj.loop = message.loop;
    }
    if (message.sceneType !== 0) {
      obj.sceneType = Math.round(message.sceneType);
    }
    if (message.info?.length) {
      obj.info = message.info.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlotMsg>, I>>(base?: I): PlotMsg {
    return PlotMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlotMsg>, I>>(object: I): PlotMsg {
    const message = createBasePlotMsg();
    message.loop = object.loop ?? false;
    message.sceneType = object.sceneType ?? 0;
    message.info = object.info?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
