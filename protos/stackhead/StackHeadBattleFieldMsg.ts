// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: stackhead/StackHeadBattleFieldMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.stackhead";

export interface StackHeadBattleFieldMsg {
  guildInfoList: number[];
  attackList: number[];
  myGuildPos: number;
}

function createBaseStackHeadBattleFieldMsg(): StackHeadBattleFieldMsg {
  return { guildInfoList: [], attackList: [], myGuildPos: 0 };
}

export const StackHeadBattleFieldMsg: MessageFns<StackHeadBattleFieldMsg> = {
  encode(message: StackHeadBattleFieldMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.guildInfoList) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.attackList) {
      writer.int32(v);
    }
    writer.join();
    if (message.myGuildPos !== 0) {
      writer.uint32(24).int32(message.myGuildPos);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StackHeadBattleFieldMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStackHeadBattleFieldMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.guildInfoList.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.guildInfoList.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.attackList.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.attackList.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.myGuildPos = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StackHeadBattleFieldMsg {
    return {
      guildInfoList: globalThis.Array.isArray(object?.guildInfoList)
        ? object.guildInfoList.map((e: any) => globalThis.Number(e))
        : [],
      attackList: globalThis.Array.isArray(object?.attackList)
        ? object.attackList.map((e: any) => globalThis.Number(e))
        : [],
      myGuildPos: isSet(object.myGuildPos) ? globalThis.Number(object.myGuildPos) : 0,
    };
  },

  toJSON(message: StackHeadBattleFieldMsg): unknown {
    const obj: any = {};
    if (message.guildInfoList?.length) {
      obj.guildInfoList = message.guildInfoList.map((e) => Math.round(e));
    }
    if (message.attackList?.length) {
      obj.attackList = message.attackList.map((e) => Math.round(e));
    }
    if (message.myGuildPos !== 0) {
      obj.myGuildPos = Math.round(message.myGuildPos);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StackHeadBattleFieldMsg>, I>>(base?: I): StackHeadBattleFieldMsg {
    return StackHeadBattleFieldMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StackHeadBattleFieldMsg>, I>>(object: I): StackHeadBattleFieldMsg {
    const message = createBaseStackHeadBattleFieldMsg();
    message.guildInfoList = object.guildInfoList?.map((e) => e) || [];
    message.attackList = object.attackList?.map((e) => e) || [];
    message.myGuildPos = object.myGuildPos ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
