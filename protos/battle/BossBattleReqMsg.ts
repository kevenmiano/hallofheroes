// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/BossBattleReqMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface BossBattleReqMsg {
  serverName: string;
  heroJob: number;
  sex: number;
  armyId: number;
  templatedId1: number;
  templatedId2: number;
  bossId: number;
}

function createBaseBossBattleReqMsg(): BossBattleReqMsg {
  return { serverName: "", heroJob: 0, sex: 0, armyId: 0, templatedId1: 0, templatedId2: 0, bossId: 0 };
}

export const BossBattleReqMsg: MessageFns<BossBattleReqMsg> = {
  encode(message: BossBattleReqMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverName !== "") {
      writer.uint32(10).string(message.serverName);
    }
    if (message.heroJob !== 0) {
      writer.uint32(16).int32(message.heroJob);
    }
    if (message.sex !== 0) {
      writer.uint32(24).int32(message.sex);
    }
    if (message.armyId !== 0) {
      writer.uint32(32).int32(message.armyId);
    }
    if (message.templatedId1 !== 0) {
      writer.uint32(40).int32(message.templatedId1);
    }
    if (message.templatedId2 !== 0) {
      writer.uint32(48).int32(message.templatedId2);
    }
    if (message.bossId !== 0) {
      writer.uint32(56).int32(message.bossId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BossBattleReqMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBossBattleReqMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.heroJob = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sex = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.armyId = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.templatedId1 = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.templatedId2 = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.bossId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BossBattleReqMsg {
    return {
      serverName: isSet(object.serverName) ? globalThis.String(object.serverName) : "",
      heroJob: isSet(object.heroJob) ? globalThis.Number(object.heroJob) : 0,
      sex: isSet(object.sex) ? globalThis.Number(object.sex) : 0,
      armyId: isSet(object.armyId) ? globalThis.Number(object.armyId) : 0,
      templatedId1: isSet(object.templatedId1) ? globalThis.Number(object.templatedId1) : 0,
      templatedId2: isSet(object.templatedId2) ? globalThis.Number(object.templatedId2) : 0,
      bossId: isSet(object.bossId) ? globalThis.Number(object.bossId) : 0,
    };
  },

  toJSON(message: BossBattleReqMsg): unknown {
    const obj: any = {};
    if (message.serverName !== "") {
      obj.serverName = message.serverName;
    }
    if (message.heroJob !== 0) {
      obj.heroJob = Math.round(message.heroJob);
    }
    if (message.sex !== 0) {
      obj.sex = Math.round(message.sex);
    }
    if (message.armyId !== 0) {
      obj.armyId = Math.round(message.armyId);
    }
    if (message.templatedId1 !== 0) {
      obj.templatedId1 = Math.round(message.templatedId1);
    }
    if (message.templatedId2 !== 0) {
      obj.templatedId2 = Math.round(message.templatedId2);
    }
    if (message.bossId !== 0) {
      obj.bossId = Math.round(message.bossId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BossBattleReqMsg>, I>>(base?: I): BossBattleReqMsg {
    return BossBattleReqMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BossBattleReqMsg>, I>>(object: I): BossBattleReqMsg {
    const message = createBaseBossBattleReqMsg();
    message.serverName = object.serverName ?? "";
    message.heroJob = object.heroJob ?? 0;
    message.sex = object.sex ?? 0;
    message.armyId = object.armyId ?? 0;
    message.templatedId1 = object.templatedId1 ?? 0;
    message.templatedId2 = object.templatedId2 ?? 0;
    message.bossId = object.bossId ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
