// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: active/DiscountMallItemInfo.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.active";

export interface DiscountMallItemInfo {
  index: number;
  itemId: number;
  price: number;
  limit: number;
  count: number;
  buycount: number;
}

function createBaseDiscountMallItemInfo(): DiscountMallItemInfo {
  return { index: 0, itemId: 0, price: 0, limit: 0, count: 0, buycount: 0 };
}

export const DiscountMallItemInfo: MessageFns<DiscountMallItemInfo> = {
  encode(message: DiscountMallItemInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).int32(message.index);
    }
    if (message.itemId !== 0) {
      writer.uint32(16).int32(message.itemId);
    }
    if (message.price !== 0) {
      writer.uint32(24).int32(message.price);
    }
    if (message.limit !== 0) {
      writer.uint32(32).int32(message.limit);
    }
    if (message.count !== 0) {
      writer.uint32(40).int32(message.count);
    }
    if (message.buycount !== 0) {
      writer.uint32(48).int32(message.buycount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscountMallItemInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscountMallItemInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.itemId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.price = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.buycount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscountMallItemInfo {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      itemId: isSet(object.itemId) ? globalThis.Number(object.itemId) : 0,
      price: isSet(object.price) ? globalThis.Number(object.price) : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      buycount: isSet(object.buycount) ? globalThis.Number(object.buycount) : 0,
    };
  },

  toJSON(message: DiscountMallItemInfo): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.itemId !== 0) {
      obj.itemId = Math.round(message.itemId);
    }
    if (message.price !== 0) {
      obj.price = Math.round(message.price);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.buycount !== 0) {
      obj.buycount = Math.round(message.buycount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscountMallItemInfo>, I>>(base?: I): DiscountMallItemInfo {
    return DiscountMallItemInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscountMallItemInfo>, I>>(object: I): DiscountMallItemInfo {
    const message = createBaseDiscountMallItemInfo();
    message.index = object.index ?? 0;
    message.itemId = object.itemId ?? 0;
    message.price = object.price ?? 0;
    message.limit = object.limit ?? 0;
    message.count = object.count ?? 0;
    message.buycount = object.buycount ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
