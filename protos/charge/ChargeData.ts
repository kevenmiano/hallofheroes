// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: charge/ChargeData.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.charge";

export interface ChargeData {
  totalMoney: string;
}

function createBaseChargeData(): ChargeData {
  return { totalMoney: "" };
}

export const ChargeData: MessageFns<ChargeData> = {
  encode(message: ChargeData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalMoney !== "") {
      writer.uint32(10).string(message.totalMoney);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChargeData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChargeData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.totalMoney = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChargeData {
    return { totalMoney: isSet(object.totalMoney) ? globalThis.String(object.totalMoney) : "" };
  },

  toJSON(message: ChargeData): unknown {
    const obj: any = {};
    if (message.totalMoney !== "") {
      obj.totalMoney = message.totalMoney;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChargeData>, I>>(base?: I): ChargeData {
    return ChargeData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChargeData>, I>>(object: I): ChargeData {
    const message = createBaseChargeData();
    message.totalMoney = object.totalMoney ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
