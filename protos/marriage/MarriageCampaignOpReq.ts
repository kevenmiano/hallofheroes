// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: marriage/MarriageCampaignOpReq.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.marriage";

export interface MarriageCampaignOpReq {
  op: number;
  fireworkId: number;
  fireworkCount: number;
}

function createBaseMarriageCampaignOpReq(): MarriageCampaignOpReq {
  return { op: 0, fireworkId: 0, fireworkCount: 0 };
}

export const MarriageCampaignOpReq: MessageFns<MarriageCampaignOpReq> = {
  encode(message: MarriageCampaignOpReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.op !== 0) {
      writer.uint32(8).int32(message.op);
    }
    if (message.fireworkId !== 0) {
      writer.uint32(16).int32(message.fireworkId);
    }
    if (message.fireworkCount !== 0) {
      writer.uint32(24).int32(message.fireworkCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarriageCampaignOpReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarriageCampaignOpReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.op = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fireworkId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fireworkCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarriageCampaignOpReq {
    return {
      op: isSet(object.op) ? globalThis.Number(object.op) : 0,
      fireworkId: isSet(object.fireworkId) ? globalThis.Number(object.fireworkId) : 0,
      fireworkCount: isSet(object.fireworkCount) ? globalThis.Number(object.fireworkCount) : 0,
    };
  },

  toJSON(message: MarriageCampaignOpReq): unknown {
    const obj: any = {};
    if (message.op !== 0) {
      obj.op = Math.round(message.op);
    }
    if (message.fireworkId !== 0) {
      obj.fireworkId = Math.round(message.fireworkId);
    }
    if (message.fireworkCount !== 0) {
      obj.fireworkCount = Math.round(message.fireworkCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarriageCampaignOpReq>, I>>(base?: I): MarriageCampaignOpReq {
    return MarriageCampaignOpReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarriageCampaignOpReq>, I>>(object: I): MarriageCampaignOpReq {
    const message = createBaseMarriageCampaignOpReq();
    message.op = object.op ?? 0;
    message.fireworkId = object.fireworkId ?? 0;
    message.fireworkCount = object.fireworkCount ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
