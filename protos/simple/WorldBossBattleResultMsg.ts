// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: simple/WorldBossBattleResultMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.simple";

export interface WorldBossBattleResultMsg {
  campaignId: number;
  userId: number;
  otherUserId: number[];
}

function createBaseWorldBossBattleResultMsg(): WorldBossBattleResultMsg {
  return { campaignId: 0, userId: 0, otherUserId: [] };
}

export const WorldBossBattleResultMsg: MessageFns<WorldBossBattleResultMsg> = {
  encode(message: WorldBossBattleResultMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.campaignId !== 0) {
      writer.uint32(8).int32(message.campaignId);
    }
    if (message.userId !== 0) {
      writer.uint32(16).int32(message.userId);
    }
    writer.uint32(26).fork();
    for (const v of message.otherUserId) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorldBossBattleResultMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorldBossBattleResultMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.campaignId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.userId = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.otherUserId.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.otherUserId.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorldBossBattleResultMsg {
    return {
      campaignId: isSet(object.campaignId) ? globalThis.Number(object.campaignId) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      otherUserId: globalThis.Array.isArray(object?.otherUserId)
        ? object.otherUserId.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: WorldBossBattleResultMsg): unknown {
    const obj: any = {};
    if (message.campaignId !== 0) {
      obj.campaignId = Math.round(message.campaignId);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.otherUserId?.length) {
      obj.otherUserId = message.otherUserId.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorldBossBattleResultMsg>, I>>(base?: I): WorldBossBattleResultMsg {
    return WorldBossBattleResultMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorldBossBattleResultMsg>, I>>(object: I): WorldBossBattleResultMsg {
    const message = createBaseWorldBossBattleResultMsg();
    message.campaignId = object.campaignId ?? 0;
    message.userId = object.userId ?? 0;
    message.otherUserId = object.otherUserId?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
