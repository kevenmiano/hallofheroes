// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: active/FirstChargeAwardInfo.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.active";

export interface FirstChargeAwardInfo {
  days: number;
  state: number;
  type: number;
}

function createBaseFirstChargeAwardInfo(): FirstChargeAwardInfo {
  return { days: 0, state: 0, type: 0 };
}

export const FirstChargeAwardInfo: MessageFns<FirstChargeAwardInfo> = {
  encode(message: FirstChargeAwardInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.days !== 0) {
      writer.uint32(8).int32(message.days);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FirstChargeAwardInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFirstChargeAwardInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.days = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.type = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FirstChargeAwardInfo {
    return {
      days: isSet(object.days) ? globalThis.Number(object.days) : 0,
      state: isSet(object.state) ? globalThis.Number(object.state) : 0,
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
    };
  },

  toJSON(message: FirstChargeAwardInfo): unknown {
    const obj: any = {};
    if (message.days !== 0) {
      obj.days = Math.round(message.days);
    }
    if (message.state !== 0) {
      obj.state = Math.round(message.state);
    }
    if (message.type !== 0) {
      obj.type = Math.round(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FirstChargeAwardInfo>, I>>(base?: I): FirstChargeAwardInfo {
    return FirstChargeAwardInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FirstChargeAwardInfo>, I>>(object: I): FirstChargeAwardInfo {
    const message = createBaseFirstChargeAwardInfo();
    message.days = object.days ?? 0;
    message.state = object.state ?? 0;
    message.type = object.type ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
