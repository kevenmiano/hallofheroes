// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: cccactive/SkyTreasureExchangeActivePB.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.cccactive";

export interface SkyTreasureExchangeActivePB {
  activeId: string;
  items: number[];
}

function createBaseSkyTreasureExchangeActivePB(): SkyTreasureExchangeActivePB {
  return { activeId: "", items: [] };
}

export const SkyTreasureExchangeActivePB: MessageFns<SkyTreasureExchangeActivePB> = {
  encode(message: SkyTreasureExchangeActivePB, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.activeId !== "") {
      writer.uint32(10).string(message.activeId);
    }
    writer.uint32(18).fork();
    for (const v of message.items) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkyTreasureExchangeActivePB {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkyTreasureExchangeActivePB();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.activeId = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.items.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.items.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkyTreasureExchangeActivePB {
    return {
      activeId: isSet(object.activeId) ? globalThis.String(object.activeId) : "",
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: SkyTreasureExchangeActivePB): unknown {
    const obj: any = {};
    if (message.activeId !== "") {
      obj.activeId = message.activeId;
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SkyTreasureExchangeActivePB>, I>>(base?: I): SkyTreasureExchangeActivePB {
    return SkyTreasureExchangeActivePB.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SkyTreasureExchangeActivePB>, I>>(object: I): SkyTreasureExchangeActivePB {
    const message = createBaseSkyTreasureExchangeActivePB();
    message.activeId = object.activeId ?? "";
    message.items = object.items?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
