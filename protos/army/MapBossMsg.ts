// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: army/MapBossMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.army";

export interface MapBossMsg {
  count1: number;
  count2: number;
  count3: number;
  bosslist: number[];
  leftFogTime: number;
}

function createBaseMapBossMsg(): MapBossMsg {
  return { count1: 0, count2: 0, count3: 0, bosslist: [], leftFogTime: 0 };
}

export const MapBossMsg: MessageFns<MapBossMsg> = {
  encode(message: MapBossMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.count1 !== 0) {
      writer.uint32(8).int32(message.count1);
    }
    if (message.count2 !== 0) {
      writer.uint32(16).int32(message.count2);
    }
    if (message.count3 !== 0) {
      writer.uint32(24).int32(message.count3);
    }
    writer.uint32(34).fork();
    for (const v of message.bosslist) {
      writer.int32(v);
    }
    writer.join();
    if (message.leftFogTime !== 0) {
      writer.uint32(40).int32(message.leftFogTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MapBossMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMapBossMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.count1 = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count2 = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.count3 = reader.int32();
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.bosslist.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.bosslist.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.leftFogTime = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MapBossMsg {
    return {
      count1: isSet(object.count1) ? globalThis.Number(object.count1) : 0,
      count2: isSet(object.count2) ? globalThis.Number(object.count2) : 0,
      count3: isSet(object.count3) ? globalThis.Number(object.count3) : 0,
      bosslist: globalThis.Array.isArray(object?.bosslist) ? object.bosslist.map((e: any) => globalThis.Number(e)) : [],
      leftFogTime: isSet(object.leftFogTime) ? globalThis.Number(object.leftFogTime) : 0,
    };
  },

  toJSON(message: MapBossMsg): unknown {
    const obj: any = {};
    if (message.count1 !== 0) {
      obj.count1 = Math.round(message.count1);
    }
    if (message.count2 !== 0) {
      obj.count2 = Math.round(message.count2);
    }
    if (message.count3 !== 0) {
      obj.count3 = Math.round(message.count3);
    }
    if (message.bosslist?.length) {
      obj.bosslist = message.bosslist.map((e) => Math.round(e));
    }
    if (message.leftFogTime !== 0) {
      obj.leftFogTime = Math.round(message.leftFogTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MapBossMsg>, I>>(base?: I): MapBossMsg {
    return MapBossMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MapBossMsg>, I>>(object: I): MapBossMsg {
    const message = createBaseMapBossMsg();
    message.count1 = object.count1 ?? 0;
    message.count2 = object.count2 ?? 0;
    message.count3 = object.count3 ?? 0;
    message.bosslist = object.bosslist?.map((e) => e) || [];
    message.leftFogTime = object.leftFogTime ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
