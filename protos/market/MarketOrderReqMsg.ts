// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: market/MarketOrderReqMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.market";

export interface MarketOrderReqMsg {
  op: number;
  orderId: string;
  type: number;
  templateId: number;
  count: number;
  point: number;
}

function createBaseMarketOrderReqMsg(): MarketOrderReqMsg {
  return { op: 0, orderId: "", type: 0, templateId: 0, count: 0, point: 0 };
}

export const MarketOrderReqMsg: MessageFns<MarketOrderReqMsg> = {
  encode(message: MarketOrderReqMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.op !== 0) {
      writer.uint32(8).int32(message.op);
    }
    if (message.orderId !== "") {
      writer.uint32(18).string(message.orderId);
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    if (message.templateId !== 0) {
      writer.uint32(32).int32(message.templateId);
    }
    if (message.count !== 0) {
      writer.uint32(40).int32(message.count);
    }
    if (message.point !== 0) {
      writer.uint32(48).int32(message.point);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketOrderReqMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketOrderReqMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.op = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.type = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.templateId = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.point = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketOrderReqMsg {
    return {
      op: isSet(object.op) ? globalThis.Number(object.op) : 0,
      orderId: isSet(object.orderId) ? globalThis.String(object.orderId) : "",
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
      templateId: isSet(object.templateId) ? globalThis.Number(object.templateId) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      point: isSet(object.point) ? globalThis.Number(object.point) : 0,
    };
  },

  toJSON(message: MarketOrderReqMsg): unknown {
    const obj: any = {};
    if (message.op !== 0) {
      obj.op = Math.round(message.op);
    }
    if (message.orderId !== "") {
      obj.orderId = message.orderId;
    }
    if (message.type !== 0) {
      obj.type = Math.round(message.type);
    }
    if (message.templateId !== 0) {
      obj.templateId = Math.round(message.templateId);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.point !== 0) {
      obj.point = Math.round(message.point);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarketOrderReqMsg>, I>>(base?: I): MarketOrderReqMsg {
    return MarketOrderReqMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarketOrderReqMsg>, I>>(object: I): MarketOrderReqMsg {
    const message = createBaseMarketOrderReqMsg();
    message.op = object.op ?? 0;
    message.orderId = object.orderId ?? "";
    message.type = object.type ?? 0;
    message.templateId = object.templateId ?? 0;
    message.count = object.count ?? 0;
    message.point = object.point ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
