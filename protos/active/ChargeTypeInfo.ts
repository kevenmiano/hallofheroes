// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: active/ChargeTypeInfo.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.active";

export interface ChargeTypeInfo {
  type: number;
  chargeDayNum: number;
  award: number[];
  award2: number[];
}

function createBaseChargeTypeInfo(): ChargeTypeInfo {
  return { type: 0, chargeDayNum: 0, award: [], award2: [] };
}

export const ChargeTypeInfo: MessageFns<ChargeTypeInfo> = {
  encode(message: ChargeTypeInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.chargeDayNum !== 0) {
      writer.uint32(16).int32(message.chargeDayNum);
    }
    writer.uint32(26).fork();
    for (const v of message.award) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(34).fork();
    for (const v of message.award2) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChargeTypeInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChargeTypeInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.chargeDayNum = reader.int32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.award.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.award.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag === 32) {
            message.award2.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.award2.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChargeTypeInfo {
    return {
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
      chargeDayNum: isSet(object.chargeDayNum) ? globalThis.Number(object.chargeDayNum) : 0,
      award: globalThis.Array.isArray(object?.award) ? object.award.map((e: any) => globalThis.Number(e)) : [],
      award2: globalThis.Array.isArray(object?.award2) ? object.award2.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: ChargeTypeInfo): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = Math.round(message.type);
    }
    if (message.chargeDayNum !== 0) {
      obj.chargeDayNum = Math.round(message.chargeDayNum);
    }
    if (message.award?.length) {
      obj.award = message.award.map((e) => Math.round(e));
    }
    if (message.award2?.length) {
      obj.award2 = message.award2.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChargeTypeInfo>, I>>(base?: I): ChargeTypeInfo {
    return ChargeTypeInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChargeTypeInfo>, I>>(object: I): ChargeTypeInfo {
    const message = createBaseChargeTypeInfo();
    message.type = object.type ?? 0;
    message.chargeDayNum = object.chargeDayNum ?? 0;
    message.award = object.award?.map((e) => e) || [];
    message.award2 = object.award2?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
