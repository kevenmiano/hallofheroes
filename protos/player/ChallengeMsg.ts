// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: player/ChallengeMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.player";

export interface ChallengeMsg {
  tarUserId: number;
  tarArmyId: number;
  payType: number;
  type: number;
}

function createBaseChallengeMsg(): ChallengeMsg {
  return { tarUserId: 0, tarArmyId: 0, payType: 0, type: 0 };
}

export const ChallengeMsg: MessageFns<ChallengeMsg> = {
  encode(message: ChallengeMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tarUserId !== 0) {
      writer.uint32(8).int32(message.tarUserId);
    }
    if (message.tarArmyId !== 0) {
      writer.uint32(16).int32(message.tarArmyId);
    }
    if (message.payType !== 0) {
      writer.uint32(24).int32(message.payType);
    }
    if (message.type !== 0) {
      writer.uint32(32).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChallengeMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChallengeMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.tarUserId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tarArmyId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.payType = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.type = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChallengeMsg {
    return {
      tarUserId: isSet(object.tarUserId) ? globalThis.Number(object.tarUserId) : 0,
      tarArmyId: isSet(object.tarArmyId) ? globalThis.Number(object.tarArmyId) : 0,
      payType: isSet(object.payType) ? globalThis.Number(object.payType) : 0,
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
    };
  },

  toJSON(message: ChallengeMsg): unknown {
    const obj: any = {};
    if (message.tarUserId !== 0) {
      obj.tarUserId = Math.round(message.tarUserId);
    }
    if (message.tarArmyId !== 0) {
      obj.tarArmyId = Math.round(message.tarArmyId);
    }
    if (message.payType !== 0) {
      obj.payType = Math.round(message.payType);
    }
    if (message.type !== 0) {
      obj.type = Math.round(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChallengeMsg>, I>>(base?: I): ChallengeMsg {
    return ChallengeMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChallengeMsg>, I>>(object: I): ChallengeMsg {
    const message = createBaseChallengeMsg();
    message.tarUserId = object.tarUserId ?? 0;
    message.tarArmyId = object.tarArmyId ?? 0;
    message.payType = object.payType ?? 0;
    message.type = object.type ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
