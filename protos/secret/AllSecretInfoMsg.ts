// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: secret/AllSecretInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.secret";

export interface AllSecretInfoMsg {
  allSecretInfoMsg: number[];
}

function createBaseAllSecretInfoMsg(): AllSecretInfoMsg {
  return { allSecretInfoMsg: [] };
}

export const AllSecretInfoMsg: MessageFns<AllSecretInfoMsg> = {
  encode(message: AllSecretInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.allSecretInfoMsg) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AllSecretInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAllSecretInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.allSecretInfoMsg.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.allSecretInfoMsg.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AllSecretInfoMsg {
    return {
      allSecretInfoMsg: globalThis.Array.isArray(object?.allSecretInfoMsg)
        ? object.allSecretInfoMsg.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: AllSecretInfoMsg): unknown {
    const obj: any = {};
    if (message.allSecretInfoMsg?.length) {
      obj.allSecretInfoMsg = message.allSecretInfoMsg.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AllSecretInfoMsg>, I>>(base?: I): AllSecretInfoMsg {
    return AllSecretInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AllSecretInfoMsg>, I>>(object: I): AllSecretInfoMsg {
    const message = createBaseAllSecretInfoMsg();
    message.allSecretInfoMsg = object.allSecretInfoMsg?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
