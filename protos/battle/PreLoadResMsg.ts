// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/PreLoadResMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface PreLoadResMsg {
  armyId: number;
  heroSkill: number[];
  soldierTempId: number[];
  soldierSkill: number[];
}

function createBasePreLoadResMsg(): PreLoadResMsg {
  return { armyId: 0, heroSkill: [], soldierTempId: [], soldierSkill: [] };
}

export const PreLoadResMsg: MessageFns<PreLoadResMsg> = {
  encode(message: PreLoadResMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.armyId !== 0) {
      writer.uint32(8).int32(message.armyId);
    }
    writer.uint32(18).fork();
    for (const v of message.heroSkill) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.soldierTempId) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(34).fork();
    for (const v of message.soldierSkill) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreLoadResMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreLoadResMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.armyId = reader.int32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.heroSkill.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.heroSkill.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.soldierTempId.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.soldierTempId.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag === 32) {
            message.soldierSkill.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.soldierSkill.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PreLoadResMsg {
    return {
      armyId: isSet(object.armyId) ? globalThis.Number(object.armyId) : 0,
      heroSkill: globalThis.Array.isArray(object?.heroSkill)
        ? object.heroSkill.map((e: any) => globalThis.Number(e))
        : [],
      soldierTempId: globalThis.Array.isArray(object?.soldierTempId)
        ? object.soldierTempId.map((e: any) => globalThis.Number(e))
        : [],
      soldierSkill: globalThis.Array.isArray(object?.soldierSkill)
        ? object.soldierSkill.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: PreLoadResMsg): unknown {
    const obj: any = {};
    if (message.armyId !== 0) {
      obj.armyId = Math.round(message.armyId);
    }
    if (message.heroSkill?.length) {
      obj.heroSkill = message.heroSkill.map((e) => Math.round(e));
    }
    if (message.soldierTempId?.length) {
      obj.soldierTempId = message.soldierTempId.map((e) => Math.round(e));
    }
    if (message.soldierSkill?.length) {
      obj.soldierSkill = message.soldierSkill.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PreLoadResMsg>, I>>(base?: I): PreLoadResMsg {
    return PreLoadResMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreLoadResMsg>, I>>(object: I): PreLoadResMsg {
    const message = createBasePreLoadResMsg();
    message.armyId = object.armyId ?? 0;
    message.heroSkill = object.heroSkill?.map((e) => e) || [];
    message.soldierTempId = object.soldierTempId?.map((e) => e) || [];
    message.soldierSkill = object.soldierSkill?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
