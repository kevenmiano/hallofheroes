// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: pet/PetChallengeInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.pet";

export interface PetChallengeInfoMsg {
  playerInfo: string;
  playerPets: number[];
  challengeLog: number[];
}

function createBasePetChallengeInfoMsg(): PetChallengeInfoMsg {
  return { playerInfo: "", playerPets: [], challengeLog: [] };
}

export const PetChallengeInfoMsg: MessageFns<PetChallengeInfoMsg> = {
  encode(message: PetChallengeInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerInfo !== "") {
      writer.uint32(10).string(message.playerInfo);
    }
    writer.uint32(18).fork();
    for (const v of message.playerPets) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.challengeLog) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PetChallengeInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePetChallengeInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playerInfo = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.playerPets.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.playerPets.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.challengeLog.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.challengeLog.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PetChallengeInfoMsg {
    return {
      playerInfo: isSet(object.playerInfo) ? globalThis.String(object.playerInfo) : "",
      playerPets: globalThis.Array.isArray(object?.playerPets)
        ? object.playerPets.map((e: any) => globalThis.Number(e))
        : [],
      challengeLog: globalThis.Array.isArray(object?.challengeLog)
        ? object.challengeLog.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: PetChallengeInfoMsg): unknown {
    const obj: any = {};
    if (message.playerInfo !== "") {
      obj.playerInfo = message.playerInfo;
    }
    if (message.playerPets?.length) {
      obj.playerPets = message.playerPets.map((e) => Math.round(e));
    }
    if (message.challengeLog?.length) {
      obj.challengeLog = message.challengeLog.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PetChallengeInfoMsg>, I>>(base?: I): PetChallengeInfoMsg {
    return PetChallengeInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PetChallengeInfoMsg>, I>>(object: I): PetChallengeInfoMsg {
    const message = createBasePetChallengeInfoMsg();
    message.playerInfo = object.playerInfo ?? "";
    message.playerPets = object.playerPets?.map((e) => e) || [];
    message.challengeLog = object.challengeLog?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
