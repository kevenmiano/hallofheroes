// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/WaitReviveMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface WaitReviveMsg {
  battleId: string;
  livingId: number;
  sp: number;
  hp: number;
}

function createBaseWaitReviveMsg(): WaitReviveMsg {
  return { battleId: "", livingId: 0, sp: 0, hp: 0 };
}

export const WaitReviveMsg: MessageFns<WaitReviveMsg> = {
  encode(message: WaitReviveMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.battleId !== "") {
      writer.uint32(10).string(message.battleId);
    }
    if (message.livingId !== 0) {
      writer.uint32(16).int32(message.livingId);
    }
    if (message.sp !== 0) {
      writer.uint32(24).int32(message.sp);
    }
    if (message.hp !== 0) {
      writer.uint32(32).int32(message.hp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaitReviveMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWaitReviveMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.battleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.livingId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sp = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.hp = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WaitReviveMsg {
    return {
      battleId: isSet(object.battleId) ? globalThis.String(object.battleId) : "",
      livingId: isSet(object.livingId) ? globalThis.Number(object.livingId) : 0,
      sp: isSet(object.sp) ? globalThis.Number(object.sp) : 0,
      hp: isSet(object.hp) ? globalThis.Number(object.hp) : 0,
    };
  },

  toJSON(message: WaitReviveMsg): unknown {
    const obj: any = {};
    if (message.battleId !== "") {
      obj.battleId = message.battleId;
    }
    if (message.livingId !== 0) {
      obj.livingId = Math.round(message.livingId);
    }
    if (message.sp !== 0) {
      obj.sp = Math.round(message.sp);
    }
    if (message.hp !== 0) {
      obj.hp = Math.round(message.hp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WaitReviveMsg>, I>>(base?: I): WaitReviveMsg {
    return WaitReviveMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WaitReviveMsg>, I>>(object: I): WaitReviveMsg {
    const message = createBaseWaitReviveMsg();
    message.battleId = object.battleId ?? "";
    message.livingId = object.livingId ?? 0;
    message.sp = object.sp ?? 0;
    message.hp = object.hp ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
