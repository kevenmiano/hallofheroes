// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: army/HeroSkillInfoMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.army";

export interface HeroSkillInfoMsg {
  skillPoint: number;
  skillScript: string;
  skillSet: number[];
}

function createBaseHeroSkillInfoMsg(): HeroSkillInfoMsg {
  return { skillPoint: 0, skillScript: "", skillSet: [] };
}

export const HeroSkillInfoMsg: MessageFns<HeroSkillInfoMsg> = {
  encode(message: HeroSkillInfoMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.skillPoint !== 0) {
      writer.uint32(8).int32(message.skillPoint);
    }
    if (message.skillScript !== "") {
      writer.uint32(18).string(message.skillScript);
    }
    writer.uint32(26).fork();
    for (const v of message.skillSet) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeroSkillInfoMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeroSkillInfoMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.skillPoint = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.skillScript = reader.string();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.skillSet.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.skillSet.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HeroSkillInfoMsg {
    return {
      skillPoint: isSet(object.skillPoint) ? globalThis.Number(object.skillPoint) : 0,
      skillScript: isSet(object.skillScript) ? globalThis.String(object.skillScript) : "",
      skillSet: globalThis.Array.isArray(object?.skillSet) ? object.skillSet.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: HeroSkillInfoMsg): unknown {
    const obj: any = {};
    if (message.skillPoint !== 0) {
      obj.skillPoint = Math.round(message.skillPoint);
    }
    if (message.skillScript !== "") {
      obj.skillScript = message.skillScript;
    }
    if (message.skillSet?.length) {
      obj.skillSet = message.skillSet.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HeroSkillInfoMsg>, I>>(base?: I): HeroSkillInfoMsg {
    return HeroSkillInfoMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HeroSkillInfoMsg>, I>>(object: I): HeroSkillInfoMsg {
    const message = createBaseHeroSkillInfoMsg();
    message.skillPoint = object.skillPoint ?? 0;
    message.skillScript = object.skillScript ?? "";
    message.skillSet = object.skillSet?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
