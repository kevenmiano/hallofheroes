// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: newactive/NewActiveInfo.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.newactive";

export interface NewActiveInfo {
  activeId: number;
  state: number;
  startTime: string;
  endTime: string;
  stage: number;
}

function createBaseNewActiveInfo(): NewActiveInfo {
  return { activeId: 0, state: 0, startTime: "", endTime: "", stage: 0 };
}

export const NewActiveInfo: MessageFns<NewActiveInfo> = {
  encode(message: NewActiveInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.activeId !== 0) {
      writer.uint32(8).int32(message.activeId);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    if (message.startTime !== "") {
      writer.uint32(26).string(message.startTime);
    }
    if (message.endTime !== "") {
      writer.uint32(34).string(message.endTime);
    }
    if (message.stage !== 0) {
      writer.uint32(40).int32(message.stage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NewActiveInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNewActiveInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.activeId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startTime = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.endTime = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.stage = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NewActiveInfo {
    return {
      activeId: isSet(object.activeId) ? globalThis.Number(object.activeId) : 0,
      state: isSet(object.state) ? globalThis.Number(object.state) : 0,
      startTime: isSet(object.startTime) ? globalThis.String(object.startTime) : "",
      endTime: isSet(object.endTime) ? globalThis.String(object.endTime) : "",
      stage: isSet(object.stage) ? globalThis.Number(object.stage) : 0,
    };
  },

  toJSON(message: NewActiveInfo): unknown {
    const obj: any = {};
    if (message.activeId !== 0) {
      obj.activeId = Math.round(message.activeId);
    }
    if (message.state !== 0) {
      obj.state = Math.round(message.state);
    }
    if (message.startTime !== "") {
      obj.startTime = message.startTime;
    }
    if (message.endTime !== "") {
      obj.endTime = message.endTime;
    }
    if (message.stage !== 0) {
      obj.stage = Math.round(message.stage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NewActiveInfo>, I>>(base?: I): NewActiveInfo {
    return NewActiveInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NewActiveInfo>, I>>(object: I): NewActiveInfo {
    const message = createBaseNewActiveInfo();
    message.activeId = object.activeId ?? 0;
    message.state = object.state ?? 0;
    message.startTime = object.startTime ?? "";
    message.endTime = object.endTime ?? "";
    message.stage = object.stage ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
