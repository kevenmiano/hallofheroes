// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.21.12
// source: battle/BattleItemMsg.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.road.yishi.proto.battle";

export interface BattleItemMsg {
  userId: number;
  battleId: string;
  serverName: string;
  uItemId: number;
  count: number;
  tempId: number;
  bagPos: number;
  skillTempId: number;
  sonType: number;
}

function createBaseBattleItemMsg(): BattleItemMsg {
  return {
    userId: 0,
    battleId: "",
    serverName: "",
    uItemId: 0,
    count: 0,
    tempId: 0,
    bagPos: 0,
    skillTempId: 0,
    sonType: 0,
  };
}

export const BattleItemMsg: MessageFns<BattleItemMsg> = {
  encode(message: BattleItemMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).int32(message.userId);
    }
    if (message.battleId !== "") {
      writer.uint32(18).string(message.battleId);
    }
    if (message.serverName !== "") {
      writer.uint32(26).string(message.serverName);
    }
    if (message.uItemId !== 0) {
      writer.uint32(32).int32(message.uItemId);
    }
    if (message.count !== 0) {
      writer.uint32(40).int32(message.count);
    }
    if (message.tempId !== 0) {
      writer.uint32(48).int32(message.tempId);
    }
    if (message.bagPos !== 0) {
      writer.uint32(56).int32(message.bagPos);
    }
    if (message.skillTempId !== 0) {
      writer.uint32(64).int32(message.skillTempId);
    }
    if (message.sonType !== 0) {
      writer.uint32(72).int32(message.sonType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BattleItemMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBattleItemMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.battleId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.serverName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.uItemId = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.tempId = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.bagPos = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.skillTempId = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.sonType = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BattleItemMsg {
    return {
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      battleId: isSet(object.battleId) ? globalThis.String(object.battleId) : "",
      serverName: isSet(object.serverName) ? globalThis.String(object.serverName) : "",
      uItemId: isSet(object.uItemId) ? globalThis.Number(object.uItemId) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      tempId: isSet(object.tempId) ? globalThis.Number(object.tempId) : 0,
      bagPos: isSet(object.bagPos) ? globalThis.Number(object.bagPos) : 0,
      skillTempId: isSet(object.skillTempId) ? globalThis.Number(object.skillTempId) : 0,
      sonType: isSet(object.sonType) ? globalThis.Number(object.sonType) : 0,
    };
  },

  toJSON(message: BattleItemMsg): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.battleId !== "") {
      obj.battleId = message.battleId;
    }
    if (message.serverName !== "") {
      obj.serverName = message.serverName;
    }
    if (message.uItemId !== 0) {
      obj.uItemId = Math.round(message.uItemId);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.tempId !== 0) {
      obj.tempId = Math.round(message.tempId);
    }
    if (message.bagPos !== 0) {
      obj.bagPos = Math.round(message.bagPos);
    }
    if (message.skillTempId !== 0) {
      obj.skillTempId = Math.round(message.skillTempId);
    }
    if (message.sonType !== 0) {
      obj.sonType = Math.round(message.sonType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BattleItemMsg>, I>>(base?: I): BattleItemMsg {
    return BattleItemMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BattleItemMsg>, I>>(object: I): BattleItemMsg {
    const message = createBaseBattleItemMsg();
    message.userId = object.userId ?? 0;
    message.battleId = object.battleId ?? "";
    message.serverName = object.serverName ?? "";
    message.uItemId = object.uItemId ?? 0;
    message.count = object.count ?? 0;
    message.tempId = object.tempId ?? 0;
    message.bagPos = object.bagPos ?? 0;
    message.skillTempId = object.skillTempId ?? 0;
    message.sonType = object.sonType ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
